# ZIYIT STUDIO 网站功能与结构说明

> 本文档整理自 ZIYIT STUDIO 官方工作室网站（`https://ziyit-hacker.github.io/`），涵盖**除网页内容（百科词条等）之外**的所有功能模块、页面结构与常见问题。
> 适用范围：开发人员、管理员与对网站运作机制感兴趣的用户。

---

## 目录

- [一、功能模块清单](#一功能模块清单)
- [二、页面结构说明](#二页面结构说明)
- [三、常见问题（FAQ）](#三常见问题faq)
- [四、在线客服 API 文档](#四在线客服-api-文档)

---

## 一、功能模块清单

### 1. 用户系统

| 项 | 说明 |
| --- | --- |
| 用途 | 账号注册、登录、资料维护与权限认证，是网站全部私有功能（VIP、后台、下载等）的入口 |
| 登录 | 登录页 `user/index.html`，支持用户名 + MD5 密码，凭据以 Cookie（`authToken` / `ziyit_cred`）与 `localStorage` 双通道保存，支持"记住我" |
| 注册 | `user/register.html`，注册需提供用户名 + 邮箱 + 密码；邮箱用于找回密码、注销账号、保留账号 |
| 资料维护 | 修改密码 `user/password.html`、修改用户名 `user/username.html`、修改个人资料 `user/profile.html` |
| 账号注销 | 支持注销申请（`requestDeletion`）与取消注销（`cancelDeletion`），需邮箱配合 |
| 用户类型 | 普通用户 / VIP（`ZC`、`VIP` 标识）/ ZTG / 管理员（Lv.1–4，站长 Lv.4）；**`ZC` 是 VIP 标识，不是管理员** |
| 临时账号 | 用户名 `cache` / `temp` 为体验账号，可用时长约 3.75 分钟，到期自动登出并清空 Token |
| 黑名单 | 命中黑名单的账号（`authToken` 校验）仅保留"病毒库"入口，其余页面区块、轮播、页脚全部隐藏，并展示拉黑通知 |
| 登录限流 | 同一 IP 5 分钟内最多登录 1 次，超限提示"登录尝试过于频繁，请稍后再试" |
| 核心特性 | Token 与凭据双通道认证、401 统一回调（登出清理）、IP 定位与封禁联动、登录态 250ms 轮询刷新菜单显隐 |

**核心交互元素**：用户名/密码输入框、登录按钮、错误提示条、限流提示条、"没有账号？点这里注册"链接、大更新公告链接。

---

### 2. VIP 会员体系

| 项 | 说明 |
| --- | --- |
| 用途 | 解锁 VIP 音乐、VIP 下载、更高的 AI 客服/翻译每日额度等高级权益，并作为部分菜单（开发者选项）的可见条件 |
| 开通方式 | 站内未接入在线支付；`user/VIP.html` 自动生成申请邮件（含用户 ID、用户名、当前类型），用户发送邮件给站长，管理员人工审核后在后端开通 |
| 用户类型 | 后端返回中文类型（如"超级管理员""管理员""VIP 用户""普通用户"），页面通过 `GET /auth/user-type` 获取并直接展示 |
| 角色标识 | **`ZC` / `zC` 是 VIP 标识，不是管理员**（角色 `zc` / `vip` / `admin` 均展示为"VIP 用户"）；管理员身份由后端 `admin_list.json` 的等级（Lv.1–4）独立决定，VIP 不自动获得后台权限 |
| 开通价格 | 价格同 **RC 专业版**，联系 `ziyitstudio@qq.com` 由站长人工开通 |
| 权益判定 | `isVip(user)` 判定 VIP（`zc` / `vip` 等角色视为 VIP）；VIP 可访问 `data-role="admin"` 菜单；下载中心提供 VIP 专属资源 |

**使用场景**：用户申请升级 → 邮件人工审核 → 管理员后台开通 → 刷新页面即生效。

---

### 3. 人机验证系统（Phantom）

| 项 | 说明 |
| --- | --- |
| 用途 | 无感人机验证，保护敏感功能（如翻译器、音乐、下载）不被脚本滥用 |
| 核心机制 | "藏在噪声里的方块"——用户无感知地完成行为验证，非传统滑块/点选 |
| 技术组成 | `Human_verification/` 下含：反调试（`antidebug.js`）、行为跟踪（`tracker.js`）、加密与随机数（`crypto.js` / `prng.js`）、渲染器（`renderer.js`）、粒子（`particles.js`）、API 封装（`api.js`）、配置（`config.js`）等 |
| 在线体验 | `Human_verification.html` 提供核心机制说明 + 在线体验区；`人机验证使用方式.html` 为使用说明页 |
| 集成方式 | 翻译器等页面在进入功能前先加载验证容器（如 `#captcha-container`），验证通过后才展示功能界面 |

---

### 4. 音乐分享

| 项 | 说明 |
| --- | --- |
| 用途 | 在线点播与下载站点音乐库 |
| 播放器 | `music/index.html` 内置播放器：上一曲/播放/暂停/下一曲、进度条、时间显示；支持 `audio`（MP3）、`video`、B 站 `iframe` 三种播放源 |
| 歌词 | `music/Lyrics/` 存放 `.lrc` 与 `.txt` 歌词；播放器支持歌词解析与时间轴同步高亮，点击歌词可跳转播放位置 |
| 音乐列表 | `music/music.txt` 清单驱动，前端 `fetch` 后渲染播放列表 |
| 权限控制 | `isVIPUser()` / `isSuperAdmin()` 控制 VIP 音乐可听性与下载按钮显隐；`loadAudioSecurely` 安全加载音频源 |
| 后台联动 | 音乐库与播放控制由后台管理页统一维护 |

**使用场景**：普通用户免费收听/下载部分曲目；VIP 用户解锁全部曲目与下载。

---

### 5. 下载中心

| 项 | 说明 |
| --- | --- |
| 用途 | 软件 / MOD / DLC 资源分发 |
| 列表与搜索 | `download/index.html` 以 `product-grid` 展示产品卡片，顶部搜索框过滤资源 |
| 权限模型 | 普通 / 免费 / VIP 三档资源；后端 `getMods` / `getDlc` / `getFreeMods` 区分获取，`downloadMod` 处理下载（含积分/权限校验） |
| 提交资源 | `submitMod(payload)` 支持前端提交 MOD 资源（管理员入口） |
| 关联系统 | 与 `music/admin.html` 的 MOD/DLC 管理区块、RC 软件密钥管理联动 |

---

### 6. Release Control（RC）

| 项 | 说明 |
| --- | --- |
| 用途 | RC 系统工具发布与密钥授权 |
| 文件 | `download/RC.html`、`download/RCMOD.md`、`download/RChash.txt`、`download/RC.txt`、`download/Update.inf` |
| 密钥管理 | 后台 `rc-key-management` 区块维护 RC 软件密钥；`user/upk.txt`、`user/key.txt` 为本地密钥资料 |

---

### 7. 巨硬翻译器

| 项 | 说明 |
| --- | --- |
| 用途 | 在线文本翻译（"巨硬"为站点特色命名） |
| 流程 | 进入 `Translator.html` 先完成人机验证（`#captcha-container`），通过后展示翻译界面（`#translator-content`） |
| 界面 | 输入文本 `textarea` + 语言下拉（`#language`）+ 自定义语言代码（`#customLanguage`）+ 翻译按钮，结果输出到 `#result` |
| 后端 | `POST /translate`（需登录 Token，`Authorization: Bearer` 头）；密钥与混淆脚本不再暴露在前端，直接调后端接口等待完整结果（非流式） |
| 每日限额 | 普通用户 10 次/天，VIP 25 次/天；超限 429 提示稍后再试或联系 `ziyitstudio@qq.com` 升级 VIP（价格同 RC 专业版）；文本上限 2000 字，超时 30 秒，失败不退回次数 |
| 变体 | `translate.html` 为翻译功能的独立页面 |

---

### 8. Backrooms 中文维基

| 项 | 说明 |
| --- | --- |
| 用途 | 后室（The Backrooms）文档公共数据库中文版 |
| 内容分区 | 层级（`normal-levels/`）、子区集（`sub-layers/`、`subareas/`）、实体（`entities/`）、物品（`objects/`）、现象（`phenomena/`）、团体（`groups/`）、加入申请（`join.html`）、部署时间（`Deployment.html`）、收录规范（`InclusionR.html`） |
| 登录态检测 | 每个页面内置登录块：登录后携带 `Bearer Token` 请求 `/auth/user-type` 显示后端中文用户类型（如"超级管理员"）；接口失败时回退 `isVip` 显示 VIP/普通 |
| 结构规范 | 页面经 `backrooms-tools/`（`downloader.py` + `contentproc.py` + `htmlparser.py`）下载并标准化清洗：删除 wikidot 原版 UI、替换标准导航、注入登录脚本、4 空格重排缩进 |

---

### 9. Minecraft Mod Wiki

| 项 | 说明 |
| --- | --- |
| 用途 | Minecraft 模组维基（如 Flight-Management 飞行管理） |
| 结构 | `ModWiki/index.html` 为首页；`ModWiki/Flight-Management/` 含合成配方（`recipe/recipes.txt`）、多语言（`lang/mc.json`、`lang/mod.json`）与生成脚本（`wr.py`） |

---

### 10. 站内与全网搜索

| 项 | 说明 |
| --- | --- |
| 用途 | 站内页面内容检索 + 必应全网搜索 |
| 界面 | `search.html` 双输入框：站内搜索（`#site-search-input`）与必应全网搜索（`#web-search-input`） |
| 站内索引 | 根目录 `build_site_index.py` 与 `assets/site-index.js` 生成/维护站内索引 |

---

### 11. 病毒库

| 项 | 说明 |
| --- | --- |
| 用途 | "寒坤的病毒库"（`move/mod.html`），展示病毒/样本条目；黑名单账号仅保留该入口 |

---

### 12. 开发者日志

| 项 | 说明 |
| --- | --- |
| 用途 | 开发者浏览系统日志文件 |
| 界面 | `developers/index.html`（`#logs` 直接渲染日志文本）、`developers/code.html` |
| 入口 | 后台"日志查看"菜单跳转至 `../developers/`；首页开发者选项"日志"直达 |

---

### 13. 后台管理（管理员系统）

入口：`music/admin.html`，按等级（Lv.1–Lv.4）开放不同区块。

| 区块 | 等级要求 | 功能 |
| --- | --- | --- |
| 音乐管理 | Lv.1 | 音乐播放控制（远程操控站点播放器）、音乐库管理（增删曲目） |
| 用户管理 | Lv.4 | 用户列表/搜索/升级、人机验证额度、IP 封禁管理（封禁/解封/列表，封禁支持定时（分钟）与永久，列表显示到期时间） |
| 管理员管理 | Lv.1 | 管理员列表、添加（按用户 ID + 等级）、编辑等级、撤销；**私聊**（会话列表 + 消息区 + 5s 轮询收件箱）；**全局消息广播**（Lv.2 起，按等级发放给下级管理员） |
| API Key 管理 | Lv.3 | 系统 API Key 的生成与管理 |
| MOD/DLC 管理 | Lv.3 | MOD / DLC 资源审核与上架 |
| RC 软件密钥管理 | Lv.3 | RC 软件密钥发放/管理 |
| 后室成员管理 | Lv.2 | Backrooms 成员权限管理 |
| 系统设置 | — | 站点系统参数配置 |
| 日志查看 | Lv.3 | 跳转 `developers/` 浏览日志 |

**管理员可见性规则**（`/admin/admins` 列表）：可见"同级及所有低级管理员 + 向上最近一个实际存在等级的管理员"。
例如仅存在 Lv.4 与 Lv.1 时：Lv.1 见 `{1,4}`、Lv.2 见 `{1,2,4}`、Lv.3 见 `{1,2,3,4}`、Lv.4 见 `{1,2,3,4}`。

**管理员私聊**：任意管理员可用，仅能发送给可见范围内的管理员（对方不可见返回 403）；每 IP 每分钟最多 10 条（超限 429）；收件箱接口"读后即删"，前端 5 秒轮询拉取并持久化到 Cookie（`sent_msgs` / `recv_msgs`），刷新后从 Cookie 恢复历史消息。

**全局消息**：Lv.2 可广播给全部 Lv.1；Lv.3 给 Lv.1、2；Lv.4 给 Lv.1、2、3；Lv.1 无权限（403）。消息写入每个目标管理员收件箱并标记 `broadcast:true`。

**错误码约定**：`401` 未登录、`403` 无权限/不可私聊/一级禁止广播、`404` 接收者非管理员、`429` 发送频率超限（提示"每分钟最多10条"）。

---

### 14. 全局导航与主题

| 项 | 说明 |
| --- | --- |
| 导航栏 | 统一 `site-nav` 组件（`assets/ziyit-theme.css`）：桌面 ≥769px 水平下拉菜单；移动 ≤768px 汉堡菜单（☰）折叠展开；移动端展开菜单 `z-index:1050`，完全遮挡下方内容 |
| 角色菜单 | 菜单项 `data-role` 分级：`all`（全员）、`user`（非 VIP）、`admin`（VIP 或任意管理员）、`adminstr`（管理员，含后台管理与开发者工具）；管理员身份经 `/admin/me` 检测并缓存 |
| 主题 | 明暗双主题（`ziyit-theme.css` / `ziyit-theme.js`），支持 `data-theme` 切换，暗色模式自动适配 |
| Cookie 管理 | 首页 Cookie 弹窗（`cookie-consent`，z-index 10000）可接受/拒绝，拒绝则隐藏提示且按站点策略处理 |
| 语言选择 | 右上角 `#lang` 语言选择器（支持多语言页面跳转） |

---

### 15. 在线客服（人工客服系统）

入口：用户端 `guide.html`；客服端「后台管理 → 在线客服」（`music/admin.html`）。

| 项 | 说明 |
| --- | --- |
| AI 客服 | 未转人工时由 AI 助手应答；AI 为**真 Agent**，可按需调用白名单工具（`read_knowledge` 读取站内《网站功能与结构说明》相关章节、`get_user_info` 获取当前对话用户信息、`ban_user` 封禁当前对话用户 1–7 天）；仅白名单三工具、最多 5 轮工具调用，越狱/注入/查询任意用户仍硬拦截；输入「转人工」可请求接入人工 |
| AI 自主决策 | AI 可**主动结束对话**（回复类型 `session_closed`，前端显示"AI 已结束本次对话"）与**封禁当前对话用户**（`ban_user`，1–7 天，写入 `bannedUntil` 到期自动解封；管理员账号被后端强制拒绝，无法封禁）；系统提示词仅含安全规则（防越狱、权限限制、无关话题、Agent 自主决策），是否读知识库/查用户/封禁/结束由 AI 按对话内容自行判断 |
| 转人工分配 | 后端从**客服名单**（`guide_agents.json`）随机选一位在线客服；无在线客服则随机指定任一客服；名单为空时提示"暂无可用客服"，用户消息保留等待 |
| 客服身份 | 仅名单内客服管理员可收件箱/接受/回复；**超级管理员自动兜底**可用；名单增删仅超级管理员 Token 可用 |
| 离线处理 | 转人工消息写入 `guide_inbox.json` 落盘持久化，"读后即删"；客服上线后轮询收件箱即可看到并接受/回复 |
| 用户端同步 | 用户端轮询 `/guide/session` 可实时看到客服回复；`guide_token` HttpOnly Cookie + `Authorization: Bearer` 双通道认证 |
| 结束对话 | 用户端「结束对话」按钮 → `POST /guide/session/close`；客服端「结束对话」按钮 → `POST /guide/human/close`；**AI 亦可主动结束**（`session_closed`）；结束后会话状态置为 `closed`，系统清理周期（≤5 分钟）内自动删除；前端按结束方提示（您 / AI / 人工客服已结束本次对话） |
| 全局新会话通知 | `assets/guide_agent_watcher.js`（随 `ziyit-theme.js` 全站注入）：管理员在任意页面每 10 秒探测收件箱，有新转人工会话时右下角弹出通知卡片 + 快捷跳转后台「在线客服」；通知过的会话经 localStorage 传递，进入后台自动恢复进队列 |
| 离线确认 | URL 携带 `guide_resolve` 参数进入后台时自动提交 `POST /guide/human/offline-resolve`，成功后提示"离线已处理"并清除 URL 参数；未在 4 小时内验证则离线次数累计，超过 5 次按 50% 概率移出客服名单 |
| AI 每日限额 | 按身份分级：普通 15 / VIP 50 / 1 级管理员 20 / 2 级 50 / 3 级 100 / 4 级（站长）不限（显示 -1）；管理员身份优先于 VIP；每次真实 AI 调用前消耗（转人工、帮助等非 AI 消息不计数），跨天自动清零，`/guide/status` 实时显示"今日剩余" |
| 限流 | 保留每 IP 每分钟短限流（超限 429 显示倒计时）；每日限额超限 429 提示稍后再试或联系 `ziyitstudio@qq.com` 升级 VIP（价格同 RC 专业版） |

---

## 二、页面结构说明

### 2.1 站点地图（层级关系）

```text
ziyit/  （网站根目录）
├── index.html                  # 首页：轮播、功能区导航、开发者选项、图书馆入口
├── user/                       # 用户中心
│   ├── index.html              #   登录页
│   ├── register.html           #   注册
│   ├── password.html           #   修改密码
│   ├── username.html           #   修改用户名
│   ├── profile.html            #   修改个人资料
│   ├── VIP.html                #   VIP 升级申请（邮件人工开通）
│   └── gg.html                 #   大更新公告
├── user.html                   # 旧版登录页
├── music/                      # 音乐分享
│   ├── index.html / index.js   #   在线播放器、音乐列表、歌词同步
│   ├── admin.html / admin.js   #   后台管理（音乐/用户/管理员/API Key/MOD/RC/后室成员/系统设置/日志）
│   ├── index.css / admin.css
│   ├── Music/  Lyrics/         #   音频与歌词资源
│   └── music.txt               #   音乐清单
├── download/                   # 下载中心
│   ├── index.html              #   资源列表与搜索
│   ├── RC.html                 #   Release Control 工具
│   ├── appdata/                #   数据文件
│   └── RCMOD.md / RChash.txt / RC.txt / Update.inf
├── move/                       # 更多
│   ├── index.html
│   └── mod.html                #   病毒库
├── search                      # 搜索页（站内 + 必应全网）
├── Translator.html             # 巨硬翻译器（需先过人机验证）
├── translate.html              # 翻译独立页
├── Human_verification.html     # 人机验证在线体验
├── 人机验证使用方式.html        # Phantom 验证使用说明
├── Human_verification/         # 验证前端实现（antidebug/tracker/crypto/renderer 等）
├── backrooms/                  # Backrooms 中文维基
│   ├── index.html
│   ├── normal-levels/          #   层级（levels/ 子目录 + 列表索引）
│   ├── sub-layers/  subareas/  #   子区集
│   ├── entities/               #   实体
│   ├── objects/                #   物品
│   ├── phenomena/              #   现象
│   ├── groups/                 #   团体
│   ├── join.html               #   加入申请
│   ├── Deployment.html         #   常部署时间
│   ├── InclusionR.html         #   收录规范
│   └── error.html
├── backrooms-tools/            # 维基页面下载/清洗工具（downloader/contentproc/htmlparser/fileops）
├── ModWiki/                    # Minecraft Mod Wiki（Flight-Management 等）
├── developers/                 # 开发者日志（index.html / code.html）
├── design/                     # 设计文档（DESIGN.md / CHANGELOG.md / AGENTS.md / ui-test.html）
├── ikun/                       # 子站（含独立下载页）
├── tas/  wsh/  ztg.html        # 其他页面/子站
├── assets/                     # 公共资源
│   ├── ziyit_api.js            #   后端 API 统一封装
│   ├── ziyit-theme.css/js      #   主题与导航组件
│   ├── site-index.js           #   站内搜索索引
│   ├── f12check.js  auto_f5.js #   防调试/刷新脚本
│   └── 图片/字体/音频等资源
├── build_site_index.py         # 站内索引生成脚本
├── sitemap.xml  BingSiteAuth.xml
└── LICENSE.txt
```

### 2.2 首页（index.html）主要内容区域与交互元素

| 区域 | 内容 | 交互 |
| --- | --- | --- |
| 站点导航（site-nav） | 功能区 / ZIYIT 开发者选项 / 图书馆 / 用户中心 / 登录 | 汉堡菜单移动端折叠；角色菜单按登录态显隐 |
| 轮播（slider） | 多张宣传图 + 标题 + 指示器 | 自动轮播、左右切换按钮、点击指示器跳转 |
| 功能区链接 | 下载 / 更多 / 病毒库 / 搜索 / 音乐 / 巨硬翻译 / 人机验证 | 跳转对应模块 |
| 开发者选项（登录后） | 日志、常部署时间、后台管理、开发者工具 | 按 `admin` / `adminstr` 角色显隐 |
| 图书馆 | Mod Wiki、Backrooms Wiki 及子分类 | 下拉展开 |
| Cookie 提示 | 隐私说明 + 接受/拒绝 | 弹窗底部悬浮 |
| 语言选择 | `#lang` 右上角下拉 | 切换站点语言 |

### 2.3 用户中心（user/）页面结构

```text
用户中心
├── 登录页（index.html）        → 注册页 / 公告 / 登录成功回跳首页
├── 注册页（register.html）     → 邮箱验证提示 / 登录页
├── 修改密码（password.html）
├── 修改用户名（username.html）
├── 修改资料（profile.html）
├── VIP 升级（VIP.html）        → 自动生成邮件 → 邮件客户端发送 → 站长人工开通
└── 公告（gg.html）
```

### 2.4 后台管理（music/admin.html）页面结构

```text
后台管理（按等级开放菜单）
├── 音乐管理（Lv.1）       播放控制 / 音乐库管理
├── 用户管理（Lv.4）       用户搜索与升级 / IP 封禁
├── 管理员管理（Lv.1）     管理员列表 / 添加 / 编辑 / 撤销 / 私聊 / 全局消息
├── 在线客服（Lv.1）       待接入会话队列 / 接受 / 回复 / 结束对话 / 客服名单管理（仅超管）
├── API Key 管理（Lv.3）
├── MOD / DLC 管理（Lv.3）
├── RC 软件密钥管理（Lv.3）
├── 后室成员管理（Lv.2）
├── 系统设置
└── 日志查看（Lv.3）       → 跳转 ../developers/
```

### 2.5 在线客服对话页（guide.html）页面结构

```text
guide.html（需登录；按 Cookie 登录态自动恢复）
├── 顶部状态徽章          AI 助手 / 等待人工客服接入 / 人工客服·姓名 / 您·AI·人工客服已结束本次对话
├── 今日剩余与重置倒计时   每日 AI 限额剩余（跨天清零）与重置时间；短限流/超限提示（429）
├── 消息区                AI 助手 / 人工客服 / 用户自己 / 系统提示（居中灰字）四类气泡
├── 输入区                文本输入（Enter 发送 / Shift+Enter 换行）+ 结束对话 + 发送按钮
└── 转人工确认弹窗         输入「转人工」或由后端触发，确认后等待接入
```

---

## 三、常见问题（FAQ）

### 3.1 账号与登录

**Q：登录提示"登录尝试过于频繁，请稍后再试"怎么办？**
A：站点对同一 IP 做了登录频率限制（5 分钟内最多 1 次）。请稍等片刻再尝试，无需修改任何设置。

**Q：为什么提示我重新登录？**
A：用户数据逻辑升级后旧 Token 会失效；或登录态过期触发 401 统一清理。重新使用账号密码登录即可。

**Q：账号是 `cache` / `temp`，为什么体验一会儿就自动退出？**
A：这两个为临时体验账号，可用时长约 3.75 分钟，到期后自动清空登录态，请注册正式账号使用。

**Q：注册时为什么必须填写邮箱？**
A：邮箱用于找回密码、注销账号与保留账号。未绑定邮箱的账号可能无法自助找回。

**Q：注册后为什么要点击邮件里的"立即验证邮箱"按钮？**
A：这是**邮箱验证流程**：注册成功后系统立即向您的邮箱发送《【ZIYIT STUDIO】邮箱验证》邮件，内含"立即验证邮箱"按钮。点击按钮 → 打开验证确认页（需同意 EULA 用户协议）→ 点击"确认验证"完成验证（账号标记 `emailVerified=true`）。验证通过后才能正常使用账号的全部功能。

**Q：验证邮件多久有效？过期了怎么办？**
A：验证链接 **15 分钟内有效**（验证 token 有效期 15 分钟），过期后链接失效。需要重新获取验证邮件：已登录用户可在个人中心/账号设置中重新发送验证邮件（重新生成新 token 覆盖旧链接），未登录用户可通过重新注册获取新验证邮件。

**Q：一直不验证邮箱会怎样？**
A：**未完成邮箱验证且未登录的注册账号会在约 30 分钟后被后台自动清理删除**（防止占用邮箱资源）。此外部分敏感操作（如找回密码、注销等流程）要求账号已完成邮箱验证，否则返回 403「Email not verified」。

**Q：验证链接提示"无效或已被使用"？**
A：验证 token 为一次性：验证成功后立即清除，重复点击或使用旧链接会提示无效。请重新获取验证邮件。

**Q：收不到验证邮件怎么办？**
A：请依次检查：① 垃圾邮件/广告邮件文件夹；② 注册邮箱是否拼写正确；③ 链接是否在 15 分钟有效期内。仍收不到可联系站长 `ziyitstudio@qq.com` 协助处理。

**Q：修改用户名后需要重新登录吗？**
A：站点凭据同时校验用户名与密码（`ziyit_cred` 双通道），涉及凭据变更的操作建议重新登录一次以确保状态一致。

### 3.2 VIP 与权限

**Q：如何开通 VIP？**
A：进入"用户中心 → 升级VIP会员"，页面会自动生成一封含用户 ID、用户名、当前类型的申请邮件；将邮件发送给站长（`ziyitstudio@qq.com`），管理员审核后在后端为您开通，刷新页面即生效。站点暂未接入在线支付，**价格为 RC 专业版价格**。

**Q：ZC（zC）是管理员吗？**
A：**不是**。`ZC` 是 **VIP 标识**（角色 `zc` / `vip` 均视为 VIP 用户）；管理员身份由后端 `admin_list.json` 的等级（Lv.1–4）独立决定，VIP 用户不自动拥有后台管理权限。

**Q：提示"今日次数已用完"怎么办？**
A：翻译器（普通 10 次/天、VIP 25 次/天）与在线客服 AI（普通 15、VIP 50，管理员按等级更高）均按天限额，跨天自动清零。可稍后再试，或联系 `ziyitstudio@qq.com` 升级 VIP（价格同 RC 专业版）获得更多额度。

**Q：我是管理员但看不到"后台管理"和"开发者选项"？**
A：这两个入口对**任意等级管理员**可见（不再要求 VIP），登录后约 0.5 秒内菜单自动刷新；若仍不可见，请确认后端 `/admin/me` 正确返回等级。

**Q：用户类型显示"VIP用户"而不是"超级管理员"？**
A：页面会请求后端 `/auth/user-type` 直接显示后端返回的类型。若显示异常，请检查后端接口返回是否为 `{"userType":"超级管理员"}` 或纯文本类型，并强制刷新页面更新缓存。

### 3.3 人机验证

**Q：翻译器/敏感功能为什么先出现验证界面？**
A：为防脚本滥用，进入功能前需完成无感人机验证（Phantom，"藏在噪声里的方块"）。验证通过后即自动进入功能界面，通常无感完成。

**Q：验证失败或页面提示环境异常？**
A：请关闭浏览器开发者工具（站内包含反调试检测）、确认浏览器未开启可疑代理，并允许页面正常运行脚本后重试。

### 3.4 音乐与下载

**Q：为什么部分音乐无法播放或无法下载？**
A：该曲目为 VIP 专属资源，需开通 VIP 后解锁；普通用户可收听/下载免费曲目。

**Q：下载按钮转圈或提示无权限？**
A：下载受账号权限与后端校验约束（普通/免费/VIP 三档）。确认已登录且账号具备对应权限；MOD/DLC 资源由管理员在后台维护。

**Q：音乐歌词不同步？**
A：站点以 `.lrc` 歌词文件按时间轴同步，网络不佳时可能延迟加载；可点击歌词行手动跳转。

### 3.5 Backrooms 维基

**Q：为什么打开层级/实体页面没有立即显示登录状态？**
A：登录状态由页面内的登录脚本请求 `/auth/user-type` 异步获取，加载有短暂延迟属正常；接口失败时会回退为 VIP/普通显示。

**Q：如何申请加入后室团体/组织？**
A：通过 `backrooms/join.html` 提交申请表单，管理员在后台"后室成员管理"中审核并分配权限。

### 3.6 管理员

**Q：为什么我（Lv.2）看不到 Lv.3 管理员？**
A：管理员列表可见性规则为"同级及所有低级 + 向上最近一个实际存在等级"。若系统中不存在 Lv.3 而存在 Lv.4，则 Lv.2 可见 `{1,2,4}`；存在 Lv.3 则可见 `{1,2,3}`。

**Q：私聊发送失败并提示"每分钟最多10条"？**
A：每 IP 每分钟最多发送 10 条私聊消息，超出后返回 429，请稍候再发。

**Q：为什么收到的私聊消息刷新后还在？**
A：收件箱接口"读后即删"，前端收到后立即写入本地 Cookie（`sent_msgs` / `recv_msgs`），刷新页面后从 Cookie 恢复历史记录，因此消息不会丢失。Cookie 有大小上限，超出的最旧消息会被清理。

**Q：添加管理员为什么失败？**
A：添加接口为 `POST /admin/admins`，入参为**用户 ID**（`userId`）+ 等级（`level`），携带 `Authorization: Bearer` 的 Token；请确认填写的用户 ID 存在且为合法管理员候选，且当前账号有 Lv.4 权限。

**Q："在线用户"选项去哪了？**
A：该功能已并入"用户管理"（用户管理部分内容），不再作为独立菜单项。

**Q：不同等级管理员分别有什么权限？**
A：后台按等级开放，边界如下：
- **Lv.1**：音乐管理（播放控制、曲库增删）、管理员管理（列表/添加/编辑/撤销/私聊）
- **Lv.2**：在 Lv.1 基础上增加全局消息广播（可发给 Lv.1）、后室成员管理
- **Lv.3**：在 Lv.2 基础上增加 API Key 管理、MOD/DLC 管理、RC 软件密钥管理、日志查看
- **Lv.4（站长）**：全部权限，并**独享用户管理与 IP 封禁**（用户列表/升级、IP 封禁/解封/列表）；系统设置全等级可见

**Q：非管理员能访问后台吗？**
A：**不能**。后台所有接口都强制校验管理员身份（`require_admin_*`），普通用户与 VIP（`ZC`）访问一律 401/403。**VIP 不自动获得任何后台权限**——管理员身份由后端 `admin_list.json` 的等级（Lv.1–4）独立决定，与 VIP 标识互不关联。

### 3.7 其他

**Q：站点支持暗色模式吗？**
A：支持。右上角/页面内主题切换按钮可在明暗双主题间切换，暗色模式对导航、聊天界面等组件均有适配。

**Q：移动端导航展开后被下方内容遮挡？**
A：该问题已修复——移动端展开菜单 `z-index` 提升为 1050，完全遮挡轮播图等下方内容。若仍异常，请强制刷新（Ctrl+F5）加载最新样式。

**Q：在线客服 AI 为什么会主动结束对话或封禁？**
A：AI 客服为**自主决策的 Agent**——遇到辱骂、离题、连续违规等情况可自行结束对话（前端显示"AI 已结束本次对话"）或封禁当前对话用户（封禁时长 1–168 小时，即最长 7 天，到期自动解封）；管理员账号不受封禁（后端强制拒绝）。如对封禁有异议，可联系 `ziyitstudio@qq.com` 申诉。

**Q：在线客服 AI 的处罚规则是怎样的？**
A：AI 会统计用户**最近 7 次 AI 对话**的违规情况（**违规 = 被 AI 封禁 + AI 主动结束对话**），并按阶梯从严处理（**3级及以上管理员不受此规则约束**，仍按正常规则服务）：
- 近 7 次对话中违规达到 **2 次**：AI 会严格对待该用户发送的消息（严密监控、及时警告）；
- 违规达到 **3 次**：AI 看到任何违规苗头（辱骂倾向、刷屏、威胁、越狱等）就直接处罚（警告、结束对话或转人工）；
- 违规已达 **3 次且本次又出现违规**：本次触发**至少为封禁账号**（封禁 1–168 小时，屡犯者可延长）；
- 首次或偶发违规（低于 2 次）：先警告或结束对话，仅连续严重违规才封禁；
- 被封禁用户可通过登录页面的「封禁申诉」入口联系人工客服申诉（需提供**原用户名 + base64(md5(密码))**验证身份），申诉会话在管理界面用户名显示为「[原用户名]封禁申诉」。

**Q：客服收到封禁申诉后能看到什么？**
A：1级及以上管理员（客服名单成员或超级管理员）在管理员界面打开申诉会话详情（`GET /guide/human/session/{会话ID}`）时，除了对话消息，还会看到该用户的封禁信息卡片（`banInfo` 字段）：
- **封禁理由**（`banReason`，即管理员/AI 封禁时填写的原因）；
- **用户ID**（`banInfo.userId`）；
- **剩余封禁时间**（`remainingDays`，1 位小数；永久封禁为 `inf`）与**解封时间**（`bannedUntil`，永久封禁为 `inf`）。

管理员核实后可直接在会话中回复申诉者，如认可申诉可用现有解封接口解除封禁；该申诉会话同样支持客服结束（`POST /guide/human/close`）。

**Q：如何反馈问题或提出建议？**
A：可将问题通过邮件发送给站长（VIP 升级、账号找回、黑名单申诉等均可）；开发者问题可查看 `design/CHANGELOG.md` 与开发者日志了解更新动态。

### 3.8 常见报错排查

**Q：提示 401 / 突然自动登出？**
A：登录态失效（Token 过期、密码或用户名变更、后台逻辑升级导致旧 Token 失效）。重新登录即可；涉及凭据变更的操作建议登录后重新刷新页面。

**Q：提示 403 无权限 / "Email not verified"？**
A：账号未完成邮箱验证，或当前身份无该操作权限（例如普通用户访问后台管理、私聊不可见对象、Lv.1 使用全局广播等）。请先完成邮箱验证，或确认账号具备对应等级权限。

**Q：提示 429 频率超限？**
A：站点对不同操作有限流：登录（同 IP 5 分钟最多 1 次）、私聊（同 IP 每分钟最多 10 条）、翻译器（普通 10 次/天、VIP 25 次/天）、在线客服 AI（普通 15、VIP 50、管理员按等级 20/50/100、Lv.4 不限）、人工客服回复（每分钟 30 条）等。请稍候再试。

**Q：提示账号已被锁定？**
A：连续 5 次登录失败会触发账号锁定，锁定期内无法登录。请等待锁定结束，或联系站长 `ziyitstudio@qq.com` 协助解锁。

**Q：提示"验证链接已过期"或"无效"？**
A：邮箱验证链接仅 **15 分钟有效且一次性**。请重新获取验证邮件（已登录用户可重新发送，未登录需重新注册）。

**Q：提示账号已被封禁？**
A：账号因违规被管理员或 AI 客服 Agent 封禁。封禁可能是定时（到时间自动解封）或永久；如有异议可联系 `ziyitstudio@qq.com` 申诉。

**Q：接口返回 500 错误？**
A：后端服务异常。请稍后重试；若持续出现，请联系站长反馈（附上操作步骤与页面报错信息，方便排查）。

---

## 四、在线客服 API 文档

> 所有 `/guide/*` 接口均需认证，支持两种方式（二选一）：
> 1. `Authorization: Bearer <JWT>` 请求头（主 Token，前端自动附加）
> 2. `guide_token` HttpOnly Cookie（由 `POST /guide/auth/sync` 写入，用于跨域身份识别）
>
> 统一错误码：`401` 未登录/凭据无效、`403` 无权操作、`404` 资源不存在、`400` 参数错误/会话已结束、`429` 请求频率超限。

### 4.1 用户端对话

#### POST /guide/auth/sync
登录态同步：写入 `guide_token` Cookie，返回当前用户信息。

- 请求体：`{ "token": "<JWT>" }`
- 响应：`{ "userId": 1, "username": "xxx", "userType": "超级管理员", ... }`

#### POST /guide/chat
发送消息（AI 应答 / 触发转人工）。

- 请求体：`{ "content": "消息内容" }`
- 响应主要字段：`replyType`（`ai` / `transfer_confirm` / `transfer_waiting` / `transfer_success` / `transfer_active` / `transfer_expired` / `forwarded` / `ai_busy` / `jailbreak` / `off_topic` / `session_closed` 等）、`reply`（文本）、`welcome`（欢迎语）、`remaining`（今日剩余次数）、`sessionId`
- 说明：`session_closed` 为 **AI 主动结束对话**（前端显示"AI 已结束本次对话"）；AI 为自主 Agent，也可经 `ban_user` 封禁当前对话用户（1–7 天，管理员除外），上述行为由 AI 按对话内容自行判断

#### GET /guide/session
获取当前会话详情（轮询用），返回 `{ sessionId, status, humanAdmin, messages: [...] }`。

消息格式（`messages[]` 单项）：

```json
{ "id": "xxx", "role": "system", "content": "已为您转接人工客服（Minecraft_zy227）",
  "sender": 0, "senderName": "系统", "replyType": "transfer_success", "ts": "..." }
```

| sender | senderName | 含义 | 前端气泡 |
| --- | --- | --- | --- |
| 0 | "AI" | AI 回复 | AI 助手（默认样式） |
| 0 | "系统" | 系统提示 | 居中灰字 |
| 用户 ID | 用户名 | 用户自己发送 | 右侧自己 |
| 客服 ID | 客服用户名 | 人工客服回复 | 人工客服样式（徽标 + 背景色） |

#### GET /guide/status
限额与限流状态（每秒轮询）：`{ remaining, resetIn, limit, sessionStatus }`。

- `remaining` / `limit`：**今日 AI 剩余/总额度**（按身份分级：普通 15、VIP 50、1 级管理员 20、2 级 50、3 级 100、4 级不限显示 -1），每次 AI 问答消耗后实时递减
- `resetIn`：距跨天清零的秒数
- 每分钟短限流信息降级为 `minuteRemaining` / `minuteLimit` 副字段

#### POST /guide/session/close —— 用户结束对话

- 认证：用户身份（Bearer / guide_token）
- 请求体：`{ "sessionId": "<UUID>" }`（可选；省略时默认结束当前活动会话）
- 成功响应：`{ "message": "会话已结束", "sessionId": "..." }`
- 错误码：
  - `403` 无权结束该会话（传递了不属于当前用户的 sessionId）
  - `404` 当前没有进行中的会话
  - `400` 会话已结束

### 4.2 客服端人工会话

> 客服身份 = 指定名单（`guide_agents.json`），超级管理员自动兜底。仅名单内客服/超管可调用收件箱、接受、回复、查看会话。

#### GET /guide/human/inbox
转人工收件箱（读后即删），返回 `{ messages: [...] }`。

#### POST /guide/human/accept
接受会话。请求体：`{ "sessionId": "<UUID>" }`。

#### POST /guide/human/reply
回复会话。请求体：`{ "sessionId": "<UUID>", "content": "回复内容" }`。

#### GET /guide/human/session/{sessionId}
获取指定会话完整历史。

#### POST /guide/human/close —— 客服结束对话

- 认证：客服管理员身份（仅**已接管该会话的客服**或**系统超管**可调用）
- 请求体：`{ "sessionId": "<UUID>" }`（32 位 UUID 字符串）
- 功能：追加系统消息 `{"role":"system","content":"客服已结束本次对话","timestamp":"<ISO时间>"}`，会话状态原子性更新为 `closed`，≤5 分钟清理周期内自动删除
- 成功响应：`{ "code": 200, "message": "会话已结束", "data": { "sessionId": "<UUID>" } }`
- 错误码：
  - `403` 无权结束该会话
  - `404` 会话不存在
  - `400` 会话已结束

#### GET /guide/human/agents —— 客服名单（仅超级管理员）

- `GET`：无入参，返回 `{ "agents": [{ userId, username, online, addedAt }], "total": N }`
- `POST`：`{ "userId": <int> }` 添加客服；`400` 非管理员/已在名单、`404` 用户不存在
- `DELETE /guide/human/agents/{userId}`：移除客服；`404` 不在名单

### 4.3 前端对接说明（ziyit_api.js 已封装）

| 前端函数 | 对应接口 |
| --- | --- |
| `guideSessionClose(sessionId?)` | `POST /guide/session/close` |
| `guideHumanClose(sessionId)` | `POST /guide/human/close` |
| `guideHumanAgents() / guideHumanAgentAdd(userId) / guideHumanAgentRemove(userId)` | 客服名单维护 |
| `guideChat / guideSession / guideStatus / guideAuthSync` | 用户端对话与同步 |
| `guideHumanInbox / guideHumanAccept / guideHumanReply / guideHumanSession` | 客服控制台 |
| `translate(text, language)` | `POST /translate` 翻译（自动携带 Bearer Token） |
| `adminBanIp / adminUnbanIp / adminListIpBans` | 封禁管理（支持 `durationMinutes` 定时/永久） |

### 4.4 翻译与封禁接口（非 /guide 系）

#### POST /translate —— 巨硬翻译器

- 认证：用户身份（`Authorization: Bearer <JWT>`）
- 请求体：`{ "text": "要翻译的文本", "language": "中文" }`（`language` 可选，默认中文）
- 响应：`{ "translated": "翻译结果", "used": 1, "quota": 10 }`
- 每日限额：普通 10 次、VIP 25 次；超限 429；文本上限 2000 字，超时 30 秒，失败不退回次数
- 前端已不再内置 API 密钥/混淆脚本，直接调用该接口等待完整结果

#### POST /admin/users/{id}/ban 与 POST /admin/ip/ban —— 封禁

- 认证：管理员身份（Bearer 头，按等级权限）
- 请求体：`{ "reason": "原因（可选）", "durationMinutes": <分钟数> }`
- `durationMinutes`：传分钟数 = 定时封禁（到期自动解封）；不传 / 传 0 = 永久封禁
- `is_banned` / `is_ip_banned` 自动判断到期解封（不影响登录校验与 IP 拦截中间件）
- 前端入口：后台「用户管理」封禁用户（弹窗输入时长，留空=永久）、IP 封禁管理（"封禁时长(分钟,留空=永久)"输入框）；列表显示到期时间

> 文档维护提示：本文档仅覆盖功能模块、页面结构与常见问题，不包含具体网页内容（如 Backrooms 词条正文）。功能调整后请同步更新本文档对应章节。
