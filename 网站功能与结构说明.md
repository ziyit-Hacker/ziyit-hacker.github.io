# ZIYIT STUDIO 网站功能与结构说明

> 本文档面向 ZIYIT STUDIO 官方工作室网站（`https://ziyit-hacker.github.io/`）的**所有访客与用户**，介绍网站的各个功能模块、页面布局、使用方法与常见问题。
> 文档完全从使用者的视角说明"怎么用"，便于任何人不借助专业技术也能看懂。

---

## 目录

- [一、网站简介](#一网站简介)
- [二、用户系统](#二用户系统)
- [三、VIP 会员体系](#三vip-会员体系)
- [四、人机验证系统（Phantom）](#四人机验证系统phantom)
- [五、音乐分享](#五音乐分享)
- [六、下载中心](#六下载中心)
- [七、Release Control（RC）](#七release-controlrc)
- [八、巨硬翻译器](#八巨硬翻译器)
- [九、Backrooms 中文维基](#九backrooms-中文维基)
- [十、Minecraft Mod Wiki](#十minecraft-mod-wiki)
- [十一、站内搜索与全网搜索](#十一站内搜索与全网搜索)
- [十二、病毒库](#十二病毒库)
- [十三、开发者日志](#十三开发者日志)
- [十四、后台管理（管理员系统）](#十四后台管理管理员系统)
- [十五、在线客服系统](#十五在线客服系统)
- [十六、全局导航与主题](#十六全局导航与主题)
- [十七、页面结构说明](#十七页面结构说明)
- [十八、常见问题大全](#十八常见问题大全)
- [十九、使用技巧与注意事项](#十九使用技巧与注意事项)
- [二十、功能更新记录](#二十功能更新记录)
- [二十一、功能更新记录](#二十一功能更新记录)
- [二十二、详细操作手册](#二十二详细操作手册)
- [二十三、Backrooms 文档创作指南](#二十三backrooms-文档创作指南)
- [二十四、场景化 FAQ（按使用场景索引）](#二十四场景化-faq按使用场景索引)
- [二十五、功能规则与参数详解](#二十五功能规则与参数详解)
- [二十六、Backrooms 层级写作进阶教程](#二十六backrooms-层级写作进阶教程)
- [二十七、网站提示语与解决办法](#二十七网站提示语与解决办法)
- [二十八、管理员日常运营指南](#二十八管理员日常运营指南)
- [二十九、附录：网站页面清单](#二十九附录网站页面清单)
- [三十、不同角色功能地图](#三十不同角色功能地图)
- [三十一、Backrooms 内容概览（读者指南）](#三十一backrooms-内容概览读者指南)
- [三十二、巨硬翻译器语言支持说明](#三十二巨硬翻译器语言支持说明)
- [三十三、补充常见问题（第二辑）](#三十三补充常见问题第二辑)
- [三十四、网站使用规范与用户约定](#三十四网站使用规范与用户约定)
- [三十五、账号安全与找回指南](#三十五账号安全与找回指南)
- [三十六、Backrooms 收录规范详解](#三十六backrooms-收录规范详解)
- [三十七、补充常见问题（第三辑）](#三十七补充常见问题第三辑)
- [三十八、功能常见问题速查（总表）](#三十八功能常见问题速查总表)
- [三十九、Backrooms 维基阅读指南（进阶）](#三十九backrooms-维基阅读指南进阶)
- [四十、音乐与下载中心使用详解](#四十音乐与下载中心使用详解)
- [四十一、在线客服深入指南](#四十一在线客服深入指南)
- [四十二、补充常见问题（第四辑）](#四十二补充常见问题第四辑)
- [四十三、系统状态与维护说明](#四十三系统状态与维护说明)
- [四十四、联系方式与支持渠道](#四十四联系方式与支持渠道)
- [四十五、全功能使用场景图文说明](#四十五全功能使用场景图文说明)
- [四十六、层级投稿后的常见问题（专题）](#四十六层级投稿后的常见问题专题)
- [四十七、巨硬翻译器深入指南](#四十七巨硬翻译器深入指南)
- [四十八、音乐与下载中心权益问答](#四十八音乐与下载中心权益问答)
- [四十九、Backrooms 创作全流程指南](#四十九backrooms-创作全流程指南)
- [五十、补充常见问题（第五辑）](#五十补充常见问题第五辑)
- [五十一、网站使用场景速查表](#五十一网站使用场景速查表)
- [五十二、层级文档格式详解（面向写作者）](#五十二层级文档格式详解面向写作者)
- [五十三、在线客服深度体验指南](#五十三在线客服深度体验指南)
- [五十四、网站安全与隐私说明](#五十四网站安全与隐私说明)
- [五十五、全功能索引表](#五十五全功能索引表)
- [五十六、补充常见问题（第六辑）](#五十六补充常见问题第六辑)
- [五十七、老用户常见认知误区澄清](#五十七老用户常见认知误区澄清)
- [五十八、RC BUG 反馈系统（含前端 / RC 端对接要求）](#五十八rc-bug-反馈系统含前端--rc-端对接要求)
- [五十九、Backrooms 四类文档系统（含前端对接要求）](#五十九backrooms-四类文档系统含前端对接要求)
- [六十、人机验证体验页与视频分包下发（含前端对接要求）](#六十人机验证体验页与视频分包下发含前端对接要求)
- [六十一、账号安全与多种验证方式（含前端对接要求）](#六十一账号安全与多种验证方式含前端对接要求)

---

## 一、网站简介

### 1.1 站点定位

ZIYIT STUDIO 是一个集**音乐分享、软件下载、维基百科、在线工具、社区互动**于一体的个人工作室网站。站点的核心内容分为几大类：

| 类别 | 代表功能 | 适合人群 |
| --- | --- | --- |
| 内容娱乐 | 音乐在线点播、歌词同步 | 喜欢听歌的用户 |
| 软件资源 | 软件 / MOD / DLC / RC 工具下载 | 需要工具与资源的用户 |
| 百科知识 | Backrooms 中文维基、Minecraft Mod Wiki | 后室爱好者、MC 玩家 |
| 在线工具 | 巨硬翻译器、人机验证体验、站内搜索 | 日常使用工具的用户 |
| 社区服务 | 用户中心、VIP、在线客服、封禁申诉 | 所有注册用户 |
| 管理功能 | 后台管理（仅管理员可见） | 站点管理员 |

### 1.2 站点理念

- **免费为主**：核心功能对普通用户免费开放。
- **VIP 增值**：VIP 会员解锁更高级的音乐与下载权限。
- **社区共建**：Backrooms 中文维基支持用户投稿、管理员审核后收录。
- **安全优先**：登录限流、封禁机制、人机验证、AI 安全防护层层保障。

### 1.3 快速开始

1. 打开网站首页，在右上角点击「登录」。
2. 没有账号？点击「没有账号？点这里注册」，填写用户名、邮箱、密码完成注册。
3. 登录后即可使用：音乐、下载、翻译器、Backrooms 投稿、在线客服等全部功能。
4. 想要更多权限？查看「用户中心 → 升级 VIP 会员」了解升级方式。

### 1.4 从首页进入查找功能

本站所有功能都挂在首页上，**不确定某个功能在哪时，先打开首页，从顶部导航逐级进入查找**：

- 顶部导航中的**功能区**：下载、更多、病毒库、搜索、音乐、巨硬翻译、人机验证等模块入口；
- 顶部导航中的**图书馆**：Minecraft Mod Wiki、Backrooms 中文维基及子分类；
- 顶部导航中的**开发者选项**：日志、后台管理等（按管理员身份显隐）；
- 页面右下角等功能区块：按各页面布局进入。

> 给 AI 客服与排查参考：需要页面地址时，**先查「二十九、附录：网站页面清单」**（其中列出了全部功能页面的完整 URL 与主要内容），不要凭空猜测功能所在地址或编造不存在的页面；也可以从首页（https://ziyit-hacker.github.io/）顺着导航逐级点进。用户问「某个功能怎么用 / 怎么打开 / 里面有什么内容」时，应**把对应页面打开读到实际内容**再回答；**只有用户明确索要链接时**才把 URL 给出来。另见 15.2.4。

---

## 二、用户系统

### 2.1 注册账号

- **入口**：用户中心注册页。
- **必填信息**：用户名、邮箱、密码。
- **说明**：
  - 用户名用于登录与展示，注册后可在用户中心修改。
  - 邮箱非常重要，用于**找回密码、注销账号、保留账号**，务必填写真实可用的邮箱。
  - 密码请妥善保管，网站不会以明文方式保存或展示你的密码。

### 2.2 登录

- **登录方式**：用户名 + 密码。
- **记住我**：勾选后登录状态会长期保持，关闭浏览器再打开仍处于登录状态。
- **登录态刷新**：网站会自动检测登录状态，菜单会根据是否登录动态显示或隐藏。
- **多设备**：同一账号可在不同设备登录，任一设备操作不会影响其他设备的浏览。

### 2.3 账号安全与异常处理

| 场景 | 表现 | 处理建议 |
| --- | --- | --- |
| 密码错误 | 提示"用户名或密码错误" | 确认大小写与输入法，或使用找回密码 |
| 登录过于频繁 | 提示"登录尝试过于频繁，请稍后再试" | 同一 IP 短时间内登录次数过多，稍等几分钟再试 |
| 登录已过期 | 页面自动跳转或提示重新登录 | 重新登录即可 |
| 账号被封禁 | 登录时显示封禁原因与剩余天数，并提供「封禁申诉」入口 | 按原因判断：等待解封或发起申诉 |

### 2.4 账号被封禁怎么办

网站对违规行为会采取封禁措施，封禁分为**定时封禁**和**永久封禁**两种：

- **定时封禁**：登录时会提示封禁原因、剩余天数与预计解封时间，到期后自动解封，无需任何操作。
- **永久封禁**：登录时会提示为永久封禁，只能通过申诉通道解决。

**如果认为自己是被误封的，可以发起封禁申诉：**

1. 在登录页面点击「封禁申诉」链接，进入申诉界面。
2. 输入自己的**用户名和密码**（用于验证身份）。
3. 提交后自动进入申诉对话页，可以描述自己的情况和申诉理由。
4. 客服管理员会看到你的申诉并查看你的封禁信息（用户编号、封禁理由、剩余时间），与你对话沟通。
5. 如果管理员认可你的申诉，会通过后台直接解除封禁，届时即可正常登录。

**申诉注意事项**：
- 申诉对话由人工客服处理，请文明、清楚地描述情况。
- 申诉期间账号仍处于封禁状态，解封由管理员判定。
- 申诉凭证有有效期，超过时间会过期，如过期需要重新发起。

### 2.5 修改资料与账号管理

| 操作 | 入口 | 说明 |
| --- | --- | --- |
| 修改密码 | 用户中心 → 修改密码 | 需验证原密码；提交后会向**当前邮箱**发确认链接，**点击确认后密码才真正改掉**（15 分钟内有效） |
| 修改邮箱 | 用户中心 → 修改邮箱 | 提交后会向**当前（旧）邮箱**发确认链接，**点击确认后邮箱才真正改掉**，随后再向新邮箱发一封验证邮件（15 分钟内有效） |
| 修改用户名 | 用户中心 → 修改用户名 | 修改后建议重新登录以确保状态一致 |
| 修改个人资料 | 用户中心 → 修改个人资料 | 目前可改用户名与邮箱；头像 / 个人简介等公开信息暂未开放 |
| 注销账号 | 用户中心 → 注销申请 | 需邮箱配合，支持取消注销 |

### 2.6 临时体验账号

网站提供 `cache` / `temp` 两个临时体验账号，**不需要注册即可体验**登录后的部分功能：

- 可用时长约为 3.75 分钟。
- 到期后会自动登出并清空登录状态。
- 体验完请注册正式账号，才能长期使用功能与保存数据。

### 2.7 黑名单机制

对于严重违规的账号，会进入黑名单。黑名单账号登录后：

- 仅保留"病毒库"页面的访问入口。
- 首页其余区块、轮播、页脚等全部隐藏。
- 页面会展示拉黑通知。

> 如果你认为被误拉黑，可以通过邮件联系站长说明情况。

---

## 三、VIP 会员体系

### 3.1 什么是 VIP

VIP 是网站的付费会员身份，用于解锁**更高级的音乐播放/下载权限**与**更多每日功能额度**。

### 3.2 VIP 标识说明（重要）

- **ZC / zC 是 VIP 身份的标识，不是管理员**。
- 管理员身份由后台的管理员等级系统独立决定，与 VIP 无关。
- 也就是说：看到 ZC 标识只代表对方开通了 VIP，不代表对方是站长或管理员。
- 管理员与 VIP 是两套并行的体系，可以同时存在，也可以单独存在。

### 3.3 VIP 权益一览

| 权益 | 普通用户 | VIP 用户 |
| --- | --- | --- |
| 免费音乐播放 | 部分曲目 | 全部曲目 |
| 音乐下载 | 免费曲目 | 全部曲目 |
| 下载中心 VIP 资源 | 不可见/不可下载 | 可下载 |
| 巨硬翻译器每日次数 | 10 次 | 25 次 |
| 在线客服 AI 每日次数 | 15 次 | 50 次 |
| 开发者选项菜单 | 不可见 | 可见 |

> 以上额度为当前配置，后续可能调整，以页面实际显示为准。

### 3.4 如何开通 VIP

VIP 通过**爱发电**自助购买，付款后由后端**自动发货**，无需等待人工审核：

1. 进入「用户中心 → 升级 VIP 会员」，点击「前往爱发电购买 VIP」。
2. 在爱发电页面选择方案 **「ZIYIT账号VIP独立方案（￥56.00/月）」**，并选择赞助期限（支持一次购买多个月）。
3. 下单时**必须填写**备注：`用户ID/用户名`（用户 ID 可在用户中心查看）。**这一步填错会导致发货到别的账号。**
4. 完成支付后，后端收到爱发电通知会立即为对应账号开通 / 续期 VIP，**有效期 = 赞助月数 × 30 天**；已是 VIP 的账号从原到期时间往后叠加，不会覆盖。
5. 回到「用户中心 → 升级 VIP 会员」页面，点击「我已购买，立即查询」，即可看到当前 VIP 到期时间。

> 特殊情况（填错用户 ID、需要人工介入等）仍可通过邮件联系站长 `ziyitstudio@qq.com` 手动开通。

### 3.5 VIP 价格

- VIP 基础价格为 **￥56.00/月**，与 **RC 专业版密钥**同价。
- 爱发电支持一次购买多个月（1 个月起），期限越长折扣越高，**实际金额以下单页显示为准**。
- VIP、RC 密钥与 DLC 扩展包是**三种独立商品**：买 VIP 只开通会员权益，买 RC 密钥只发放 RC 许可密钥，买 DLC 只发放对应扩展包，互不替代（详见「七、Release Control（RC）」）。

### 3.6 关于用户类型

登录后，页面上会显示你的用户类型，可能包括：普通用户、VIP 用户、管理员、超级管理员等。用户类型的显示由后台系统返回，**管理员等级与 VIP 身份是分开判定的**，互不影响。

---

## 四、人机验证系统（Phantom）

### 4.1 什么是 Phantom

Phantom 是网站自研的**无感人机验证**系统，用来保护翻译器等敏感功能不被脚本和机器人滥用。它的理念是"**藏在噪声里的方块**"——用户几乎感觉不到自己在做验证，但实际上系统已经在后台完成了"你是真人"的判定。

### 4.2 与传统验证的区别

| 对比项 | 传统验证码 | Phantom 无感验证 |
| --- | --- | --- |
| 用户体验 | 需要看字、点图、拖滑块 | 无感完成，几乎不打断操作 |
| 难度 | 字母数字易看错，滑块易烦躁 | 只要正常操作即可通过 |
| 安全性 | 可被识别工具破解 | 结合行为分析，更难以模拟 |
| 使用场景 | 登录、注册等 | 翻译器等敏感功能 |

### 4.3 哪些功能需要验证

目前**巨硬翻译器**等敏感功能在进入前会先加载验证容器，验证通过后才会展示功能界面。验证通常是即时完成的，用户通常不会感到等待。

### 4.4 在线体验

网站提供「人机验证在线体验」页面，可以：

- 了解 Phantom 的核心机制说明。
- 在页面内的体验区实际体验验证过程。
- 还有一份「人机验证使用方式」说明页，介绍使用细节。

**体验页的额度规则（v0.3.19）**：体验页需**登录后**使用。页面会让你**自行输入一个 API 密钥**（只校验格式，随便输一个格式合法的即可试用），每次验证会扣除**当前登录账号名下** API 密钥的可用额度；当额度为 0（名下没有可用密钥 / 密钥已停用 / 本月额度用尽 / 今日额度用尽）时，页面会**禁用「开始验证」并说明原因**。由于额度只按登录身份计，输入别人的密钥并不会花到别人的额度。详见 60。

### 4.5 安全模型与防护层级

Phantom 的"正确答案"（光点应当走过的那条轨迹）**只存在于服务器**，任何时候都不会出现在响应字段里——页面里播放的引导视频只承载视觉引导。

需要先说清一个事实：引导视频把轨迹**画成了像素**（纯黑底 + 沿路径移动的光点簇），所以机器可以逐帧取质心、把视频里的轨迹"抠"出来。只要人眼看得见，图像识别就提取得到，这不是加密能解决的问题。因此 v0.3.2 起不再追求"提取不到"，而是让**「提取到」不等于「答对」**：同一段视频里**同时渲染多条候选路径**的光点（默认 8 条），用户跟随**任意一条**都算通过（评分对这些候选取最高分）；攻击者即使把全部候选都抠出来，也只能在「单题最多 3 次提交 + 连续失败封禁」的限制下盲猜哪一条会被用于评分。

v0.3.3 起再补上一道**实时流式上报**：前端从引导视频开始播放那一刻起，就按固定间隔把**新增的采样点**加密上报给后端，后端边收边记录**每一批到达服务器的真实时刻**（服务器时钟，客户端伪造不了），松手时再照旧提交完整轨迹。于是"抓包拿到视频 → 慢慢逐帧抠轨迹 → 空等几秒后整体回放"这条路走不通了：一段 6 秒的轨迹必然对应约 6 秒的真实到达跨度，**离线分析再也压不进这个窗口**，攻击者必须做**实时**图像识别。后端还会顺带校验"实时流的内容确实是最终提交轨迹的一段"（按顺序、容差内匹配），拦住"实时灌一段无关轨迹、事后再提交离线拟合结果"。

**诚实的边界**：批次里的坐标依然由客户端产生，理论上可以伪造；这项机制真正新增的可信信号只有两个——每批次的**到达时刻**，以及**流内容必须与最终提交同源**。它抬高的是自动化成本（逼对手实时化），而不是"根治"。噪声同理：真人与识别工具面对的是同一批像素，单靠"把噪声做得更随机"并不能阻止提取。

v0.3.4 补上的是**入口与来源层**。此前有一版攻击干脆不开浏览器：直接用脚本完成密钥协商、把引导视频下下来、用 OpenCV 逐帧取亮像素质心、再加密回传，全程只带 `api-key` 和 `Content-Type` 两个请求头。针对这条路，现在：

- **请求层浏览器门禁**：`/challenge`、`/verify/chunk`、`/verify` 要求请求自报为真实浏览器——UA 必须是浏览器且不含脚本库特征，同时必须具备浏览器必带的 `Origin`、`Accept-Language`，并校验 `Sec-Fetch-*` 与 Client Hints（`sec-ch-ua` 版本必须与 UA 一致）。裸脚本在这些检查上会直接 403，并在审计日志留下 `human_browser_gate` 记录便于排查误伤。
- **真实来源 IP 还原**：站点经内网穿透对外服务，此前所有访客在服务端看来都是同一个回环地址，导致失败频控、封禁、注册冷却"全站共用一个桶"——真凶失败几次能把全站锁住，他一通过又顺手清空所有人的计数。现在从代理头解析真实来源 IP，上述限流与封禁按真凶逐个记账。
- **Δt 抖动双向把关**：原本只否决"过于均匀"的时间戳（脚本按帧率公式步进）。对手随即改为人为放大抖动来绕开，而真人的指针采样抖动幅度是有限度的，因此现在对**过于杂乱**的 Δt 同样一票否决。
- **注册入口限流**：一次性/临时邮箱域名直接拒收（这类邮箱能自助完成"注册→收信→点链接"闭环），并限制单个 IP 每自然日的成功注册数。
- **单帧零信号（原值覆盖合成）**：原来前端是把簇层视频与噪声"取较亮者"叠加，簇区亮度整体高于背景，任何截图用亮度阈值一卡就能把方块框出来。现在改成把簇像素的**原值**覆盖到噪声上——簇区像素与四周噪声同为 0~255 均匀分布，**单帧截图看不出方块在哪**；人眼靠"这一小块图案在刚性移动、周围噪声逐帧重掷"的时域线索捕捉（为此把簇的中心与粒子偏移都取整，让整簇只做整数像素平移，并把逐帧丢弃率归零，图案不会被逐帧揉碎）。附带好处：图案刚性后编码器帧间能做完美匹配，实测 K=8 的视频从约 1.16MB 降到约 192KB（传输量降约 6 倍）。
- **静止诱饵块**：单帧零信号之后，机器自然会改看**时域**（找出"逐帧取值稳定的那一小块"）。因此额外撒 3 块**与真方块单帧完全同构**的诱饵：同样大小的方框、同样数量的孤立随机像素、灰度同为 1~255 均匀、位置同样随机；但诱饵**逐帧一动不动、连像素值都不变**（若每帧重掷，"逐帧稳定"这条判据就会把诱饵筛掉，等于白加）。单帧里它们和真方块一模一样，分辨只剩"谁在动"——而这正是真人一眼就能做到的。（v0.3.4 在前端噪声层撒；v0.3.5 起改由后端渲染进引导视频。）

v0.3.5 把"起手提示"与"诱饵"一并下沉到后端的引导视频里——前端不再自己画方块、也不再撒诱饵：

- **起手提示段进视频**：视频开头多出 2 秒提示段，在**起点**（K 条候选共享的首控制点）渲染一块**整块方波翻转**的方块（亮 255 / 暗 24，1.5Hz）。前端按住就从 0 秒整段播，按后端加密下发的 `previewSeconds` 把"提示段"切成"跟随段"——起点不必再由前端从视频首帧反推，"按住后看不到起手方块"这类问题随之消失。暗态取近黑 24 而非纯黑 0：前端合成只覆盖亮度 > 8 的像素，纯黑会被当成黑底而透出实时噪声，暗态就彻底看不见了。这段基本是纯黑 + 一块实心方块，几乎不增加视频体积。
- **静止诱饵块进视频**：诱饵改由后端渲染（默认 3 块，提示段与跟随段全程都有），前端撒诱饵的那套代码随之删除。诱饵与真簇单帧同构、逐帧一动不动（位置与像素值都是常量）。代价是体积：诱饵是散落在全画面上的孤立随机像素，编码器压不动——同一组 8 条候选路径下实测，6 秒无诱饵约 374KB → 8 秒含 3 块诱饵约 617KB（增量几乎全在诱饵，提示段本身几乎免费）；嫌大就下调 `PHANTOM_VIDEO_DECOY_COUNT`。
- **时长语义**：响应里的 `videoDurationMs` 现在表示**视频总时长**（提示段 + 跟随段），加密参数里新增 `previewSeconds`；`/verify` 的合成轨迹签名比对同时覆盖这两个时长——对手把伪造 Δt 等比缩放到哪一个都算命中。

v0.3.19 再把**视频下发本身**改成"就绪握手 + 分包、严格串行拉取"：`POST /challenge` 只回视频元数据（`videoStream`）与总包数，不再随回包把整段视频灌下来；前端准备好播放器后先 `POST /video/ready` 握手，再按 `index = 0,1,2…` 调 `POST /video/chunk` **一包一包取**（每包 5 帧）。服务端维护一个**严格串行游标**：只接受游标指向的下一包，跳号 / 抢跑 / 并发预取一律 409，同一包的重复请求则按"已下发过"幂等重发、不推进游标。这样一来，"一次抓包就把整段引导视频攥在手里、离线慢慢抠轨迹"这条路的成本被抬高一档——视频只能**边播边要**，而且每包都以 IDR 关键帧开头、GOP 与包大小对齐，正常前端用 MediaSource 顺序 append 即可无缝播放。

**诚实的边界**：请求头是客户端可控的，对手把浏览器整套请求头抄下来即可绕过门禁，真正挡住的是"裸请求直连"这一档；它必须与来源 IP 限流、轨迹层否决叠加使用，单独一层都不构成墙。

| 防护层 | 面向的对手 | 有效性 |
| --- | --- | --- |
| 视频分包 + 严格串行拉取（v0.3.19） | 抓包一次性拿走整段引导视频、再离线逐帧抠轨迹的攻击者 | 有效（抬高成本）：视频只能边播边要、一包一包取（每包 5 帧），跳号 / 抢跑 / 并发预取一律 409，整段无法被并行或成批拿走 |
| 体验页额度按登录身份归属（v0.3.19） | 伪造请求头、自称是某个 API 密钥所有者的调用方 | 有效：体验页自输的密钥只校验格式，身份与额度一律按服务端签名校验过的登录身份判定 —— 填别人的密钥花不到别人的额度 |
| 起手提示段进视频（v0.3.5） | 需要从视频首帧反推起点、或按住后看不到起手方块的真人与脚本 | 有效：起点由后端渲染在视频里（整块黑↔白翻转，位置即 K 条候选共享的首控制点），前端只垫噪声 |
| 单帧零信号合成（v0.3.4） | 下载引导视频后逐帧阈值取质心、或对页面截图定位方块的攻击者 | 有效：单帧内簇区与噪声同分布，阈值 / 局部亮度检测器无从下手；多帧时序分析仍能把簇抠出来（这属于"提取到≠答对"那道既有防线） |
| 静止诱饵块（v0.3.4，v0.3.5 移入视频） | 把"逐帧取值稳定的小块"直接当成方块位置的机器 | 有效（抬高成本）：诱饵与真方块单帧完全同构、时域上同样"稳定"，筛掉它必须做运动一致性分析；真人靠运动即可跟住真块 |
| 请求层浏览器门禁（v0.3.4） | 不开浏览器、直接用脚本发请求并解析验证视频的攻击者 | 有效（抬高成本）：裸脚本在入口即 403；请求头可被伪装，故需与来源 IP、轨迹层叠加 |
| 真实来源 IP 还原（v0.3.4） | 借内网穿透隐藏来源、绕过 IP 维度限流的攻击者 | 有效：失败频控 / 封禁 / 注册冷却按真实来源逐个记账 |
| Δt 抖动双向把关（v0.3.4） | 为躲"等差时间戳"否决而人为放大抖动的脚本 | 有效：Δt 变异系数既不能过低（等差）也不能过高（超出真人指针采样带宽） |
| 注册入口限流（v0.3.4） | 用一次性邮箱批量注册、单 IP 反复注册的攻击者 | 有效：一次性邮箱域名拒收 + 单 IP 每日注册上限 |
| 实时流式上报（v0.3.3） | 抓包拿到视频、离线逐帧抠轨迹后再回放的攻击者 | 有效（抬高成本）：轨迹必须边画边报，6 秒轨迹对应约 6 秒真实到达跨度，离线分析压不进窗口；并校验流与提交流同源 |
| 多候选轨迹（v0.3.2） | 抓包拿到视频、用图像识别逐帧抠光点轨迹的攻击者 | 有效：抠出全部候选也不等于知道哪条被评分，需在限流下盲猜 |
| 控制点与种子不下发 | 分析响应字段的攻击者 | 有效：响应中没有任何可推导出答案的字段 |
| 密钥化派生（v0.3.1） | 已经拿到完整服务端源码的攻击者 | 有效：缺少只存在于服务端的私钥，无法离线复现答案 |
| 前端实时噪声层 | 只看到最终画面（截图 / 录屏）的攻击者 | 有限：噪声只在浏览器端实时生成，视频文件本身不含噪声，直接解析视频不受影响 |

**为什么"源码可见"也拿不到答案**

早期的思路是"控制点不下发，所以安全"。但这留下一个隐患：如果轨迹是由「公开的伪随机算法 + 一个输入」确定性生成的，那么拿到源码的人只要在本地复刻同一个函数、喂入相同输入，就能原地算出标准答案——**既不需要破解视频，也不需要拿到那个输入串**。

v0.3.1 起改为**密钥化派生**：派生时把「服务端私有密钥」和「一次性随机数」一并并入计算，即

```
path_seed = HMAC(服务端私钥, 一次性随机数 ‖ challengeId)
```

因此：

- 攻击者即使逐字复刻派生算法，没有私钥也得不到同一条轨迹（算法可复现 ≠ 答案可复现）。
- 每次挑战都会新取一次性随机数，同一题重复派生也不会得到相同控制点（防跨题复用与重放）。

该私钥只保存在服务端进程内（由环境变量注入，未注入时进程启动时随机生成），**不写入日志、不写入任何响应字段**。

**时效校验为什么只看服务器时钟**

提交时需要判断"这段轨迹是不是发题后当场画出来的"，防止有人预先录好轨迹、发题后瞬间回放。旧实现拿客户端上报的绝对时间戳与服务器时间相减，存在两个问题：

1. **可被精确绕过**：脚本只要把自己机器的系统时间调到与服务器一致，差值就落回合法区间。换言之它并不构成对自动化的防线。
2. **误杀真人**：系统时间不准（时区设置错误、校时失败）的正常用户会被直接拒绝。

现在时效判定**一律以服务器时钟为准**：服务器记录发题时刻，提交时计算"发题→提交实际经过了多少秒"，再要求客户端自报的轨迹时长不超过该经过时间加一个小容差；题目总时长由过期时间（TTL）兜底。服务器时钟客户端无法伪造，既拦得住"发题即回放预生成轨迹"，也不会误伤时钟不准的真人。

**调试日志**

调试模式（`PHANTOM_DEBUG`）会把每次提交的轨迹与完整评分明细（各项得分、是否触发否决、动态通过线等）打印到日志，相当于把"怎样调参才能通过"直接交给攻击者。**生产环境不开启**该开关。

### 4.6 常见问题

**Q：为什么我什么都没做就通过验证了？**
A：这正是无感验证的设计目标——系统通过分析鼠标移动、页面行为等特征自动判定，正常操作的用户无需额外操作。

**Q：验证失败或提示环境异常怎么办？**
A：请检查：
1. 关闭浏览器的开发者工具（F12 / 审查元素）。网站包含反调试检测，开着开发者工具会被判定为可疑环境。
2. 确认浏览器没有开启可疑代理或网络加速软件。
3. 刷新页面后重试。

**Q：提示环境异常是不是我被封了？**
A：不是封禁。这只是验证系统检测到可疑环境（如开着开发者工具）后的保护性拦截，恢复正常环境后即可使用。

---

## 五、音乐分享

### 5.1 功能概述

音乐分享是网站的在线点播与下载功能，内置完整的网页播放器，支持：

- 在线播放、暂停、上一曲/下一曲切换
- 进度条拖拽与时间显示
- 歌词同步显示（点击歌词可跳转到对应播放位置）
- 多种播放源：普通音频（MP3）、视频、B 站视频

### 5.2 音乐权限

| 曲目标签 | 播放 | 下载 |
| --- | --- | --- |
| 免费曲目 | 所有用户 | 所有用户 |
| VIP 曲目 | 仅 VIP | 仅 VIP |

- 登录后即可收听免费曲目。
- 未登录或普通用户遇到 VIP 曲目会提示无权限。
- VIP 用户可解锁全部曲目与下载。

### 5.3 播放器使用说明

1. 进入音乐页面，页面会自动加载音乐清单并显示播放列表。
2. 点击任意曲目开始播放。
3. 播放器区域支持：
   - 播放 / 暂停按钮
   - 上一曲 / 下一曲按钮
   - 进度条点击或拖拽定位
   - 当前时间 / 总时长显示
   - 歌词面板：随播放自动滚动并高亮当前行，点击歌词行可跳转
4. 遇到视频类曲目，播放器会切换为视频播放模式。

### 5.4 音乐库的来源

音乐库由管理员在后台统一维护，支持增删曲目、配置播放源。普通用户无需操作，播放列表会自动更新。

### 5.5 常见问题

**Q：为什么部分音乐无法播放？**
A：该曲目可能是 VIP 专属资源，需要开通 VIP 后解锁；也可能是播放源临时失效，请稍后重试或联系管理员反馈。

**Q：下载按钮转圈或提示无权限？**
A：请确认：
1. 已登录账号。
2. 该曲目对当前身份开放下载（普通用户只能下载免费曲目）。
3. 网络正常。

**Q：歌词不同步或没有歌词？**
A：歌词由管理员维护，部分曲目可能没有歌词文件。网络不佳时歌词可能延迟加载，可以点击歌词行手动跳转。

**Q：音乐可以后台播放吗？**
A：只要保持音乐页面打开，切换其他标签页播放不会中断。

---

## 六、下载中心

### 6.1 功能概述

下载中心提供**软件、MOD、DLC** 等资源的发布与下载，是网站的软件资源分发区。

### 6.2 资源分类与权限

| 资源类型 | 说明 | 谁可以下载 |
| --- | --- | --- |
| 普通资源 | 免费公开的资源 | 所有用户 |
| 免费资源 | 需要登录后下载的免费资源 | 登录用户 |
| VIP 资源 | 高级资源 | 仅 VIP 用户 |
| DLC 扩展包 | RC 软件的付费扩展包（爱发电自助购买、永久有效） | 已拥有该扩展包的用户 |

### 6.3 使用方法

1. 进入下载中心，页面以卡片形式展示所有产品资源。
2. 顶部搜索框可以输入关键词过滤资源。
3. 点击资源卡片查看详情。
4. 点击下载按钮开始下载（需要相应权限）。

### 6.4 常见问题

**Q：下载按钮没反应？**
A：请确认已登录，且账号具备该资源的下载权限（普通/免费/VIP 三档）。

**Q：怎么申请上架自己的资源？**
A：资源上架由管理员审核维护，你可以联系管理员提交。

**Q：下载速度慢怎么办？**
A：下载速度取决于网络环境与资源所在服务器，与账号权限无关。

### 6.5 资源包（.rcm）格式校验

资源库里每条资源都要填一个 `.rcm` 下载地址（`lastFileUrl`）。**光看后缀不算数** —— 后端会把该地址
指向的内容**真的取回来**，按《Release Control MOD DLC 开发者文档》验一遍，只有确认是**合法的
`.rcm` 包**才允许写进资源库（用户提交 `POST /mods/submit`、管理员 `POST/PUT /admin/mods` 都走这一关）。

| 校验项 | 规则 |
| --- | --- |
| 包体 | 必须是 ZIP（官方 `.rcm` = 整个目录打包成 ZIP 后改扩展名），且根目录必须有 `ModConfig.json` |
| 必备字段 | `Name` / `License` / `Version` / `Path` / `Menu` 缺一不可 |
| `Name` | 1-64 字符，仅字母、数字、下划线，且必须以字母或下划线开头 |
| `Version` | 符合 `MAJOR.MINOR.PATCH`（允许 `1.2.3-beta.1` 这类预发布后缀） |
| `Path.MainCode` | 必填，且该文件必须真的在包里（DLC 包里是加密后的 `原名.rce`，两种写法都认） |
| `Path.Code` / `Path.Lang` | 列出的文件同样必须在包内存在 |
| `Menu` | 非空数组，每一项都要有 `label` 与 `command` |
| `MinVersion` / `MaxVersion` | 同时填写时必须满足 `MinVersion <= MaxVersion` |
| DLC 加密布局 | `License` 为 `ZIYIT STUDIO` 时，包内非配置文件都是 `原名.rce`（内容 = 16 字节 IV + 16 字节整数倍密文），布局不对直接拒 |

其它规则：

- **远程地址必须是公网 http(s)**：内网 / 回环 / 链路本地 / 保留段地址（`127.0.0.1`、`localhost`、
  `169.254.169.254` 等）一律拒收，避免被当成「向站内网发请求」的跳板；跳转最多跟 5 跳，
  **每一跳都重新做公网校验**。
- **本地资源**（`/downloads/xxx.rcm`）直接读服务器上的文件校验，不走网络。
- 大小上限 32MB、抓取超时 15 秒，可用环境变量 `RCM_MAX_BYTES`（字节）/ `RCM_FETCH_TIMEOUT`（秒）调整。
- 校验不通过返回 `400 Invalid .rcm file: <原因>`。

> **为什么要这么做**：过去只判断「字符串是不是以 `.rcm` 结尾」，任何人都能往**公开**资源库里塞
> 任意链接（内网地址、钓鱼页、假包），而 RC 端拿到这个地址就会去下载安装 —— 等于把资源库变成了
> 「任意链接」的投递通道（2026-09-25 安全审计第 3 项）。

---

## 七、Release Control（RC）

> **显示名说明：下载中心中，RC 工具在下载页面 / 资源卡片上显示的名称为「任务管理器」，「任务管理器」即 RC 工具，二者是同一软件。**

### 7.1 什么是 RC

RC（Release Control）是 ZIYIT 出品的**系统工具**，用于软件的发布与授权管理。下载中心的 RC 板块提供 RC 软件的下载与密钥授权说明。

### 7.2 相关文件与资源

| 内容 | 说明 |
| --- | --- |
| RC 下载页 | 提供 RC 工具本体下载 |
| 密钥说明文档 | 说明 RC 软件的密钥使用方式 |
| 校验文件 | 提供哈希校验，用于验证下载文件完整性 |
| 更新信息 | 提供版本更新信息 |

### 7.3 密钥管理

RC 软件需要**密钥授权**才能使用。密钥按权限分为三种：**RC 专业版（Pr）**、**RC 普通版（Or）**、**RC 试用版（Tr）**。

**获取方式**

| 方式 | 说明 |
| --- | --- |
| 爱发电自助购买 | 见下表；付款后后端自动生成密钥并发放到对应账号 |
| 管理员发放 | Lv.3 及以上管理员在后台「RC 密钥」中发放 / 续期 |
| 历史密钥补录 | 早期未留存明文的密钥，可由管理员在后台用「回填历史明文密钥」补录 |

**爱发电方案**

| 方案 | 价格 | 对应权限 |
| --- | --- | --- |
| RC普通版密钥独立方案 | ￥27.00/月 | 普通版（Or） |
| RC专业版密钥 | ￥56.00/月 | 专业版（Pr） |

> 扩展包（DLC）是另一类爱发电商品，备注格式为 `用户ID/扩展包ID`，详见 7.4。

购买流程：

1. 登录后进入「用户中心 → 升级 VIP 会员」页，或直接访问爱发电主页 <https://afdian.com/a/ziyit_studio>。
2. 选择对应的 RC 密钥方案与赞助期限（支持一次购买多个月，期限越长折扣越高）。
3. 下单时**必须填写**备注：`用户ID/用户名`；**填错会发货到错误的账号**。
4. 支付完成后后端自动生成密钥，**有效期 = 赞助月数 × 30 天**；同一权限下已有密钥的账号**自动续期叠加**，不会重复发放新密钥。

**密钥是怎么保存的**

- 密钥格式为 **5 组 × 5 位大写十六进制**（格式示例：`A1B2C-3D4E5-F6071-8293A-B4C5D`，非真实密钥）。
- 服务器**只保存一份**内容：用 **AES（Fernet）加密后的明文密钥**（仅用于向你本人或管理员回显）。RC 客户端校验用的 **MD5 + Base64 哈希**不再单独存储，改为每次校验时由服务器从明文即时算出（所以单独拿到数据文件也拿不到可直接冒用的凭据，还须有服务器上的主密钥文件）。
- 明文密钥只会在「我的 RC 密钥」页面下发给你本人，其余场合不外传。

**在哪里查看自己的密钥**

- 登录后打开「用户中心 → 我的 RC 密钥」（`https://ziyit-hacker.github.io/user/rc-key.html`），或点击顶部导航「我的RC密钥」。
- 页面会列出你名下所有密钥：权限（普通版 / 专业版 / 试用版）、**明文密钥**（可一键复制）、激活时间、到期时间、来源（爱发电自动发放 / 历史密钥补录 / 管理员发放）。
- 续费后到期时间自动延长；请留意页面显示的到期时间，到期后需续费才能继续使用。

**管理端**

- Lv.3 及以上管理员在后台「RC 密钥」区块可查看**所有用户**的密钥明文、权限与到期时间（不显示哈希），并可手动发放 / 续期。
- 管理员发放 / 替换密钥时**直接填写明文密钥**（后台不填、也不展示哈希），服务端**只保存 AES 加密后的明文**，校验哈希按需即时派生 —— 填什么就是什么，可用于补发或换发。
- 同一权限下已有密钥时，按填写的明文**覆盖**该密钥并按有效期天数**重设**到期时间（不叠加）。
- 早期发放、未留存明文的密钥会在页面上标注「明文未在服务端留存」，可用「回填历史明文密钥」按历史清单补齐：先「预演」列出将要补录的账号，确认后再写入。
- 回填只补明文，**不新建密钥、不改权限、不改有效期**。

> 注意：这里说的「许可证密钥」与 RC 文件加密用的「恢复密钥」是两回事 —— 恢复密钥用于解密 `.rce` 文件，见 7.5。

### 7.4 扩展包（DLC）自动发货

**DLC（扩展包）** 是 RC 软件的付费增值包，与 RC 密钥一样通过**爱发电**自助购买，付款后由后端**自动发货**，无需等待人工审核。扩展包**永久有效**。

**商品与下单备注**

| 项目 | 说明 |
| --- | --- |
| 购买渠道 | 爱发电主页 <https://afdian.com/a/ziyit_studio> 的扩展包商品 |
| 商品 ID | `3f7d2856b3be11f1a6825254001e7c00`（站内已上架的扩展包商品 ID；可用环境变量 `AFDIAN_DLC_PRODUCT_IDS` 追加/覆盖，逗号分隔） |
| 订单备注 | **必须**填 `用户ID/扩展包ID`，填错会发货到错误的账号或无法发货 |
| 扩展包ID 写法 | 填**编号**（`modId`，如 `3`）或**包名**（如 `ProcessOptimizer`，不分大小写）都认；以「下载中心 → MOD / DLC 管理」或 RC 软件 DLC 列表中显示的为准 |
| 备注示例 | `264/3`、`264/ProcessOptimizer` |
| 不可单独购买 | 基础包 `CoreToolkit`（编号 1）是**随单附赠**的，不能当扩展包买；填了会被判为无效备注 |

**发货内容**

一次 DLC 订单会同时发放**两个包**，且都写**永久有效**（到期时间为空）：

| 发放内容 | 说明 |
| --- | --- |
| `modId: 1`（`CoreToolkit`） | 基础包：购买**任意**扩展包都附赠 |
| 买家所购扩展包 | 备注里填的那一个 |

**发货判定优先级**

后端在收到爱发电通知（Webhook）、管理员扫单补发、用户自助查单时，按订单上的**商品 / 方案 ID** 依次判定，命中即按该类发放：

1. **DLC 扩展包**（商品 ID 命中）→ 发「基础包 + 所购扩展包」，**不发** RC 密钥、**不发** VIP。
2. **RC 密钥方案** → 发放对应权限的许可证密钥。
3. **VIP 方案** → 开通 / 续期 VIP。

DLC 判定**先于**方案白名单：扩展包若以爱发电「商品（SKU）」而非「方案」形式下单，订单可能没有 `plan_id`，只认 `plan_id` 会静默漏发，因此后端会同时收集 `plan_id`、`sku_detail` 内的 `sku_id` / `product_id` / `goods_id` 等字段一并比对。

**到账后在哪里看**

- **网页端自助查询**：登录后打开「用户中心 → 我的扩展包」（`user/dlc.html`），或点顶部导航「我的扩展包」，可查看自己已拥有的扩展包（包名、编号、版本、说明、**永久有效**、获取时间、来源）并直接下载。数据来自 `GET /dlc/mine`（只认登录身份，不接受其他用户 ID 参数）。
- 打开 **RC 软件顶部菜单「DLC → 管理 DLC」**（DLC 管理器）：已授权的扩展包会显示为可用，发货后无需额外操作，重开管理器 / 刷新列表即可看到。
- 在**下载中心**下载 DLC 文件同样要求已拥有，未拥有时下载接口返回 `403 DLC not owned`。
- 可在用户中心点「查询我的订单」自助查单（见下）。

**幂等与异常处理**

- **不重复发货**：以爱发电订单号为准入账，同一条订单**重复推送 / 重复扫单只发一次**；已拥有同一个扩展包时也不会重复写入。
- **异常订单只记账不发货**：备注格式不对、扩展包编号 / 包名不存在、填的是基础包等情况，订单会记入台账、原因标记为 `dlc_not_found`，由管理员在后台「爱发电订单」中看到并人工处理（宁可让人看见，也不会写一条"已发货"却什么都没发）。
- 每笔发货都会写入审计日志（`afdian_grant_dlc`），含订单号、发放的扩展包编号与来源。

**自助查单**

登录后在用户中心触发「查询我的订单」：服务端会主动向爱发电拉取最近订单，把**已付款但未入账**的订单立即补发（DLC / RC 密钥 / VIP 都会一并检查）。查单有冷却时间（默认 60 秒，可用 `AFDIAN_SELF_CHECK_COOLDOWN` 调整）。

**当前站内扩展包**

| 编号 | 包名 | 版本 | 说明 |
| --- | --- | --- | --- |
| 1 | CoreToolkit | 1.2.1 | 基础包，随单附赠，不可单独购买 |
| 2 | Test | 1.0.1 | 扩展包 |
| 3 | ProcessOptimizer | 1.0.1 | 扩展包 |
| 4 | SystemGuardian | 1.0.0 | 扩展包 |

> 上表以站内实际列表为准，后台「MOD / DLC 管理」可增删改。

### 7.5 RC 文件加密服务

RC 提供两种**文件加密**方式，加密后的文件以 `.rce` 格式保存，同时生成一份 `.key` 密钥文件（两行：第一行恢复密钥 ID，第二行恢复密钥）。两种方式产生的文件格式完全一致，区别只在"密钥从哪来"。

| 方式 | 密钥来源 | 单文件上限 | 配额规则 |
| --- | --- | --- | --- |
| 联网密钥 | 由网站服务器实时下发（一个文件一把密钥） | 无额外限制 | **托管配额**：当前托管的恢复密钥数量，删除某个文件的密钥即恢复一个额度 |
| 云加密 | 由网站服务器在服务端完成加密 | **100MB** | **月度配额**：按自然月（北京时间）计算，每月重置；因服务端同样托管一把恢复密钥，**同时占 1 个托管额度**（与联网密钥共用） |

**配额上限（两种方式相同，按身份分级）**

| 身份 | 上限 |
| --- | --- |
| 普通用户 | 15 |
| VIP 用户 | 50 |
| 1–3 级管理员 | 25 |
| 4 级管理员（站长） | 不限 |

**托管额度为两种方式共用**：用一次云加密 = 扣 1 次本月云加密次数 + 占 1 个托管额度；删除该文件的密钥记录只退还托管额度，本月已消耗的云加密次数不返还。

**加密方式说明**

- 加密算法：**AES-256-CBC**（PKCS7 填充），按 **1MB** 分块处理，与 RC 客户端原有加密实现保持一致。
- 联网密钥：RC 向服务器申请到恢复密钥后，**在本地完成加密**，密钥随文件一一配对。
- 云加密：文件上传到服务器，**由服务器完成加密后直接回传 `.rce` 文件**，服务器**不留存文件内容**；恢复密钥与配置在服务器加密托管，仅账号本人可查看。
- 两种方式都会在服务器登记该文件的恢复密钥，以便日后在网页端查看与管理。

**「我的文件密钥」网页端管理**

RC 软件内提供「我的文件密钥」入口，会打开网页端页面（`https://ziyit-hacker.github.io/user/file-keys.html`）。在该页面可以：

- 查看自己名下所有已加密文件的记录（文件名、加密方式、时间、大小）。
- 查看配额用量：云加密（本月已用 / 上限）与托管密钥（联网密钥 + 云加密，当前托管 / 上限）。
- 按需重新查看某个文件的恢复密钥（需再次输入登录密码验证），并可下载 `.key` 恢复密钥文件。
- 删除不再需要的文件密钥记录 —— 删除后立即返还 1 个**托管额度**（联网密钥与云加密共用那一栏）；云加密已消耗的月度次数不返还。

> 提示：恢复密钥是解密文件的唯一凭据，请与 `.rce` 文件一并妥善保存。删除服务器上的密钥记录后，若本地 `.key` 文件也丢失，文件将无法恢复。

### 7.6 常见问题

**Q：下载中心里显示的「任务管理器」是什么？**
A：「任务管理器」就是 RC 工具（Release Control）。RC 在下载中心上架时显示名为「任务管理器」，因此下载页面 / 资源卡片上看到的「任务管理器」即 RC 软件本体，获取密钥后按说明激活即可使用。

**Q：如何获取 RC 密钥？**
A：在爱发电（<https://afdian.com/a/ziyit_studio>）购买「RC普通版密钥独立方案（￥27.00/月）」或「RC专业版密钥（￥56.00/月）」，下单时按 `用户ID/用户名` 填写备注；付款后后端自动生成并发放密钥。也可以联系管理员手动发放。

**Q：购买后在哪里查看自己的 RC 密钥？**
A：登录后进入「用户中心 → 我的 RC 密钥」（`user/rc-key.html`），或点击顶部导航「我的RC密钥」，页面会显示明文密钥、权限与到期时间。

**Q：续费会换一把新密钥吗？**
A：不会。同一权限下已有密钥的账号续费时会在**原密钥上叠加有效期**，密钥明文保持不变，无需在 RC 客户端里重新更换。

**Q：我的密钥显示「明文未在服务端留存」？**
A：这是早期发放的密钥（当时只保存了哈希，没有保存明文）。可以让管理员在后台用「回填历史明文密钥」补齐，补齐后即可在页面看到明文。

**Q：RC 密钥和 VIP 是一回事吗？**
A：不是。VIP 是网站会员身份（音乐 / 下载 / 额度），RC 密钥是 RC 软件的授权许可，两者独立、需分别购买。

**Q：下载的 RC 文件提示校验失败？**
A：可能是下载不完整或被篡改，请重新下载并使用校验文件核对哈希值。

**Q：VIP 价格与 RC 专业版的关系？**
A：两者基础价都是 ￥56.00/月，但**是两种独立商品**：VIP 开通会员权益，RC 专业版密钥发放 RC 专业版授权，互不替代。

**Q：联网密钥和云加密该怎么选？**
A：文件不太大、想省流量就在本地用联网密钥加密；文件较大或本地加密不方便时用云加密（单文件上限 100MB，服务器加密后回传，不留存文件内容）。两种方式产生的 `.rce` 格式一致；云加密的**月度次数**单独计算，但**托管额度**（当前托管的恢复密钥数）两种方式共用。

**Q：提示"本月云加密次数已达上限"或"密钥数量已达上限"怎么办？**
A：云加密次数按自然月（北京时间）重置，下月自动恢复；**托管额度**是存量配额（联网密钥与云加密共用），可以在「我的文件密钥」页面删除不再需要的文件密钥记录来立即释放额度。

**Q：加密后没保存 `.key` 文件还能解密吗？**
A：可以登录网页端「我的文件密钥」查看该文件的恢复密钥（需身份验证）。但如果你既删除了服务器上的记录、本地 `.key` 也丢失，则文件将无法恢复。

**Q：DLC（扩展包）怎么购买？**
A：在爱发电（<https://afdian.com/a/ziyit_studio>）购买扩展包商品，下单备注按 `用户ID/扩展包ID` 填写（扩展包ID 可填编号如 `3`，也可填包名如 `ProcessOptimizer`）；付款后后端自动发货，**永久有效**。每单还会**附赠基础包 CoreToolkit（编号 1）**。

**Q：买了扩展包，为什么我的包里多了一个 CoreToolkit？**
A：CoreToolkit 是基础包，购买**任意**扩展包都会随单附赠，属于正常发放，不额外收费。

**Q：付款了但 RC 软件里没看到扩展包？**
A：先确认订单备注填的是 `用户ID/扩展包ID`（不是 `用户ID/用户名`）；备注不对、扩展包编号 / 包名不存在时，后端只记账不发货（后台台账显示 `dlc_not_found`）。可登录后打开「用户中心 → 我的扩展包」（或点顶部导航「我的扩展包」）对照查看已到账的扩展包，也可在用户中心点「查询我的订单」自助补发，或联系管理员在后台「爱发电订单」中人工补发。

**Q：DLC 会过期吗？**
A：不会。通过爱发电购买的扩展包（含附赠的基础包）**永久有效**；管理员在后台人工发放时可选填到期时间，以页面显示为准。

**Q：DLC、RC 密钥和 VIP 是一回事吗？**
A：不是。VIP 是网站会员身份，RC 密钥是 RC 软件的授权许可，DLC 是 RC 的付费扩展包 —— 三者是**独立商品**，互不替代。爱发电下单时备注格式也不同：VIP / RC 密钥填 `用户ID/用户名`，DLC 填 `用户ID/扩展包ID`。

### 7.7 RC 已知 BUG 与反馈

- RC 网页（`download/RC.html`）提供**已知 BUG 列表**，任何访客都能查看，列表展示**提交者**、状态、来源与危险等级。
- BUG 可由 **RC 网页 / 管理员后台 / RC 软件客户端**三处提交；提交后状态默认为「**正在审批**」，管理员（Lv.3+）可改为 正在重新审批 / 正在规划 / 正在修复 / 已修复 / 无问题 / 永不修复。
- 每条反馈由后端自动成文为 Markdown 报告（排版对齐《漏洞说明报告》），前端转 HTML 展示。
- 完整的接口清单、状态机、字段限制与「给前端 / RC 端的对接要求」见 **58**。

---

## 八、巨硬翻译器

### 8.1 功能概述

巨硬翻译器是站点的特色在线翻译工具，名字取自"微软（谐音巨硬）"的玩梗命名。支持多语言文本互译，翻译结果由站点的翻译服务生成，质量稳定。

### 8.2 使用流程

1. 进入翻译器页面。
2. 页面会先进行**人机验证**（Phantom），验证通过后自动进入翻译界面。
3. 在文本输入框中输入要翻译的内容。
4. 选择目标语言（或填写自定义语言代码）。
5. 点击翻译按钮，等待翻译结果输出到结果区域。
6. 复制结果即可使用。

### 8.3 语言支持

- 内置常用语言下拉列表，可直接选择（如中文、英文、日文等）。
- 支持自定义语言代码：如果下拉列表中没有你需要的语言，可以在自定义框中填写语言代码进行翻译。
- 翻译文本上限为 **2000 字**，超出请分次翻译。

### 8.4 每日限额

翻译功能有每日使用次数限制，防止滥用：

| 身份 | 每日次数 |
| --- | --- |
| 普通用户 | 10 次 |
| VIP 用户 | 25 次 |

- 次数按天计算，跨天自动清零重置。
- 用完当天次数后，翻译会提示"今日次数已用完，请稍后再试"。
- 想要更多次数？开通 VIP（价格同 RC 专业版），或联系站长（`ziyitstudio@qq.com`）。

### 8.5 常见问题

**Q：翻译很慢或超时？**
A：翻译由服务器生成，耗时与文本长度和网络有关。超过 30 秒未返回会提示超时，可以重试或减少文本长度。

**Q：为什么进不去翻译界面？**
A：需要先通过人机验证。如果一直停在验证界面，请关闭浏览器开发者工具后刷新重试。

**Q：翻译失败会扣次数吗？**
A：翻译成功才计次数；失败（网络错误、超时等）不会扣减次数。

**Q：结果可以商用吗？**
A：翻译结果请自行判断用途，站点不对翻译内容的使用后果负责。

---

## 九、Backrooms 中文维基

### 9.1 板块介绍

Backrooms 中文维基是网站的**后室（The Backrooms）文档公共数据库**，收录大量中文后室文档，包括层级、实体、物品、现象、团体等各类条目。支持用户投稿，由管理员审核后收录。

### 9.2 内容分区

| 分区 | 内容 | 说明 |
| --- | --- | --- |
| 层级 | 常规层级、特殊层级 | 如 Level 0、Level 109 等 |
| 子区集 | 子层级、子区域 | 附属层级的细分 |
| 实体 | 实体条目 | 后室中的生物与存在 |
| 物品 | 物品条目 | 后室中的特殊物品 |
| 现象 | 现象条目 | 后室中的异常现象 |
| 团体 | 组织团体条目 | 后室中的组织 |
| 加入申请 | 加入后室团体 | 提交申请后由管理员审核 |
| 部署时间 | 常部署时间说明 | 站点管理信息 |
| 收录规范 | 投稿标准 | 投稿前必读 |

> **文档类型与投稿（v0.3.19）**：维基的**层级 / 实体 / 物品 / 现象**四类文档已在后端登记，**每类一套独立接口**；其中**实体文档必须携带 IETS 实体分级标识、现象文档必须携带 IPTS 现象分级标识**，缺标识会被拒收。所有文档都会显示**编写者用户名与头像**（实时更新）。站内约 950 篇既有维基文档已全量迁入后端，统一归属「未知用户」，正文改由后端接口提供；组织类（团体）文档不迁移。接口与前端对接要求详见 59。

### 9.3 层级文档创建器

创建器是维基为作者提供的**在线编辑工具**，可以在网页上直接编写、预览、导出后室层级文档，无需手动写网页代码。

#### 9.3.1 进入与登录

- 打开创建器页面，需要**登录后才能使用**（编辑、加载模板、上传文件、导出等全部功能均需登录）。
- 未登录会看到整页的登录提示，点击「去登录」完成登录后刷新即可使用。

#### 9.3.2 基本编辑流程

1. **选择生存难度模板**：从下拉框选择生存难度（如 0-5 级、死区、宜居、未分级、修正、终结、不适用、等待分级、自定义）。
   - 生存难度默认设为 **1 级**。
   - 除数字等级外，死区/宜居等名称**都可以修改**——直接在等级名称输入框中输入新名称，右侧预览会即时更新。
2. **编辑文档内容**：
   - 页面顶部区域可以填写层级标题、正文描述。
   - 描述、前哨、入口、出口等区块各自独立编辑。
   - 支持添加**多个段落**，每个段落独立编辑。
   - 支持添加**自定义区块**（如"实体"、"发现记录"等），自定义区块支持多个段落。
3. **实时预览**：编辑过程中，右侧（或下方）会实时渲染最终的页面效果，点击预览中的可编辑元素可以直接修改。
4. **加载模板**：可以加载现有层级模板作为起点。
5. **上传 HTML**：可以把已存在的层级 HTML 文件上传，解析回编辑器继续修改。
6. **导出 HTML**：编辑完成后导出为 `.html` 文件，可以提交到审核或本地保存。

#### 9.3.3 生存难度组件

生存难度是层级文档的核心元素，位于文档顶部：

- 包含：生存难度标题、等级名称、属性列表（如实体数量、出口难度等）。
- 数字等级（1-5 级）与特殊等级（死区、宜居、未分级、修正、终结、不适用、等待分级）均支持修改名称。
- 支持自定义颜色：选择自定义预设时可以设置专属颜色，用于区分不同层级风格。
- 属性列表（实体、出口等）也支持逐项修改。

#### 9.3.4 图片与评分

- **图片库**：编辑器内置图片库，可以打开图片库选择已上传的图片插入文档。
- **上传图片**：需要登录，可以上传自己的图片用于文档配图。
- **评分绑定**：可以创建评分、绑定评分到文档（需要登录）。

#### 9.3.5 导出说明

- 导出文件为单个 `.html` 文件，包含全部样式与内容。
- 导出后的文件可直接用浏览器打开查看效果。
- 导出的文件结构符合维基收录标准，可直接用于投稿。

### 9.4 层级提交与审核系统

维基支持**所有登录用户投稿文档**，提交后由管理员审核，通过后收录到公开列表。

#### 9.4.1 谁可以投稿

- **任何已登录用户都可以投稿**（v0.3.20 起**不再要求后室成员身份**，非成员提交页会按当前身份给出提示而不再拦人）。
- 每个作者拥有的文档数量有上限（四类合计 50 个），每月提交次数有上限（四类合计 30 次），超限会提示。

#### 9.4.2 提交层级

1. 进入「层级提交」页面（需要登录）。
2. 填写**层级 ID**：格式为 `Level-` + 编号，例如 `Level-706`、`Level-!+`、`Level-NO-DATA`。
3. 填写**层级名称**：自由填写任意名称，例如"回忆"、"霓虹医院"等，用于列表展示。
4. 选择**层级 HTML 文件**（不超过 1MB）上传。
5. 提交成功后，稿件进入"待审核"状态。

#### 9.4.3 审核状态说明

| 状态 | 含义 |
| --- | --- |
| 待审核 | 已提交，等待基础审核 |
| 已过基础审核 | 通过 AI 初审，等待管理员高级审核 |
| 未过基础审核 | 基础审核未通过，可修改后重新提交 |
| 高级审核中 | 管理员正在审核 |
| 已收录 | 审核通过，已进入公开列表 |
| 未收录 | 高级审核未通过 |
| 已重写 | 原版本已下架，稿件被重写（会保留原文件） |

#### 9.4.4 查看与下载自己的稿件

- 在「我的层级」列表可以看到自己提交的所有稿件及状态。
- **查看**：点击查看可以在新标签页打开自己的稿件预览（即使还在审核中也能看到，页面顶部会显示"正在审核中"提示）。
- **下载**：可以随时把稿件下载为 HTML 文件。
- **修改**：重新选择文件上传即可更新版本，旧版本会自动保留。
- **删除**：可以删除自己的稿件。

#### 9.4.5 审核流程（管理员视角）

审核分为**基础审核**与**高级审核**两级：

**基础审核（所有管理员均可）：**
1. 在待审核列表中查看稿件（支持在线阅读与下载）。
2. 点击「生成提示词」——系统会生成一份审核提示词（包含层级 ID、名称、作者等信息）。
3. 管理员**下载稿件文件**，连同提示词一起交给自己的 AI 进行初审（网站不提供 AI，审核由管理员自行完成）。
4. 把 AI 的完整回复粘贴到「提交审核结果」并提交。
5. 若通过，可选择"直接提交高级审核"；若未通过，会列出理由（邮件将按 1. 2. 3. 编号发给作者）。

**高级审核（Lv.3+ 管理员）：**
1. 在高级审核列表中查看待审核稿件。
2. 点击「通过」收录；或填写理由后点击「拒绝」。
3. 对已收录的稿件，管理员可以执行**重写**（下架并保留原文件）或**删除**。

### 9.5 层级列表与访问

#### 9.5.1 动态层级列表

- 维基提供**动态层级列表**页面，实时展示所有层级：每条显示层级 ID、名称、作者、状态、版本号。
- 已重写的条目有特殊标记。
- 支持按关键词搜索、按状态筛选。
- 点击条目可直接打开对应层级页面（公开可见的只有"已收录"状态的层级）。
- **固定直达地址**：任意层级都有一个固定不变的访问地址，格式为
  `https://ziyit-hacker.github.io/backrooms/normal-levels/?open_level=层级编号`
  把末尾的"层级编号"换成目标层级的 ID（如 `Level-706`）即可直接打开该层级页面。
  例如：`https://ziyit-hacker.github.io/backrooms/normal-levels/?open_level=Level-706`。
  这个地址可复制转发给他人，收到的人打开后会自动定位并展示对应层级。

#### 9.5.2 层级页面访问规则

| 访问方式 | 能看到的层级 |
| --- | --- |
| 未登录（公开访问） | 仅"已收录"（且未被重写）的层级，其他返回无法访问提示 |
| 作者本人 | 自己所有状态的稿件，页面会注入"正在审核中"提示条 |
| 管理员 | 待审核稿件，同样可正常查看 |

> 简单说：公开只能看到正式收录的层级；自己的稿件无论什么状态自己都能看；管理员可以看所有待审核稿件。
>
> 通过上面的固定直达地址访问时，同样遵守本规则：公开访问只能打开"已收录"的层级，审核中或未收录的层级会提示无法访问（作者本人或管理员可正常打开）。

#### 9.5.3 层级评分

**谁能评**：
- 登录用户即可评分，不限于后室成员。
- 只能对**已收录（审核完成）**的层级评分；审核中、未收录的稿件不可评分。

**怎么评**：
- 打开已收录的层级页面，点击 **＋** 投一票好评，点击 **−** 投一票差评。
- 每个用户对同一层级只能投一票，**不可重复、不可撤销**；好评与差评互斥，投出后无法更改。

**评分的计算逻辑**：
- 每个层级的评分结果 = **该层级获得的全部好评数 − 全部差评数**（即"好评 − 差评"）。
- 例如：某层级获得 10 个好评、3 个差评，则评价值为 10 − 3 = **7**；
  若好评 2、差评 5，则评价值为 2 − 5 = **−3**（允许为负数）。
- 没有任何投票时，评价值为 **0**。
- 好评、差评各计数一次，数值不受其他因素（如登录身份、会员等级）影响，纯粹按票数相减计算。

### 9.6 加入申请

如果希望成为后室团体成员（加入成员名单，享有成员标识与成员专属管理能力），可以：

1. 进入「加入申请」页面，填写申请表单。
2. 提交后由管理员在后台「后室成员管理」中审核。
3. 审核通过后即进入成员名单。**注意：投稿并不要求成员身份**，任何登录用户都可以直接投稿。

### 9.7 收录规范

投稿前请务必阅读「收录规范」页面，了解：

- 文档格式要求（标题、生存难度、描述、入口/出口等结构）。
- 内容要求（原创、符合后室设定、无违规内容）。
- 图片与资源使用规范。
- 违规处理说明。

### 9.8 常见问题

**Q：为什么我打开某些层级显示无法访问？**
A：该层级可能尚未收录（还在审核中）或已被重写下架。公开可见的只有"已收录"状态的层级。如果你是该层级作者，登录后即可查看自己的稿件。

**Q：投稿后多久能审核完？**
A：审核由管理员人工进行，时间不定。请耐心等待，状态变化可以在「我的层级」中查看。

**Q：我的稿件为什么被拒了？**
A：未通过时会显示具体理由（按 1. 2. 3. 编号列出），请按理由修改后重新提交。

**Q：修改层级会丢版本吗？**
A：不会。每次修改都会保留旧版本，方便回溯。

**Q：文件大小有限制吗？**
A：提交的 HTML 文件建议不超过 1MB，超出会提示。

**Q：不是后室成员可以投稿吗？**
A：可以。投稿已对**全体登录用户**开放，不再要求后室成员身份；提交后由管理员审核。想进入后室成员名单仍可走「加入申请」。

---

## 十、Minecraft Mod Wiki

### 10.1 板块介绍

Minecraft Mod Wiki 是网站的 Minecraft 模组维基板块，收录模组的详细介绍、合成配方、多语言支持等资料。目前主要收录 **Flight-Management（飞行管理）** 等模组。

### 10.2 内容结构

| 内容 | 说明 |
| --- | --- |
| 模组首页 | 模组介绍、版本信息、下载指引 |
| 合成配方 | 模组物品的合成方式（配方表） |
| 多语言文件 | 模组内置的多语言支持说明 |
| 生成脚本 | 用于维护百科内容的自动化脚本（仅开发者使用） |

### 10.3 使用方法

1. 从首页「图书馆」进入 Minecraft Mod Wiki。
2. 选择模组进入详情页。
3. 查看模组介绍、配方、语言支持等信息。

### 10.4 常见问题

**Q：怎么提交我的模组资料？**
A：模组百科内容由开发者维护，暂不支持用户直接编辑。如有需要可联系管理员。

---

## 十一、站内搜索与全网搜索

### 11.1 功能概述

搜索页提供两种搜索能力：

1. **站内搜索**：搜索网站内所有页面的内容，快速定位需要的信息。
2. **全网搜索**：接入必应搜索，直接在站内发起全网搜索。

### 11.2 使用方法

1. 进入搜索页面。
2. **站内搜索**：在站内搜索框中输入关键词，回车即可看到匹配的站内页面结果，点击跳转。
3. **全网搜索**：在全网搜索框中输入关键词，回车后会跳转到必应搜索结果。

### 11.3 常见问题

**Q：站内搜不到某些内容？**
A：站内索引会定期更新，新发布的页面可能尚未编入索引。也可以改用全网搜索。

**Q：搜索结果是乱码或不全？**
A：请确认关键词输入正确，尝试更换关键词或减少字数。

---

## 十二、病毒库

### 12.1 功能概述

"病毒库"板块（寒坤的病毒库）收录病毒/样本条目，是站点的样本资料库。

### 12.2 访问说明

- 正常用户在「更多」菜单中可以找到病毒库入口。
- **黑名单账号仅保留该入口**——被拉黑的账号登录后只能访问病毒库页面，其余内容全部隐藏。

### 12.3 常见问题

**Q：为什么我只能看到病毒库？**
A：你的账号可能被加入了黑名单。黑名单账号登录后仅保留病毒库访问权限，如认为误判请联系站长申诉。

---

## 十三、开发者日志

### 13.1 功能概述

开发者日志是面向**管理员**的系统日志浏览功能，可以查看网站后台的运行日志。

### 13.2 使用说明

- 入口：后台管理 →「日志查看」（需要 Lv.3 管理员），或首页开发者选项中的「日志」。
- 打开后直接浏览日志内容，可查看系统运行记录与异常信息。

### 13.3 常见问题

**Q：普通用户能看到日志吗？**
A：不能，开发者日志仅对管理员开放。

---

## 十四、后台管理（管理员系统）

### 14.1 概述

后台管理是站点管理员的操作中心。不同等级的管理员可使用的功能不同。

### 14.2 管理员等级说明

| 等级 | 定位 | 开放能力概要 |
| --- | --- | --- |
| Lv.1 | 初级管理员 | 音乐管理、管理员管理、在线客服、层级基础审核 |
| Lv.2 | 中级管理员 | 包含 Lv.1，另开后室成员管理、全局消息广播 |
| Lv.3 | 高级管理员 | 包含 Lv.2，另开层级高级审核、API Key 管理、MOD/DLC 管理、RC 密钥管理、爱发电订单、日志查看 |
| Lv.4 | 站长 | 全部功能，含用户管理（封禁、IP 封禁、升级） |

> 管理员等级由站长在后台设置。**管理员 ≠ VIP**：VIP 是付费会员身份，管理员是权限身份，两者相互独立。

### 14.3 功能区块说明

#### 14.3.1 音乐管理（Lv.1）

- **播放控制**：远程操控站点播放器（播放/暂停/切歌等）。
- **音乐库管理**：增删曲目，配置播放源。

#### 14.3.2 管理员管理（Lv.1）

- 查看管理员列表、添加管理员（按用户编号 + 等级）、编辑等级、撤销管理员。
- **可见性规则**：只能看到"同级及所有低级管理员 + 向上最近一个实际存在等级的管理员"。例如系统只有 Lv.4 与 Lv.1 时，Lv.2 能看到 Lv.1、Lv.2 与 Lv.4。
- **管理员私聊**：管理员之间可以互相发私聊消息（仅限可见范围内的管理员），消息通过页面轮询实时送达，刷新后消息不会丢失。
- **全局消息广播**（Lv.2+）：向指定等级范围内的管理员群发消息。

#### 14.3.3 在线客服控制台（Lv.1）

- **待接入会话队列**：看到用户转人工的会话，点击「接受」接入。
- **回复**：与用户实时对话。
- **结束对话**：处理完毕后结束会话。
- **封禁申诉会话**：处理被封禁用户的申诉，可查看申诉者的封禁信息（用户编号、封禁理由、剩余时间），认可后可解封。
- **客服名单管理**（仅站长）：增删在线客服名单成员。

#### 14.3.4 用户管理（Lv.4）

- **用户列表与搜索**：按用户编号/用户名搜索用户。
- **封禁用户**：支持**定时封禁**（填写封禁时长，分钟为单位，到期自动解封）与**永久封禁**（留空）。
- **解封用户**：手动解除封禁。
- **IP 封禁管理**：封禁/解封 IP、查看封禁列表。同样支持定时与永久。
- **用户升级**：调整用户类型（普通/VIP）。

#### 14.3.5 后室成员管理（Lv.2）

- 查看后室成员列表（按成员编号）。
- 编辑成员权限类型。
- 站长（Adminstrator）不可被编辑。

#### 14.3.6 层级审核（Lv.1+）

- 见"Backrooms 中文维基 → 审核流程"章节。
- 基础审核所有管理员（Lv.1+）均可，高级审核 Lv.3+，重写/删除 Lv.2+。

#### 14.3.7 API Key 管理（Lv.3）

- 系统 API Key 的生成与管理。

#### 14.3.8 MOD / DLC 管理（Lv.3）

- MOD / DLC 资源审核与上架，维护下载中心的资源库。
- **扩展包标记**：资源可标记为 DLC（`isDLC`）；DLC 资源在下载中心对用户可见，但下载需**已拥有该扩展包**，否则返回 `403 DLC not owned`。
- **单用户扩展包管理**：可在该区块为用户查看已拥有的 DLC（编号、包名、获取时间、到期时间）、**发放 DLC**（到期时间留空 = 永久）与**撤销 DLC**。
- **爱发电自动发货的扩展包**同样写入用户的拥有记录（到期时间为空 = 永久），并带来源标记，便于与人工发放区分；重复发放同一扩展包不会重复写入。

#### 14.3.9 RC 软件密钥管理（Lv.3）

- 查看**全部用户**的 RC 密钥：用户名、用户 ID、权限（专业版 / 普通版 / 试用版）、**明文密钥**、来源与到期时间（不显示哈希）。
- 手动发放 / 替换密钥：**直接填写明文密钥**（服务端只保存 AES 加密后的明文，校验哈希按需即时派生，后台无需填写哈希），选择权限与有效期天数（天数为 0 表示永久）。同一权限下已有密钥时，按填写的明文**覆盖**该密钥并重设到期时间。
- **回填历史明文密钥**：读取历史密钥清单，为早期只存了哈希、未留存明文的记录补上加密明文 —— 先「预演」列出将要补录的账号，确认后再写入（只补明文，不新建密钥、不改权限与有效期）。

#### 14.3.10 系统设置

- 站点系统参数配置。

#### 14.3.11 日志查看（Lv.3）

- 跳转开发者日志页面浏览系统日志。

#### 14.3.12 爱发电订单（Lv.3）

- 查看爱发电订单台账：订单号、购买人（用户 ID / 用户名）、金额、方案 ID、发放类型（**VIP / RC 密钥 / DLC 扩展包**）、发放结果与到期时间；DLC 订单会额外记录发放的扩展包编号；未发货的订单会一并列出原因（未付款 / 方案未登记 / 找不到账号 / 备注格式错误 / **扩展包不存在 `dlc_not_found`** 等）。
- **扫订单补发**：主动向爱发电拉取最近订单，对已付款但未发货的订单补发（用于 Webhook 通知漏收的情况）。
- **查单并补发**：按订单号查询并发起补发。
- 发货与补发都会写入审计日志；同一订单重复推送不会重复发放。
- DLC 订单的判定优先级高于 RC 方案与 VIP 方案，详见 7.4。

### 14.4 后台使用注意事项

- 后台所有操作需要对应等级权限，无权操作会提示拒绝。
- 封禁操作请谨慎使用，误封会影响用户正常使用；被封用户可以在登录页发起申诉。
- 在线客服的对话记录是实时同步的，管理员可在任意有网络的设备上处理。

### 14.5 常见问题

**Q：我是管理员但看不到后台入口？**
A：请确认登录的账号具备管理员等级，且等级满足对应功能要求。登录后约 0.5 秒菜单会自动刷新。

**Q：管理员私聊消息会丢失吗？**
A：不会。收件箱消息"读后即删"，页面收到后立即保存到本地，刷新页面后会恢复历史消息。

**Q：我能看到比我等级高的管理员吗？**
A：只能看到"同级及所有低级 + 向上最近一个实际存在等级"。具体规则见 14.3.2。

**Q：封禁用户时填了时长，到期会自动解封吗？**
A：会。定时封禁到期后系统自动解封，无需手动操作；列表中的封禁记录会显示到期时间。

---

## 十五、在线客服系统

### 15.1 功能概述

在线客服是网站的实时客服系统，分为 **AI 客服** 与 **人工客服** 两个阶段：

1. 默认由 AI 客服应答。
2. 需要人工帮助时，输入「转人工」即可请求接入人工客服。
3. 被封禁的用户可以通过「封禁申诉」通道联系客服申诉（见"用户系统 → 账号被封禁怎么办"）。

### 15.2 AI 客服

#### 15.2.1 AI 能做什么

- 回答网站功能、结构、使用方法相关的问题。
- 根据网站说明文档，按需查询相关知识来回答。
- 了解当前对话用户的基本信息（用户名、角色等），从而给出更贴合的回答。
- 有**自主决策能力**：根据对话内容判断是否需要结束对话，或在遇到严重违规（辱骂、刷屏、恶意攻击、离题等）时采取**封禁当前对话用户**的措施。

#### 15.2.2 AI 的边界

- AI 只能查询**当前对话用户本人**的基本信息，**无法查询其他任意用户的信息**。
- AI 的知识来自网站的功能与结构说明文档，仅限网站相关内容，不回答无关问题。
- 涉及越狱、注入、恶意指令等会被安全防护拦截。
- AI 的封禁权限受严格限制：只能封禁当前对话用户，封禁时长有限（1-7 天），到期自动解封，且**无法封禁管理员**。

#### 15.2.3 AI 每日使用限额

AI 问答按身份分级限制每日次数：

| 身份 | 每日次数 |
| --- | --- |
| 普通用户 | 15 次 |
| VIP 用户 | 50 次 |
| 1 级管理员 | 20 次 |
| 2 级管理员 | 50 次 |
| 3 级管理员 | 100 次 |
| 4 级（站长） | 不限 |

- 次数按天计算，跨天自动清零。
- 页面左上角会显示「今日剩余 N 次」，用完会提示"今日 AI 问答次数已用完，请稍后再试；或联系 ziyitstudio@qq.com 升级 VIP"。
- 除每日限额外，还有每分钟的短时限流，防止恶意刷屏。

#### 15.2.4 AI 是怎么"知道"网站的

AI 并不预吞整份文档，而是按需调用工具去查：

| 工具 | 作用 |
| --- | --- |
| `read_knowledge` | 检索《网站功能与结构说明》的对应章节（即本文件） |
| `view_site_page` | **实际打开**官网页面，读取加载完成后的正文 |
| `get_user_info` | 查询当前对话用户的用户名、用户ID、注册时间、管理员等级 |
| `get_user_api_history` | 查询当前对话用户最近调用的 10 个接口 |
| `get_user_discipline` | 查询当前对话用户最近 7 次 AI 对话的纪律记录 |
| `ban_user` | 严重违规时封禁当前对话用户（1-168 小时，不能封管理员） |
| `list_context_groups` | 看当前上下文**有哪几个分类**、每类多少个问答回合（只有分类概览） |
| `list_group_summaries` | 看某个分类下**每个回合的回答简介**（≤15 字，不含消息原文） |
| `get_turn_content` | 按**单个回合编号**取那一条的完整问答原文 |
| `add_memory` | 写入一条长期记忆（≤50 字） |
| `get_memory` | 读取本会话全部长期记忆的完整内容 |
| `delete_memory` | 按编号删除一条长期记忆 |

**关于网址的处理原则**：

- 用户问某个功能**怎么用 / 怎么打开 / 在哪里 / 页面里有什么内容**时，AI 会**直接把对应页面打开、读到实际内容**再回答，并结合文档讲清操作步骤。
- **只有用户明确索要链接**（如「网址是什么」「给我个链接」「发我地址」）时，AI 才把 URL 给出来。
- AI 给出的 URL 一律取自本文档「二十九、附录：网站页面清单」，不会自己拼地址。

**AI 打开页面时是"完整读取"**（只把加载完成后的内容交给 AI）：

1. 跟随重定向直到最终页面（例如 `…/download` 会被跟到 `…/download/`），并把最终地址一并告知 AI；
2. 把响应体**整份读完**之后才交给 AI，不会把加载到一半的内容当成答案；
3. 按页面声明的字符集解码（页面 meta charset → 响应头 → UTF-8 → GBK 等），避免中文乱码；
4. 只有页面确实过长（超过读取上限，默认 30000 字符）时才截断，并明确标注「已截断」；
5. 若地址跳转到了官网之外的域名，直接拦截，不会去读站外内容。

#### 15.2.5 AI 的对话上下文是"压缩"的（分类 + 回答简介）

AI 客服**不会拿到历史对话的原文**（既省 token、也降低被历史消息注入的风险）。历史被压缩成三层，由 AI 用工具**逐层、按需**获取：

| 层级 | 内容 | 获取方式 |
| --- | --- | --- |
| 第一层 | **有哪几个分类**、每类多少个问答回合 | `list_context_groups()` |
| 第二层 | 某分类下**每个回合的回答简介**（≤15 字） | `list_group_summaries(group)` |
| 第三层 | 某一条回合的**完整问答原文** | `get_turn_content(ref)` |

- **压缩规则**：按时间顺序每 **16 个问答回合**（一问一答算 1 个回合）切一类，系统最多只保留**最近 2 类**（即最近约 32 个回合）——更早的内容直接丢弃，AI 看不到。
- **分类号越小越新**：最近的一类编号永远是 `1`；每个回合的编号形如 `1-3`（分类号-类内序号）。因此随着对话推进，同一段内容所在的分类号会顺移。
- **回答简介谁生成**：由 AI 在**每一轮回复末尾**追加一行 `[SUMMARY]本轮回答简介`，后端解析后**从给用户显示的正文里剥掉**（用户看不到这一行），并存入该条 AI 消息的 `summary` 字段；上下文压缩时按每回合 1 条简介展示给 AI。
  - 简介要求：**不超过 15 字（含标点符号）**，且**必须与该会话已有各条简介不重复**。
  - 若 AI 没给、超字数或与已有简介重复，后端自动**按该轮正文前 15 字兜底生成**（重复时加 `…n` 区分），保证简介始终合格且唯一。
- **系统每次都会提醒 AI 先粗看**：系统提示中**强制要求** AI 每轮回答前先 `list_context_groups()` 看分类、需要时再看简介；**强烈禁止默认查看消息完整内容**——只有确实要核对用户原话、当时细节或自己承诺过的事情时，才按**单个回合编号**取那一条，严禁批量拉取消息原文。
- 相关环境变量：`GUIDE_CONTEXT_GROUP_SIZE`（每类回合数，默认 16）、`GUIDE_CONTEXT_MAX_GROUPS`（最多保留几类，默认 2）、`GUIDE_SUMMARY_MAX_CHARS`（简介字数上限，默认 15）。

#### 15.2.6 AI 的长期记忆（每个会话独立）

- AI 可以用 `add_memory(content)` 把需要**跨轮次记住**的信息写进本会话的**长期记忆**（例如用户的版本、环境、正在跟进的问题、已承诺的事情）。
- 单条**最多 50 字（含标点符号）**；`get_memory()` 可读取本会话**全部长期记忆的完整内容与编号**。
- 每个会话**最多 15 条**。写满后 `add_memory` 会**拒绝写入并提示**，此时 AI 必须**先 `get_memory()` 看清全部记忆，自己挑一条最不重要的用 `delete_memory(mem_id)` 删掉，再重新写入**——不删就无法继续存。
- **只属于当前会话**：长期记忆存在该会话记录里（`guide_sessions.json` 的 `longMemory` 字段），**不同会话之间完全隔离、不共享**，也不会跨会话拼接。
- 相关环境变量：`GUIDE_MEMORY_MAX_CHARS`（单条字数上限，默认 50）、`GUIDE_MEMORY_MAX_ITEMS`（每会话条数上限，默认 15）。

#### 15.2.7 AI 什么时候才算「未响应」

AI 在回答前可能连续调用多个工具（查文档 → 打开页面 → 核对账号……），**系统不会按「调用了几轮」硬性掐断**，也不会返回「抱歉，本次处理过程过于复杂，请重试」这类话术——那种话只会把已经花掉的 Token 白白浪费掉。

判定标准只有一条：**只要 AI 在用户提问后 30 秒内调用过工具或输出过内容，就不算「未响应」**，系统会一直让它把答案跑完。

- 真正判为「未响应」只有两种情况：
  1. 连续 **30 秒**既没有调用任何工具、也没有输出任何内容（实际中最常见的触发是某一轮直接返回空内容；单次请求本身的超时另由 HTTP 超时负责——非流式 8 秒、流式 30 秒）；
  2. 撞上**安全上限**（默认 20 轮工具循环，正常对话根本用不到）——这是防止 AI 陷入「无限调工具」把 Token 烧光的兜底。
- 一旦判为「未响应」，用户看到的是统一文案「**AI 服务响应超时，请稍后重试。**」（与非流式的接口超时文案一致），会话记录里记为 `ai_busy`。
- 相关环境变量：`GUIDE_AGENT_STALL_SECONDS`（无进展判定秒数，默认 30）、`GUIDE_AGENT_MAX_ROUNDS`（安全上限轮数，默认 20）。

### 15.3 人工客服

#### 15.3.1 转人工流程

1. 在对话框中输入「转人工」。
2. 系统弹出确认提示，确认后进入等待状态。
3. 系统会从客服名单中随机分配一位**在线客服**；若没有在线客服，则会指定任意一位客服，消息保留等待其上线处理。
4. 客服接入后，页面显示"人工客服·姓名"，即可实时对话。

#### 15.3.2 客服不在线怎么办

- 转人工消息会持久化保存，客服上线后会看到并处理。
- 会话会保留等待，用户无需重复操作。

#### 15.3.3 结束对话

对话可以由任意一方结束：

| 结束方 | 显示文案 |
| --- | --- |
| 用户主动点击「结束对话」 | "您已结束本次对话" |
| AI 主动结束（AI 判定对话完成） | "AI 已结束本次对话" |
| 人工客服结束 | "人工客服已结束本次对话" |

- 结束后会话状态置为"已结束"，输入区会禁用。
- 会话记录会在系统清理周期内自动删除。

### 15.4 常见问题

**Q：AI 为什么不回答我的问题？**
A：AI 只负责网站相关问题。如果提示"无关话题"或被安全防护拦截，请回到网站相关话题。

**Q：AI 主动结束对话是怎么回事？**
A：AI 有自主决策能力，当判断对话已结束、内容离题或出现违规时会主动结束对话。这是正常现象，重新发起对话即可。

**Q：我被 AI 封禁了？**
A：在对话中辱骂、攻击、刷屏等严重违规行为可能触发 AI 的封禁措施。封禁通常是短期的（最长 7 天），到期自动解封；认为自己被误封可以通过登录页的「封禁申诉」联系人工客服。

**Q：转人工后多久有人接？**
A：取决于是否有客服在线。有在线客服时会立即分配；无在线客服时消息会保留，等客服上线后处理。

**Q：次数用完了怎么办？**
A：普通用户每天 15 次，VIP 每天 50 次。次数按天重置。想提升额度可以开通 VIP（价格同 RC 专业版，联系 ziyitstudio@qq.com）。

**Q：客服会话记录安全吗？**
A：会话仅客服相关人员可见，结束后会自动清理，用户无需担心隐私泄露。

**Q：AI 为什么不直接给我网址？**
A：AI 会直接替你把页面**打开读到内容**再讲给你听，这样回答更准确、也不会给错链接。如果你就是想要链接，明确说「给我个链接 / 网址是什么」，AI 就会给。

**Q：AI 的回答是一个字一个字冒出来的？**
A：对，AI 客服采用**流式返回**，文字边生成边显示，不用等整段想完才出现，长回答体验更好。

### 15.5 接口说明（前后端对接）

> 本节面向前端开发者，普通用户不需要了解。

#### 15.5.1 发送消息（流式，推荐）

```
POST /guide/chat/stream
Content-Type: application/json

{ "message": "用户输入的内容" }
```

响应为 SSE（`Content-Type: text/event-stream`），每条事件都是 `data: {json}` 后面跟一个空行。

| 事件的 `type` | 含义 | 前端建议做法 |
| --- | --- | --- |
| `status` | 进度提示，如「正在查阅网站文档…」 | 显示在气泡里；它同时充当心跳 |
| `delta` | 增量文本片段（`text` 字段） | **追加**到当前气泡 |
| `reset` | 本轮已发出的内容是过渡话术，不是最终回复 | **清空**当前气泡已显示的内容 |
| `final` | 最终完整回复（`reply` 字段） | 用 `reply` **覆盖**显示 |
| `done` | 收尾元数据 | 更新剩余次数、输入框状态等 |
| `error` | 失败说明（`message` 字段） | 提示用户；随后仍会收到一条 `done` |

`done` 的字段与 15.5.2 的返回体完全一致。

要点：

- AI 在调用工具前可能先说一句过渡话术，这种情况会先发 `reset` 再发最终内容，所以**必须处理 `reset`**，否则过渡话术会残留在气泡里。
- 不需要调用 AI 的分支（管理员话题拦截、人工客服模式、转人工、越狱/帮助/跑题拦截）**只发一条 `done`**，没有任何 `delta`。
- `[END]`、`[CLOSE]`、`[HUMAN]`、`[SUMMARY]` 这类内部标记不会出现在 `final` 里（后端已剥离）。其中 `[SUMMARY]` 是 AI 每轮回答简介的标记行，**只可能在流式 `delta` 里一闪而过**，收到 `final` 后被覆盖即可，前端无需单独处理。
- 每日次数超限时返回的是普通的 HTTP 429（不是 SSE），按错误处理即可。
- 接口是 POST 且需要登录凭据，前端**不能用 `EventSource`**；请用 `fetch` + `response.body.getReader()` 逐块读取并自行按 `\n\n` 切分事件。
- 建议加 `cache: 'no-store'`。

#### 15.5.2 发送消息（非流式，兼容保留）

```
POST /guide/chat
{ "message": "用户输入的内容" }
```

等 AI 整段生成完一次性返回 JSON，字段：`sessionId`、`messageId`、`reply`、`replyType`、`sessionStatus`、`remaining`、`resetIn`、`showTransferOption`、`welcome`；转人工相关分支还会带 `humanAdmin`。

已切到流式接口的前端可以不再使用本接口。它保留只是为了兼容尚未切换的旧页面，两个接口的入参、限流、每日次数扣减、会话与纪律处置逻辑完全一致。

#### 15.5.3 其他接口

| 接口 | 说明 |
| --- | --- |
| `POST /guide/auth/sync` | 把 JWT 写入 HttpOnly Cookie（`guide_token`） |
| `GET /guide/session` | 获取当前会话与历史消息 |
| `GET /guide/status` | 剩余调用次数与重置倒计时 |
| `POST /guide/session/close` | 用户手动结束自己的对话（结束后自动清理删除） |
| `GET /guide/human/inbox`、`/guide/human/agents` | 客服侧：会话收件箱、客服名单 |
| `POST /guide/human/accept`、`/guide/human/reply`、`/guide/human/close` | 客服侧：接入会话、回复、结束 |
| `GET /guide/human/session/{session_id}` | 客服侧：查看指定会话 |
| `POST /guide/appeal`、`GET /guide/appeal/session`、`POST /guide/appeal/reply` | 封禁申诉通道 |

---

## 十六、全局导航与主题

### 16.1 站点导航

网站所有页面使用统一的顶部导航，包含以下主菜单：

- **功能区**：下载、更多、病毒库、搜索、音乐、巨硬翻译、人机验证等模块入口。
- **ZIYIT 开发者选项**（登录后可见）：日志、常部署时间、后台管理、开发者工具等。
- **图书馆**：Mod Wiki、Backrooms Wiki 及子分类。
- **用户中心**：登录、注册、资料管理、我的密钥、我的文件密钥、我的 RC 密钥、VIP 等。
- **语言选择**：右上角切换站点语言。

### 16.2 导航显隐规则

| 菜单项 | 谁可见 |
| --- | --- |
| 全员菜单 | 所有访客 |
| 普通用户菜单 | 仅登录用户 |
| 管理员菜单（后台管理等） | 管理员或 VIP |
| 管理专用菜单（后台管理、开发者工具） | 管理员 |

> 登录后约 0.5 秒内菜单会自动刷新，无需手动刷新页面。

### 16.3 移动端适配

- 桌面端（宽屏）：水平下拉菜单。
- 移动端（窄屏）：汉堡菜单（☰）折叠，点击展开。
- 移动端展开的菜单会完全遮挡下方内容，避免误触。

### 16.4 明暗双主题

- 站点支持**明色 / 暗色**两套主题。
- 通过页面上的主题切换按钮切换，选择会保留。
- 暗色模式下导航、聊天界面等组件均已适配，夜间使用更舒适。

### 16.5 Cookie 提示

首次访问网站会出现 Cookie 提示弹窗：

- 点击「接受」：同意站点使用 Cookie，继续正常浏览。
- 点击「拒绝」：隐藏提示并按站点策略处理（不影响核心功能使用）。

### 16.6 语言选择器

- 右上角语言选择器支持在支持多语言的页面间跳转切换。
- 切换后主要菜单与界面文字会跟随变化。

### 16.7 常见问题

**Q：导航菜单不见了？**
A：请确认是否登录、是否刷新了页面。登录态变化后菜单会在短时间内自动刷新。

**Q：暗色模式如何恢复明色？**
A：再次点击主题切换按钮即可切回。

**Q：移动端菜单点不开？**
A：请确认点击的是右上角☰按钮；如仍异常，强制刷新页面（Ctrl+F5 或长按刷新）。

---

## 十七、页面结构说明

### 17.1 首页

首页是网站的入口，主要包含：

| 区域 | 内容 | 交互 |
| --- | --- | --- |
| 顶部导航 | 功能区 / 开发者选项 / 图书馆 / 用户中心 / 登录 | 菜单点击跳转，移动端折叠 |
| 轮播图 | 多张宣传图 + 标题 + 指示器 | 自动轮播，可手动切换，点击指示器跳转 |
| 功能区 | 下载 / 更多 / 病毒库 / 搜索 / 音乐 / 巨硬翻译 / 人机验证 | 点击进入对应模块 |
| 开发者选项 | 日志、常部署时间、后台管理、开发者工具 | 按管理员身份显隐 |
| 图书馆 | Mod Wiki、Backrooms Wiki 及子分类 | 下拉展开 |
| Cookie 提示 | 隐私说明 + 接受/拒绝 | 底部悬浮弹窗 |
| 语言选择 | 右上角下拉 | 切换站点语言 |

### 17.2 用户中心

| 页面 | 功能 |
| --- | --- |
| 登录页 | 登录（支持记住我）、跳转注册、封禁申诉入口 |
| 注册页 | 填写用户名、邮箱、密码完成注册 |
| 修改密码 | 修改登录密码 |
| 修改用户名 | 修改展示用户名 |
| 修改资料 | 完善个人资料 |
| VIP 升级 | 爱发电自助购买 VIP（自动发货）、查询到账与到期时间；「查询我的订单」可自助补发 VIP / RC 密钥 / DLC 扩展包 |
| 我的 RC 密钥 | 查看自己名下的 RC 许可证密钥（明文 / 权限 / 到期时间） |
| 我的扩展包 | 查看自己**已拥有**的 DLC 扩展包（包名 / 编号 / 版本 / 说明 / 永久有效 / 获取时间 / 来源），并可直接下载 |
| 公告页 | 查看网站大更新公告 |

### 17.3 音乐页面

| 区域 | 内容 |
| --- | --- |
| 播放器 | 播放/暂停、切歌、进度条、时间、歌词同步 |
| 播放列表 | 全部曲目，区分免费/VIP |
| 下载入口 | 按权限显示下载按钮 |

### 17.4 下载中心

| 区域 | 内容 |
| --- | --- |
| 产品卡片 | 资源名称、简介、权限标签 |
| 搜索框 | 关键词过滤资源 |
| 详情/下载 | 资源详情与下载按钮 |
| DLC 资源 | 扩展包类资源，需账号**已拥有**该扩展包才能下载（否则提示「您未拥有该 MOD/DLC」） |

### 17.5 Backrooms 维基

| 区域 | 内容 |
| --- | --- |
| 首页 | 各分区入口、导航 |
| 层级列表 | 静态列表 + 动态层级合并展示 |
| 层级页面 | 完整文档（生存难度、描述、实体、入口/出口等） |
| 文档创建器 | 在线编辑/预览/导出 |
| 提交页 | 提交层级、我的层级管理 |
| 审核页（管理员） | 基础审核、高级审核、已收录管理 |
| 动态列表页 | 全部层级实时列表 |
| 加入申请 | 后室成员申请 |
| 部署时间 / 收录规范 | 站务信息 |

### 17.6 在线客服页面

| 区域 | 内容 |
| --- | --- |
| 顶部状态徽章 | AI 助手 / 等待人工客服 / 人工客服·姓名 / 对话已结束 |
| 次数与重置 | 今日剩余次数、重置倒计时 |
| 消息区 | AI / 人工客服 / 自己 / 系统提示四种气泡 |
| 输入区 | 文本输入（Enter 发送、Shift+Enter 换行）、结束对话、发送按钮 |
| 转人工弹窗 | 确认后进入人工等待 |

### 17.7 封禁申诉页面

| 区域 | 内容 |
| --- | --- |
| 表单视图 | 输入用户名 + 密码验证身份 |
| 对话视图 | 与人工客服实时对话（轮询同步消息） |
| 状态提示 | 会话结束提示"本次申诉已结束" |

### 17.8 后台管理页面

| 区块 | 等级要求 | 功能 |
| --- | --- | --- |
| 音乐管理 | Lv.1 | 播放控制、音乐库管理 |
| 管理员管理 | Lv.1 | 管理员列表/添加/编辑/撤销、私聊、全局消息 |
| 在线客服 | Lv.1 | 会话队列、接受/回复/结束、申诉处理、客服名单 |
| 层级审核 | Lv.1 | 基础审核 Lv.1+（所有管理员）、高级审核 Lv.3+、已收录管理 Lv.2+ |
| 用户管理 | Lv.4 | 用户搜索、封禁/解封、IP 封禁、升级 |
| 后室成员管理 | Lv.2 | 成员权限管理 |
| API Key 管理 | Lv.3 | 密钥生成与管理 |
| MOD/DLC 管理 | Lv.3 | 资源审核与上架、单用户扩展包发放/撤销 |
| RC 密钥管理 | Lv.3 | RC 密钥发放 |
| 爱发电订单 | Lv.3 | 订单台账、扫单补发、查单补发（VIP / RC 密钥 / DLC） |
| 系统设置 | — | 站点参数配置 |
| 日志查看 | Lv.3 | 跳转开发者日志 |

### 17.9 其他页面

| 页面 | 说明 |
| --- | --- |
| 翻译器 | 人机验证 + 在线翻译 |
| 搜索页 | 站内搜索 + 全网搜索 |
| 病毒库 | 病毒样本资料 |
| 开发者日志 | 管理员查看系统日志 |
| 大更新公告 | 查看最新更新动态 |

---

## 十八、常见问题大全

### 18.1 账号与登录

**Q：注册时为什么必须填写邮箱？**
A：邮箱用于找回密码、注销账号与保留账号。未绑定邮箱的账号可能无法自助找回。

**Q：登录提示"登录尝试过于频繁，请稍后再试"怎么办？**
A：网站对同一 IP 做了登录频率限制，请稍等几分钟再尝试，无需修改任何设置。

**Q：为什么提示我重新登录？**
A：可能原因：登录态过期、账号数据升级后旧登录态失效、或已注销。重新登录即可。

**Q：账号是 cache / temp，为什么用一会儿就自动退出？**
A：这两个是临时体验账号，可用时长约 3.75 分钟，到期后自动清空登录态。请注册正式账号使用。

**Q：修改用户名后需要重新登录吗？**
A：涉及账号凭据的变更，建议重新登录一次以确保状态一致。

**Q：登录时提示 [object Object] 怎么办？**
A：这是旧版本的显示问题，现在已修复。若仍出现，请强制刷新页面后重试。

**Q：被封禁了还能登录吗？**
A：封禁账号登录会显示封禁原因与剩余时间，无法正常登录。可通过登录页的「封禁申诉」通道申诉。

### 18.2 VIP 与权限

**Q：如何开通 VIP？**
A：进入"用户中心 → 升级 VIP 会员"，点击「前往爱发电购买 VIP」，在爱发电选择「ZIYIT账号VIP独立方案（￥56.00/月）」并选择赞助期限；下单时按 `用户ID/用户名` 填写备注，付款后自动开通（有效期 = 赞助月数 × 30 天），刷新页面即可看到到期时间。

**Q：VIP 价格是多少？**
A：基础价 ￥56.00/月，与 RC 专业版同价；支持一次购买多个月，期限越长折扣越高，实际金额以爱发电下单页为准。

**Q：买了 RC 密钥，为什么 VIP 页面查不到？**
A：RC 密钥与 VIP 是两种独立商品。RC 密钥请到「用户中心 → 我的 RC 密钥」查看，VIP 页面只显示会员身份与到期时间。

**Q：RC 密钥在哪里查看？**
A：登录后进入「用户中心 → 我的 RC 密钥」，或点击顶部导航「我的RC密钥」，可查看明文密钥、权限与到期时间。

**Q：DLC（扩展包）在哪里购买、在哪里查看？**
A：在爱发电购买扩展包商品，下单备注按 `用户ID/扩展包ID` 填写（扩展包ID 填编号如 `3` 或包名如 `ProcessOptimizer`）；付款后自动发货且**永久有效**，另附赠基础包 CoreToolkit。查看方式：登录后进入「用户中心 → 我的扩展包」（或顶部导航「我的扩展包」）网页端自助查询，也可打开 RC 软件顶部菜单「**DLC → 管理 DLC**」，或到下载中心对应 DLC 资源下载。详见 7.4。

**Q：ZC / zC 是管理员吗？**
A：**不是**。ZC / zC 是 VIP 身份的标识，与管理员身份无关。管理员由后台等级系统独立决定。

**Q：我是管理员但看不到后台入口？**
A：登录后约 0.5 秒菜单自动刷新；若仍不可见，请确认管理员等级已正确设置。

### 18.3 人机验证

**Q：验证一直失败？**
A：请关闭浏览器开发者工具、关闭可疑代理、刷新重试。详见"人机验证 → 常见问题"。

### 18.4 音乐与下载

**Q：VIP 音乐为什么普通用户听不了？**
A：VIP 曲目需要开通 VIP 后解锁；普通用户可收听/下载免费曲目。

**Q：下载提示无权限？**
A：确认已登录且账号具备该资源权限（普通/免费/VIP 三档）。

**Q：下载 DLC 提示「您未拥有该 MOD/DLC」？**
A：DLC 是 RC 的付费扩展包，需先在爱发电购买（备注 `用户ID/扩展包ID`，付款后自动到账、永久有效）。若已付款仍提示未拥有，请在用户中心点「查询我的订单」自助补发，或联系管理员处理。

**Q：歌词不同步？**
A：歌词文件由管理员维护，部分曲目可能没有；网络不佳时可能延迟加载，可点击歌词行手动跳转。

### 18.5 翻译器

**Q：翻译次数用完了？**
A：普通用户每天 10 次，VIP 25 次，跨天清零。可开通 VIP 或联系站长提升额度。

**Q：翻译超时？**
A：超过 30 秒提示超时，可重试或缩短文本（上限 2000 字）。

### 18.6 Backrooms 维基

**Q：如何投稿层级？**
A：登录后即可（不再要求后室成员身份），进入「文档提交」填写文档 ID、名称并上传 HTML 文件。

**Q：我的稿件状态代表什么？**
A：见"Backrooms 中文维基 → 审核状态说明"表格。

**Q：为什么公开列表里没有我的层级？**
A：只有"已收录"状态的层级公开可见，审核中/未通过的不显示。

### 18.7 在线客服

**Q：AI 客服和人工客服的区别？**
A：AI 即时应答网站相关问题；人工客服通过输入「转人工」接入，适合复杂问题与申诉。

**Q：AI 会封人吗？**
A：在对话中严重违规（辱骂、刷屏、攻击等）时可能触发 AI 封禁（仅限当前对话用户，最长 7 天），到期自动解封。

### 18.8 管理员

**Q：管理员列表可见性规则？**
A："同级及所有低级管理员 + 向上最近一个实际存在等级的管理员"。

**Q：私聊提示"每分钟最多10条"？**
A：私聊发送有频率限制，稍候再发即可。

**Q：收到的私聊刷新后还在吗？**
A：在。消息收到后会保存到本地，刷新后自动恢复。

### 18.9 其他

**Q：站点支持暗色模式吗？**
A：支持，右上角/页面内主题切换按钮可切换明暗主题。

**Q：如何反馈问题或提出建议？**
A：通过邮件联系站长（ziyitstudio@qq.com），VIP 升级、账号找回、封禁申诉、黑名单申诉等均可。

**Q：如何联系客服申诉封禁？**
A：登录页点击「封禁申诉」→ 输入用户名密码 → 进入申诉对话。见"用户系统 → 账号被封禁怎么办"。

---

## 十九、使用技巧与注意事项

### 19.1 日常使用技巧

**1. 记住我，免重复登录**
- 登录时勾选「记住我」，下次打开网站自动保持登录状态。
- 公共电脑使用后请退出登录，保护账号安全。

**2. 善用站内搜索**
- 找功能入口、找页面内容，先在搜索页搜一下，比翻菜单快得多。

**3. 翻译器配合学习**
- 翻译器支持自定义语言代码，语言下拉里没有的语种可以自己填代码。

**4. Backrooms 创作**
- 用文档创建器先选模板再改内容，比从零写快得多。
- 生存难度、描述、自定义区块都支持多段落，长文建议分段编辑。
- 创作完成后先导出预览，再提交审核，避免反复修改。

**5. 在线客服求助**
- 简单问题直接问 AI 客服；被封禁、账号问题建议转人工。
- 转人工前把问题描述清楚，客服处理更快。

**6. 管理员效率**
- 管理员私聊消息会自动恢复，不怕刷新丢消息。
- 层级审核时先下载稿件再让 AI 初审，提示词一键复制，效率更高。

### 19.2 安全注意事项

- 不要在任何非官方渠道输入你的账号密码。
- 网站不会通过邮件/私信索要你的密码。
- 开启开发者工具（F12）访问网站时，人机验证等安全机制可能拦截，属正常保护。
- 你的完整个人档案（邮箱、注册与登录时间、登录 IP 记录、封禁详情、各项配额等）只有**你本人**与**管理员**可见；其他登录用户**完全无法读取**你的档案（请求一律被拒），网站也不会把这类信息展示给第三方。
- 在对话中辱骂、攻击、刷屏可能触发 AI 或管理员的封禁措施，请文明交流。
- 发现账号异常（异地登录、资料被改）请尽快修改密码并联系站长。

### 19.3 社区规范

- 请尊重其他用户与管理员，文明发言。
- Backrooms 投稿必须为原创内容，抄袭、低质内容会被拒绝。
- 不要在客服对话中发送广告、垃圾信息或无关内容。
- 遵守网站收录规范与法律法规。

### 19.4 常见操作误区

| 误区 | 正确做法 |
| --- | --- |
| 以为 ZC 是管理员 | ZC 是 VIP 标识，管理员另有等级体系 |
| 以为所有音乐都能免费下 | 只有免费曲目对普通用户开放，VIP 曲目需 VIP |
| 以为投稿后立刻可见 | 需管理员审核，通过后才收录公开 |
| 以为封禁是永久的 | 大多数是定时封禁，到期自动解封 |
| 以为 AI 客服什么都知道 | AI 只了解网站相关内容，其他问题请自行搜索 |
| 以为翻译失败会扣次数 | 只有翻译成功才计次数 |

---

## 二十、术语表

| 术语 | 含义 |
| --- | --- |
| VIP | 付费会员身份，解锁高级音乐/下载与更多功能额度 |
| ZC / zC | VIP 身份的标识，不是管理员 |
| 管理员 | 拥有后台管理权限的用户，按 Lv.1-Lv.4 分级 |
| 站长 | 最高级管理员（Lv.4），拥有全部权限 |
| 后室成员 | 申请并加入后室成员名单的用户（**投稿资格已对所有登录用户开放，不再与成员身份绑定**） |
| Phantom | 网站自研的无感人机验证系统 |
| RC | 网站出品的系统工具（Release Control） |
| 层级 | Backrooms 中的楼层/空间条目 |
| 生存难度 | 层级顶部的难度等级标识（0-5 级、死区、宜居等） |
| 基础审核 | 层级投稿的第一级审核（管理员用自有 AI 初审） |
| 高级审核 | 层级投稿的第二级审核（管理员人工审核） |
| 收录 | 层级审核通过并公开展示 |
| 重写 | 已收录层级被下架并重写（保留原文件） |
| 封禁申诉 | 被封禁用户联系人工客服申诉解封的通道 |
| 每日限额 | 翻译器/在线客服 AI 按天计算的次数限制 |
| 人机验证 | 区分真人与脚本的验证机制 |
| 恢复密钥 | RC 加密文件的解密凭据，随加密生成并可在网页端查看 |
| `.key` 文件 | RC 加密后生成的密钥文件（第一行恢复密钥 ID，第二行恢复密钥） |
| `.rce` 文件 | RC 加密后的文件本体，需配合对应恢复密钥解密 |
| 联网密钥 | RC 文件加密方式之一：向服务器申请密钥后本地完成加密 |
| 云加密 | RC 文件加密方式之一：由服务器完成加密后回传，单文件上限 100MB |
| RC 许可证密钥 | RC 软件的授权密钥（5 组 × 5 位大写十六进制），与文件「恢复密钥」无关 |
| DLC / 扩展包 | RC 软件的付费增值包（下载中心里 `isDLC` 的资源），爱发电自助购买后**永久有效** |
| 基础包 / CoreToolkit | `modId 1` 的扩展包，购买任意 DLC 时随单**附赠**，不可单独购买 |
| 爱发电 | 第三方赞助 / 支付平台，网站 VIP、RC 密钥与 DLC 扩展包均通过它自助购买 |

---

## 二十一、功能更新记录

> 以下记录本站点面向用户的重要功能更新，帮助用户了解新增能力。

### 2026-09 更新

**Passkey（通行密钥）上线：可以用指纹 / Windows Hello / 安全密钥登录了（v0.3.27）**
- **什么是 Passkey**：就是 GitHub / 微软那种「刷指纹、刷脸、插安全密钥」的登录。私钥**只存在用户自己的设备里**（手机 / 电脑 / YubiKey），**永不发送给服务器**；服务器只存公钥。因此就算网站数据库被拖走，也**无法伪造任何人的登录**。
- **绑定的是官网域名，不是后端域名**：Passkey 遵循 W3C WebAuthn 标准，浏览器强制要求「页面域名 == RP ID」。本站前端在 GitHub Pages（`ziyit-hacker.github.io`），所以 Passkey 只在这个域名下有效 —— **后端换 ngrok 地址不会让已绑的 Passkey 失效**，反过来在钓鱼站上刷不出来（浏览器层面直接拒绝）。因此前端页面必须用 HTTPS 打开，本地 `file://` 或后端 ngrok 页面上**用不了**。
- **可以当第一因子，也可以当第二因子**：
  - 当第二因子：`/auth/login` 回 `mfaRequired` 后，`methods` 里出现 `passkey` 就可以点「用 Passkey 完成验证」；
  - 当第一因子：`邮箱+Passkey` 这类组合，登录页输用户名后直接点「用 Passkey 登录」即可（无需密码）。
- **一个账号最多绑 10 个**，强烈建议**至少在两个设备上各绑一个**（换设备才不至于被锁在外面）；绑好后可在账号安全页里看到每个 Passkey 的名字与最后使用时间，并可单独删除。
- **删不掉最后一把钥匙**：删除属于「弱化」操作 —— 已绑 2FA 的账号必须给 2FA 码或恢复码；删完若不够所要求的验证方式数量会 **403** 且**不落库**（管理员删最后一个必然被拦）。
- **「无密码登录」现在真的能开了**：前提是**已验证邮箱 + 已绑 2FA + 已绑 Passkey** 三者齐备，此时可在账号安全页关掉密码登录（`GET /auth/security` 的 `canDisablePassword` 会告诉前端能不能关）。
- **不用装任何库**：后端没有引 `py_webauthn`（它会把生产环境在用的 `cryptography` 从 47 顶到 50，威胁 RC 密钥加密），改用现成的 `cryptography` + 自写极简 CBOR 解码器，按 W3C WebAuthn L2 **逐项校验** `challenge` / `origin` / `rpIdHash` / 用户在场标志 / 凭据一致性 / 签名。签名计数（`signCount`）**只记录不拒绝** —— 同步型 Passkey（iCloud / Google 密码管理器）计数常年为 0，据此拒绝会误伤真实用户。
- 新增接口：`POST /auth/security/passkey/setup`、`POST /auth/security/passkey/enable`、`POST /auth/security/passkey/delete`、`POST /auth/login/passkey/start`、`POST /auth/login/passkey/finish`；工程约定新增 `WEBAUTHN_RP_ID` / `WEBAUTHN_RP_NAME` / `WEBAUTHN_ORIGINS` 三个环境变量（留空自动按 `HOME_URL` 推导）。详见 **61**。
- **修掉三个登录问题（本次一并上线）**：
  1. **邮箱验证码「刚收到的码反而无效」**：码原先绑在登录票据上，而每补一个因子就会换一张新票据（旧的作废），码跟着丢了 —— 用户点「重发」后输哪封邮件里的码都 401。现在码**按账号存**，**有效期内重复发送沿用同一个码**，换票据也不丢；用掉即废。
  2. **邮箱 + 密码 这种组合走不通**：`邮箱+密码` 是合法组合，但第二因子原本只支持 2FA / 恢复码 / 邮箱码，密码这条路是断的。现在 `/auth/login/factor` 支持 **`factor: "password"`**（传 `password`，md5），并且与 `/auth/login` **共用同一套失败锁定**（连续 5 次错锁 15 分钟）。
  3. **Passkey 登录弹「使用通行证密钥登录出现问题」（Windows 安全中心）**：账号没绑过 Passkey 时，`/auth/login/passkey/start` 原本回「200 + 空的 `allowCredentials`」，浏览器拿着空列表只会让 Windows Hello 报这句看不懂的错。现在改为直接回 **400 + 一句能看懂的提示**（账号不存在与没绑 Passkey 的响应**完全相同**，防用户名枚举）。
- **给前端的要求**（前端仓库需自行修改，本次只动后端）：
  1. **Passkey** 必须由前端调用浏览器原生 WebAuthn（`navigator.credentials.create()` / `.get()`），**没有别的办法绕过**，也**不需要引任何第三方库**。坑：① `challenge` / `id` / `rawId` / **`user.id`** 是 base64url 字符串，喂给浏览器前**必须转 ArrayBuffer**、回传时再转回来；② 注册时**不要自己拼 `authenticatorData`**，后端只认 `response.attestationObject`，把 `response` 原样回传即可；③ 必须 HTTPS 且页面域名与 RP ID 一致。**61.7.2 提供了可直接照抄的 `assets/webauthn.js`**（base64url 互转、环境自检、`bindPasskey()`、`loginWithPasskey()`、统一错误提示）。
  2. **登录页**：「用 Passkey 登录」按钮**不能点了就无脑调浏览器** —— 后端在「账号没绑 Passkey」时回 **400**，直接展示那句提示即可（否则 Windows Hello 会弹「使用通行证密钥登录出现问题」）；拿不准就隐藏按钮。第二步按回包的 `methods` 渲染：`email` / `totp` / **`password`（收 md5 密码，传 `factor:"password"`）** / `passkey`，并保留「用恢复码」兜底入口。
  3. **「账号安全」页**：加 Passkey 区块（列表 + 绑定 + 删除）；邮箱验证码的「发送」按钮加 **60 秒倒计时**（倒计时内后端不重发，但码不变、仍可用）。
  完整清单见 61.7。

**账号安全升级：多种验证方式（密码 / 邮箱验证码 / 2FA）（v0.3.26）**
- **登录可以要求「两种验证方式」了**：后端把验证方式抽象成 4 类**因子** —— `password`（密码）/ `email`（已验证邮箱 + 一次性验证码）/ `totp`（2FA，微软或 Google Authenticator 等标准动态码）/ `passkey`（通行密钥，**v0.3.27 已上线**）。用户可以要求**凑够 2 类**（如 `密码+2FA`、`密码+邮箱`、`邮箱+2FA`），任意组合，**同一类做两遍不算两种**。
- **默认行为完全不变**：`requiredFactors` 默认 **1**，老用户照旧「用户名 + 密码」直接登录，`/auth/login` 的请求体与返回值**一个字都没改**；`users.json` 里没有 `security` 字段的老账号**不需要任何数据迁移**。
- **2FA 自助绑定 + 一次性恢复码**：安全中心里调 `totp/setup` 拿密钥与二维码（后端直接给 PNG data URI，前端不用引二维码库）→ 输码 `totp/enable` 启用 → **此刻一次性下发 10 个恢复码**（后端只存 bcrypt hash，之后查不回来，用掉即废）。**2FA 防重放**：同一个时间窗的码用过一次就不能再用；容 ±1 个时间窗照顾手机时钟漂移。
- **管理员强制两种、且关不掉**：有后台权限的账号登录时被后端**惰性写入** `requiredFactors = 2`、`policySource = "admin"`，调低或关闭 2FA 一律 **403**。若管理员还没绑第二方式，登录会返回 `enrollRequired + enrollToken`（**不给登录凭据**），用这张**受限票据**只能进安全中心绑 2FA，绑完当场直接签发登录凭据。
- **「无密码登录」的前提**：**已验证邮箱 + 已绑 2FA + 已绑 Passkey** 三者齐备才允许关掉密码登录。Passkey 还没上线，所以现在关不掉（接口会明确说缺什么）。
- **兜底不做本地解锁脚本**：靠 10 个恢复码 + 换另一类因子（`密码+邮箱` / `邮箱+2FA` / `密码+2FA`）就能自救。
- **票据纪律**：登录中间票据 `mfaToken` 300 秒有效、**换 IP / 换 User-Agent 立即作废**、用一次就废、**错满 5 次作废**（防 6 位码被爆破）；**它不能当登录凭据用**，调 `/auth/me` 之类一律 401。受限票据 `enrollToken` 同理，且**只被安全中心接口认**。
- **密钥永不外泄**：`security` 字段（TOTP 密钥、恢复码 hash、passkey 公钥）已从 `/auth/me`、`/users` 等所有公开视图整体屏蔽，安全中心只给脱敏总览。
- 新增接口：`POST /auth/login/email/start`、`POST /auth/login/factor`、`GET /auth/security`、`POST /auth/security/policy`、`POST /auth/security/totp/setup`、`POST /auth/security/totp/enable`、`POST /auth/security/totp/disable`、`POST /auth/security/recovery-codes`。详见 **61**。
- **给前端的要求**（前端仓库需自行修改，本次只动后端）：登录页要支持**两步登录**（见 `mfaRequired` / `enrollRequired`）；第二步按 `methods` 渲染（邮箱验证码 / 2FA 码 / **提供「用恢复码」兜底入口**）；每次回包里的 `mfaToken` 都要覆盖本地旧的；新增「账号安全」页面（`user/security.html`），绑定二维码**直接用回包 `qrPng` 塞 `<img src>`**（不要引二维码库），**恢复码必须让用户确认已保存后再关闭弹窗**；管理员界面**不要给「关闭两种验证」按钮**（后端也会 403）。密码仍传 **md5**。完整清单见 61.7。

**安全加固：改邮箱需旧邮箱确认 + 资源包（.rcm）内容校验（v0.3.25）**
- **改邮箱不再立即生效**：`PUT /users/email` 提交后只向**当前（旧）邮箱**发确认链接（15 分钟有效），**点击确认后邮箱才真正改掉**，随后再向新邮箱发一封验证邮件；`PUT /users/profile` 里带 email 的写法也并进同一条流程（此前那条**连验证信都没有**，直接就改）。这样即使密码泄露，也无法仅凭密码把邮箱改成攻击者的、再走「忘记密码」接管账号 —— 真主人一定会在**旧邮箱**里看到确认邮件。详见 2.5。
- **资源包（.rcm）必须验内容**：`lastFileUrl` 不再「以 `.rcm` 结尾就收」，而是把内容取回来按官方格式验一遍（ZIP + `ModConfig.json` + `Name` / `License` / `Version` / `Path.MainCode` / `Menu` 等全字段，`Path.Code` / `Path.Lang` 列出的文件必须在包内，DLC 的 `.rce` 加密布局也查），不合法直接 `400`。详见 6.5。
- **拒收内网地址**：远程 `.rcm` 只允许公网 http(s)，`127.0.0.1` / `localhost` / `169.254.169.254` 之类一律拒收（防被当成向站内网发请求的跳板）；跳转最多 5 跳，**每跳都重新校验**。
- **清理测试残留**：正式环境资源库里 `modId 5 / 6 / 7` 三条测试记录（`http://127.0.0.1:80/x.rcm`、`http://localhost/x.rcm`、`http://example.com/ok.rcm`）已删除，`nextModId` 保持 8 不回退（避免 ID 复用）。
- **关于「profile 存储不过滤 / bio 可存 `<script>`」（审计第 2 项）**：后端**没有** `bio` / 个人简介字段 —— `UpdateProfileRequest` 只有 `username` 与 `email`，`users.json` 里也不存在该字段，因此这条不适用于当前后端；防御责任在**渲染侧**（见下方「给前端的要求」第 3 条）。
- **给前端的要求**（前端仓库需自行修改，本次只动后端）：
  1. `user/profile.html`：改邮箱按钮的成功提示要从「邮箱修改成功」改成「**确认邮件已发送到您当前的邮箱，请点击邮件中的链接完成修改**」—— 邮箱现在不再即时生效（后端返回体仍是 `{"message": "Confirmation email sent"}`）。
  2. `assets/blacklist.js`：第 188-189 行把登录凭据 `authToken` **原文** `console.log` 到了控制台（`console.log('从cookie/localStorage获取的用户数据:', userData)`），请删掉该打印；控制台里能看到的 token 会被同页脚本 / 浏览器插件顺手拿走。（`assets/ziyit_api.js` 第 133-134 行只打印「有没有 token」的布尔值，无泄露，可保留。）
  3. 页面渲染后端可控文案（资源名 / 资源简介 / 反馈正文等）时必须做 HTML 转义或白名单净化，不要用 `innerHTML` 直接拼接。
- 新增环境变量：`RCM_MAX_BYTES`（默认 `33554432`）、`RCM_FETCH_TIMEOUT`（默认 `15`）。

**AI 客服上下文压缩 + 长期记忆（v0.3.24）**
- **上下文改为「压缩后按需获取」**：AI 客服不再拿到历史对话原文，而是被压成三层——`list_context_groups()`（有哪几个分类）→ `list_group_summaries(group)`（某分类下每回合的回答简介，≤15 字）→ `get_turn_content(ref)`（按单个回合编号取那一条完整原文）。按每 **16 个问答回合**切一类，系统最多只保留**最近 2 类**，更早的直接丢弃。
- **回答简介由 AI 自己生成**：AI 每轮回复末尾追加一行 `[SUMMARY]≤15 字简介`（字数含标点、且与已有简介不重复），后端解析后**从给用户显示的正文里剥掉**并落库；AI 没给 / 超字数 / 重复时，后端按该轮正文前 15 字兜底生成（重复的加 `…n` 区分）。
- **系统每轮都强令 AI 先粗看、禁止翻原文**：系统提示要求 AI 每次回答前先看分类、需要时看简介；**强烈禁止默认查看消息完整内容**，只有要核对用户原话、当时细节或自己承诺过的事情时才按**单个回合编号**取一条，严禁批量拉取消息原文。
- **新增长期记忆（每会话独立）**：`add_memory(content)`（单条 ≤50 字含标点）/ `get_memory()`（读全部记忆完整内容与编号）/ `delete_memory(mem_id)`（按编号删）。每会话**最多 15 条**，写满后写入会被拒绝并提示，AI 必须**自己先看全部记忆、挑最不重要的一条删掉再写**。长期记忆存在**该会话**的 `longMemory` 字段里，**会话之间完全不共享**。
- 新增环境变量：`GUIDE_CONTEXT_GROUP_SIZE` / `GUIDE_CONTEXT_MAX_GROUPS` / `GUIDE_SUMMARY_MAX_CHARS` / `GUIDE_MEMORY_MAX_CHARS` / `GUIDE_MEMORY_MAX_ITEMS`（旧的 `DEEPSEEK_MAX_HISTORY` 已移除）。详见 15.2.5 / 15.2.6。
- **不再出现「本次处理过程过于复杂」那句废话**：AI 连续调工具不再被「5 轮」硬性掐断，改为**只要 30 秒内调用过工具或输出过内容就继续跑完**；真判为未响应时统一回「AI 服务响应超时，请稍后重试。」。新增环境变量 `GUIDE_AGENT_STALL_SECONDS`（默认 30）、`GUIDE_AGENT_MAX_ROUNDS`（安全上限，默认 20）。详见 15.2.7。

**RC BUG 漏洞编号 RCVE 由系统强制生成（v0.3.21）**
- **编号不再靠人填**：RC BUG 反馈的**标题由系统拼装** —— 管理员确认修复并填写「**解决版本**」后，系统自动分配漏洞编号 `RCVE-出现版本-解决版本-该解决版本的BUG编号`（如 `RCVE-26.9-26.9-001`），并把 `[编号]` **强制加在标题前**（列表里显示成 `[RCVE-26.9-26.9-001] 管理员权限运行时 MOD/DLC 管理界面拖放安装功能失效`）。提交者（RC 网页、管理员后台、RC 软件端）**都不能自己输入编号**，标题里自带的前缀会被服务端去掉。
- **两套编号各司其职**：反馈编号 `RCFB-####` 提交即分配、**永久不变**；漏洞编号 `RCVE-…` 只在确定解决版本后才出现，未定版本时为空，列表里不显示编号前缀。
- **出现版本 / 解决版本**：出现版本默认取提交时「涉及版本」的版本段（`26.9-snapshot-26W35A` → `26.9`），管理员调查后可在后台改；解决版本由管理员确认修复后填写。
- **第三段按解决版本分段自增**：`26.9` 修复的就是 `26.9` 段的编号、`26.8` 修的是 `26.8` 段的编号，同一段内从 `001` 起、换解决版本重新计；既有历史编号（26.7 / 26.8 / 26.9 各段）继续续号，不会重号。
- **历史数据无感兼容**：此前手工写在标题里的 `[RCVE-…]` 前缀会被后端识别为编号与出现 / 解决版本，标题与编号都不变。
- 详见 58.3.1 / 58.4.5。

**投稿权限对全体登录用户开放（v0.3.20）**
- **不再要求后室成员身份**：四类文档（层级 / 实体 / 物品 / 现象）的**提交、修改、删除自己的稿件**，现在只要**登录**即可，不再校验 `user.json` 后室成员名单；管理员端接口（审核、重写、管理员删除）不变。
- **403 含义随之变化**：投稿相关接口的 403 现在只剩两种情况 —— **账号已被封禁**，或**改/删的不是自己的稿件**；「仅限后室成员使用」这条提示已从后端与前端的提交页移除。
- **前端按权限展示**：`backrooms/submit.html` 会调 `GET /auth/user-type` 显示当前身份（超级管理员 / 管理员 / 成员 / VIP 用户 / 普通用户），成员与管理员显示「可直接提交」，其余身份明确提示「投稿已对全体登录用户开放」，不再拦人。
- 其它约束不变：ID 前缀、名称非空、`.html`、≤1MB、实体必须带 IETS / 现象必须带 IPTS 标识、每账号拥有 ≤50 个文档、每月 ≤30 次上传。

**Backrooms 四类文档系统 + 人机验证视频分包下发（v0.3.19）**
- **文档从两类扩展到四类**：除原有的「层级」，新增「**实体**」「**物品**」「**现象**」三类文档，**每类一套独立接口**（`/backrooms/levels` / `/backrooms/entities` / `/backrooms/objects` / `/backrooms/phenomena`）。**实体文档必须携带 IETS 实体分级标识、现象文档必须携带 IPTS 现象分级标识**，缺标识一律 400 拒收；层级与物品不要求标识。详见 59。
- **文档编写者信息实时显示**：文档页 / 元数据接口回包带 `author`（`userId` / `username` / `avatarId` / `avatarUrl`）；索引里**只存 `ownerId`**，用户名与头像每次请求**现查 users.json**，用户改名或换头像后文档上立刻同步，不留旧快照。
- **全站维基文档迁移到后端存储**：原有约 950 篇静态维基文档（层级 / 实体 / 物品 / 现象）已**全量导入后端文档库**（`backrooms_levels/index.json` + `files/`），统一归属「**未知用户**」（`ownerId = 0`），页面正文改由后端接口提供；文内相对引用已解析为原绝对地址，图片与站内跳转照旧可用。详见 59.4。
- **人机验证体验页**：新增 `GET /experience/status` 与体验凭据通道（请求头 `x-phantom-exp-key`）——登录用户在体验页**自行输入任意格式合法的 API 密钥**即可试用；密钥**只校验格式**，扣的是**当前登录账号名下**的 API 密钥额度（服务端权威），所以填别人的密钥也花不到别人的额度。**额度为 0（无密钥 / 已停用 / 月额度或日额度耗尽）即禁止验证**。详见 60。
- **验证视频改为「就绪握手 + 分包下发」**：`POST /challenge` 不再随回包一次性下发整段视频字节，只回 `videoStream` 元数据；前端准备好播放器后调 `POST /video/ready` 握手，再按 `index = 0,1,2…` 调 `POST /video/chunk` **逐包取（每包 5 帧）**。**严格串行**：只接受服务端游标指向的下一包，跳号 / 抢跑 / 并发预取一律 409，因此**不可能同时发起请求和发送数据**。详见 60.4 / 60.5。
- **实时流验证更密**：实时流上报间隔由 100ms 收紧到 **50ms**（`stream.intervalMs`），单位时间采样次数翻倍；同时放宽抖动容差比例，兼顾网络状况与不同用户设备性能，避免误伤真人。详见 60.7。

**在线客服 AI 体验升级（v0.3.18）**
- **AI 回复改为流式返回**：新增流式接口 `POST /guide/chat/stream`（SSE），AI 的回答**边生成边显示**，不用等整段想完才出现。原有 `POST /guide/chat` 保留以兼容尚未切换的旧页面，两者入参、限流、每日次数扣减与会话纪律完全一致。事件协议与前端对接要求见 15.5.1。
- **AI 读网页改为"完整读取"**：AI 打开官网页面时会**跟随重定向到最终页面**、把响应体**整份读完**之后才交给它（不再出现只读到一半、或重定向后拿不到内容的情况）；按页面声明的字符集解码（meta charset → 响应头 → UTF-8 → GBK 等）避免中文乱码；只有页面确实过长（默认 30000 字符）时才截断并明确标注；若地址跳转到了官网之外会被直接拦截。见 15.2.4。
- **AI 找网址的策略更明确**：用户问「某功能怎么用 / 怎么打开 / 在哪里 / 里面有什么内容」时，AI 会**直接把页面打开读到内容**再回答；**只有用户明确索要链接**时才把 URL 给出，且 URL 一律取自「二十九、附录：网站页面清单」，不会自己拼地址。见 15.2.4。

**RC BUG 反馈系统（v0.3.17）**
- **三个入口提交、一个公开列表**：RC 网页（`download/RC.html`）、管理员后台、RC 软件客户端都能提交 BUG 反馈，共用同一套后端接口；`GET /rc/bugs` **公开可读**，任何访客都能看到已知 BUG 列表（含提交者、状态、来源）。
- **状态机（7 态）**：提交后默认「**正在审批**」；管理员（Lv.3+）可改为「正在重新审批 / 正在规划 / 正在修复 / 已修复 / 无问题 / 永不修复」。普通用户提交后若管理员未改状态，就一直显示「正在审批」。
- **提交必须带凭据**：网页端带 `Bearer` token，RC 软件端带许可证 `username + key_hash`；**提交者由服务端从凭据解析**，客户端无法自称身份，列表与报告里的提交者以服务端记录为准。提交者是否管理员由服务端判定（用于报告里标注「管理员 / 普通用户」）。
- **自动成文 Markdown**：后端按 `Release_control/BUGs`《漏洞说明报告》的排版，为每条反馈生成报告正文（表格头 + 问题描述 / 复现步骤 / 期望结果 / 实际结果 / 补充信息 + 状态变更记录附录），随接口回包（`markdown` 字段）并落盘到 `rc_bugs/<编号>-BUG反馈报告.md`。前端用 `marked` 转 HTML 显示。
- **防刷与审计**：提交按 IP 限流（每 IP 5 分钟最多 5 条）；提交与改状态均写审计日志（`rc_bug_submit` / `rc_bug_status`）。
- 后端接口与「给前端 / RC 端的要求」详见 58。

**爱发电 DLC（扩展包）自动发货（v0.3.16）**
- **扩展包作为商品自助购买**：RC 的 DLC（扩展包）在爱发电上以商品形式出售（商品 ID `3f7d2856b3be11f1a6825254001e7c00`），下单备注填 `用户ID/扩展包ID` —— 扩展包ID 填**编号**（如 `3`）或**包名**（如 `ProcessOptimizer`，不分大小写）都认。付款后后端**自动发货**，无需人工审核。
- **一单发两个包、永久有效**：购买任意扩展包都会**附赠基础包 `CoreToolkit`（`modId: 1`）**，与所购扩展包一并发放，且两者**永久有效**（到期时间为空）。
- **判定优先级**：DLC 商品 ID 命中 → 只发扩展包（**不发** RC 密钥、**不发** VIP）；未命中再依次判 RC 方案、VIP 方案。DLC 判定置于方案白名单**之前**，以兼容商品（SKU）形式下单（此类订单可能没有 `plan_id`）。
- **幂等与异常**：同一订单号只发一次；已拥有同一扩展包不会重复写入；备注解析不出扩展包（格式错 / 编号不存在 / 填的是附赠基础包）时**只记台账不发货**（原因 `dlc_not_found`），由管理员在后台人工处理。发货写入审计日志 `afdian_grant_dlc`。
- **管理端**：后台「MOD / DLC 管理」可为单个用户查看 / 发放 / 撤销扩展包；「爱发电订单」台账新增 `DLC` 发放类型与所发扩展包编号，扫单补发 / 查单补发同样覆盖 DLC。用户中心「查询我的订单」自助查单也会补发 DLC。
- **网页端自助查询**：新增「我的扩展包」页面（`user/dlc.html`）与 `GET /dlc/mine`，登录后可查看自己**已拥有**的扩展包（包名 / 编号 / 版本 / 说明 / 永久有效 / 获取时间 / 来源）并直接下载；全站顶部导航（含「我的RC密钥」的页面）均已加入口。
- 详细说明见 7.4。

**安全修复（v0.3.11）**
- **修复越权读取用户档案**：此前 `GET /users/{user_id}` 只校验「是否登录」，任何登录用户都能遍历用户 ID 读到他人的完整档案（含邮箱、登录 IP 记录、封禁详情、各项配额）。现已改为**仅本人或管理员**可访问，其他人一律返回 403；且**先判权限再看存在性**，连"该 ID 存不存在"都不再泄露。该接口此前无任何调用方（前端资料页走 `/auth/me`），属早期遗留的"查看任意用户资料"接口。见 19.2。
- **用户名写入加字符白名单（防存储型 XSS）**：注册、修改资料、修改用户名、管理员编辑用户四条写入路径统一校验，用户名只允许中英文、数字、下划线、连字符、点和空格，长度 1–24；`<script>alert(1)</script>`、`<img src=x onerror=...>` 之类的 payload 根本存不进来。另外后台用户列表渲染用户名改用纯文本（`textContent`）而非 `innerHTML` 拼接，作为渲染端的第二道防线。
- **移除自激活接口**：删除了一个遗留接口（`POST /keys/activate`）—— 它允许调用方自行提交密钥哈希并自报权限，而客户端校验又是拿这份自报内容去比对，等于任何人可凭空把自己"激活"成任意权限。此前后台出现的一批垃圾密钥记录即由此产生。RC 客户端与网页端均不调用该接口，删除不影响正常使用。
- **管理员发放密钥改为填写明文、且不再落盘哈希**：后台发放 / 替换 RC 密钥时直接填写明文密钥；服务端**只保存 AES 加密后的明文**，RC 客户端校验用的 `MD5 + Base64` 哈希改为每次校验时即时派生、不再单独存储；同一权限下已有密钥时按填写的明文**覆盖**并重设到期时间。详见 7.3。

**人机验证安全加固（Phantom v0.3.4）**
- **请求层浏览器门禁**：`/challenge`、`/verify/chunk`、`/verify` 现在要求请求来自真实浏览器（UA 是浏览器且无脚本库特征、带 `Origin` / `Accept-Language`、`Sec-Fetch-*` 与 Client Hints 自洽）。针对的是一版"完全不开浏览器"的攻击：脚本自行完成密钥协商、下载引导视频、用 OpenCV 逐帧取亮像素质心再加密回传。裸脚本在入口即被拒，并在审计日志留下 `human_browser_gate` 记录（此层靠请求头，可被伪装，须与下列措施叠加）。
- **真实来源 IP 还原**：站点经内网穿透对外服务，此前所有访客在服务端看来都是同一个回环地址，导致失败频控 / 封禁 / 注册冷却"全站共用一个桶"——真凶失败几次会把全站锁住，他一通过又清空所有人的失败计数，封禁列表永远是空的。现在从代理头解析真实来源 IP，上述限流与封禁按真凶逐个记账。
- **Δt 抖动双向把关**：原先只否决"过于均匀"的轨迹时间戳（脚本按帧率公式步进）；对手随即改成人为放大抖动来绕开。真人指针采样的抖动幅度有生理上限，因此现在对**过于杂乱**的 Δt 同样一票否决。
- **注册入口限流**：一次性 / 临时邮箱域名直接拒收，并限制单个 IP 每自然日的成功注册数（此前的注册冷却只按时间间隔拦，拦不住"隔几分钟来一次"的批量注册）。
- **合成轨迹签名**：对手为通过"自报轨迹时长不得超过服务端实测经过时间"的校验，会把伪造的 Δt 序列等比缩放到恰好等于引导视频时长，于是自报总时长与视频时长完全相等。此类样本直接拒绝（属该脚本的指纹，加抖动即可绕过，非通用防线）。
- **单帧零信号（原值覆盖合成）**：前端不再把簇层视频与噪声"取较亮者"叠加（那样簇区亮度整体偏高，截图一卡阈值就现形），改成把簇像素原值覆盖到噪声上，簇区与噪声同分布 —— 单帧截图看不出方块在哪；人眼靠"图案刚性移动、周围噪声逐帧重掷"的时域线索捕捉。为此把簇的中心与粒子偏移取整、逐帧丢弃率归零（图案不被揉碎），并给离屏取帧加了 `willReadFrequently`。实测副作用是好的：图案刚性后编码器帧间完美匹配，K=8 视频从约 1.16MB 降到约 192KB，渲染耗时 0.84s → 0.68s。
- **静止诱饵块**：单帧零信号之后，机器该改看时域了（找"逐帧取值稳定的那一小块"），于是额外撒 3 块与真方块**单帧完全同构**、但**逐帧不动**的诱饵（像素值也固定，否则"逐帧稳定"这条判据会把诱饵筛掉）。单帧看它与真方块毫无差别，分辨只能靠运动；真人跟住动的那个即可。

**人机验证安全加固（Phantom v0.3.5）**
- **起手提示段与诱饵块下沉到后端视频**：引导视频开头多出 2 秒"起手提示段"——在起点（多条候选共享的首控制点）渲染一块整块黑↔白翻转的方块（亮 255 / 暗 24，1.5Hz），前端按住即从 0 秒整段播，按后端下发的 `previewSeconds` 切"提示 → 跟随"。静止诱饵块（3 块）也由后端渲染进视频、提示段与跟随段全程都有。前端因此删掉了"自己画预览方块""自己撒诱饵""从视频首帧反推起点"三套逻辑，只保留「实时噪声 + 视频前景原值覆盖」。附带：`videoDurationMs` 改为表示视频总时长（提示段 + 跟随段）。代价：提示段几乎不占体积，诱饵是散落的孤立像素、编码器压不动，K=8 时体积约 +65%（`PHANTOM_VIDEO_DECOY_COUNT` 可下调）。

**人机验证安全加固（Phantom v0.3.3 / v0.3.2 / v0.3.1）**
- **实时流式上报（v0.3.3）**：前端从引导视频开始播放起，就按固定间隔把**新增的轨迹点**加密上报给后端，后端记录每一批**到达服务器的真实时刻**；松手提交完整轨迹时，后端校验"轨迹时长与真实到达跨度相符"且"实时流内容确是提交轨迹的一段（保序匹配）"。由此，"抓包拿视频 → 离线慢慢抠轨迹 → 空等几秒再整体回放"不再可行，**离线图像分析被挡在门外**，攻击者必须做实时识别。诚实边界：批次内坐标仍由客户端产生、可伪造，可信的只是"到达时刻"与"流和提交流同源"两点，本质是抬高成本而非根治。
- 轨迹"标准答案"（贝塞尔控制点）与其派生种子**始终只存在于服务器**，不下发到任何客户端。
- **多候选轨迹（v0.3.2）**：引导视频里**同时渲染多条候选路径**的光点（默认 8 条），跟随**任意一条**都算通过（评分对这些候选取最高分）。此前视频把正确答案以光点形式画在纯黑背景上，机器可逐帧取质心直接抠出轨迹——只要人眼看得见就挡不住提取；改为多候选后，"抠出来"不再等于"答对"，攻击者必须在「单题最多 3 次提交 + 连续失败封禁」下盲猜哪一条会被评分。
- 轨迹派生改为**密钥化派生**（v0.3.1）：`path_seed = HMAC(服务端私钥, 一次性随机数 ‖ challengeId)`。即使有人拿到了完整的服务端源码并逐字复刻派生算法，缺少只存在于服务器进程内的私钥，也无法在本地算出标准答案（**算法可复现 ≠ 答案可复现**）；一次性随机数保证同一题重复派生也不会得到同一条轨迹。
- 提交**时效校验改用服务器时钟**：以"发题 → 提交"的服务端实测经过时间为基准，要求客户端自报轨迹时长不超过该时间加一个小容差。此前基于客户端绝对时间戳的判定可被"把本机时间调成与服务器一致"精确绕过，同时会误伤系统时间不准的真人，现已移除。
- 生产环境**不再开启调试日志**（`PHANTOM_DEBUG`）：该开关会打印提交轨迹与全部评分明细，等于把调参信号交给攻击者。

**RC 文件加密服务上线**
- RC 新增两种文件加密方式：**联网密钥**（服务器实时下发密钥、本地加密）与**云加密**（服务器加密后回传 `.rce`，单文件上限 100MB，服务器不留存文件内容）。
- 加密算法与 RC 原有实现保持一致：AES-256-CBC（PKCS7），1MB 分块；同时生成 `.key` 密钥文件（恢复密钥 ID + 恢复密钥）。
- 配额按身份分级：普通 15 / VIP 50 / 1–3 级管理员 25 / 4 级管理员不限。**云加密按自然月（北京时间）重置**，且因服务端同样托管恢复密钥会**同时占 1 个托管额度**；**托管额度为存量配额（联网密钥 + 云加密共用）**，删除某文件的密钥记录即释放一个额度（云加密已消耗的月度次数不返还）。
- RC 内新增「我的文件密钥」入口，可在网页端查看已加密文件记录、配额用量、恢复密钥，并删除不再需要的记录。
- 账号注销时会同步清理该账号的加密托管数据与用量记录。

**爱发电自动发货 + RC 密钥方案上线**
- 新增两个爱发电自助方案：**RC普通版密钥独立方案（￥27.00/月，权限 Or）** 与 **RC专业版密钥（￥56.00/月，权限 Pr）**；原有的 **ZIYIT账号VIP独立方案（￥56.00/月）** 继续对应 VIP。三个方案下单时都需按 `用户ID/用户名` 填写备注。
- **自动发货**：后端接收爱发电 Webhook 通知，并支持管理员「扫订单补发 / 查单补发」；付款后立即为对应账号开通 —— VIP 写入会员到期时间，RC 方案按方案发放对应权限的许可证密钥。有效期 = 赞助月数 × 30 天（可用 `AFDIAN_DAYS_PER_MONTH` 调整）。
- **RC 密钥存储**：服务器只保存 **AES（Fernet）加密后的明文密钥**（用于回显），校验用的 `base64(md5(明文))` 哈希按需即时派生、不单独落盘，明文本身也不落盘。
- **续期规则**：同一权限下已有 RC 密钥的账号续费时**在原密钥上叠加有效期**，不重复发放新密钥。
- **用户自助查询**：新增「我的 RC 密钥」页面（`user/rc-key.html`）与 `GET /rc/keys/mine`，登录后可查看自己密钥的**明文**、权限、激活 / 到期时间与来源；全站顶部导航与用户中心均已加入口。
- **管理端**：新增「RC 密钥」与「爱发电订单」两个后台区块（Lv.3+）。「RC 密钥」可查看所有用户的明文密钥并发放 / 续期，支持「回填历史明文密钥」为早期只存哈希的密钥补录明文；「爱发电订单」可查看订单台账、扫单补发与按订单号补发。

### 2026-08 更新

**在线客服升级为智能 Agent**
- AI 客服具备自主决策能力，可主动结束对话，对严重违规对话可采取有限封禁（仅限当前对话用户、最长 7 天、无法封禁管理员）。
- AI 可按需查询网站功能说明文档来回答，并了解当前用户的基本信息。
- AI 每日问答限额按身份分级：普通 15 次 / VIP 50 次 / 1 级管理员 20 次 / 2 级 50 次 / 3 级 100 次 / 4 级不限。
- 对话结束按结束方显示不同文案（您 / AI / 人工客服 已结束本次对话）。

**封禁与申诉系统**
- 后台封禁用户与封禁 IP 支持定时（按分钟）与永久两种方式，到期自动解封。
- 封禁账号登录时显示原因与剩余时间，并提供「封禁申诉」入口。
- 新增封禁申诉通道：用户凭用户名+密码发起申诉，与人工客服实时对话，管理员可查看封禁信息并解封。

**巨硬翻译器升级**
- 翻译由网站服务器统一处理，无需浏览器端额外配置，安全性提升。
- 每日限额：普通 10 次 / VIP 25 次，跨天清零。

**Backrooms 层级管理系统**
- 新增层级提交、审核、动态列表功能。
- 文档创建器改为登录后使用。
- 提交层级需填写层级 ID 与层级名称（如 Level-706 / 回忆）。
- 两级审核：基础审核（管理员用自有 AI 初审）+ 高级审核（管理员人工审核）。
- 管理员审核支持在线查看与下载稿件；作者可查看自己所有状态的稿件。
- 静态层级列表与实时更新的动态层级合并展示，已收录层级会实时出现在列表中。

**用户系统优化**
- 修复登录封禁提示显示 [object Object] 的问题。
- 用户类型显示优化，VIP 与管理员身份明确区分。

**创作器功能增强**
- 自定义区块标题对齐优化。
- 自定义区块支持多段落。
- 生存难度默认 1 级，死区/宜居等特殊等级名称均可修改。
- 管理员页面在线人数实时显示（进入页面立即刷新 + 每 30 秒更新）。

---

*本文档由 ZIYIT STUDIO 维护，面向网站用户。如有疑问或建议，请联系 ziyitstudio@qq.com。*

---

## 二十二、详细操作手册

> 本章节为各功能提供逐步操作详解，适合第一次使用的用户按步骤操作。

### 22.1 注册与登录（新用户必看）

**第一步：进入登录页**
1. 打开网站首页，点击右上角「登录」按钮。
2. 登录页上方是登录表单，下方有"没有账号？点这里注册"链接。

**第二步：注册账号**
1. 点击"没有账号？点这里注册"。
2. 填写用户名：建议 2-16 个字符，中英文均可，作为登录账号与展示名称。
3. 填写邮箱：必须是真实邮箱，用于找回密码与账号管理。
4. 填写密码：建议 8 位以上，包含字母与数字。
5. 点击注册按钮，完成注册。

**第三步：登录**
1. 回到登录页，输入用户名与密码。
2. 如需长期保持登录，勾选「记住我」。
3. 点击登录按钮，成功后会跳转回首页。

**第四步：确认登录状态**
1. 登录后右上角菜单会变化，出现"用户中心"相关内容与你的用户名。
2. 如果不确定是否登录，点击任意需要登录的功能（如音乐下载）测试。

> 提示：想快速体验登录后的效果，可以使用临时体验账号 cache / temp（无需注册，约 3.75 分钟有效）。

### 22.2 开通 VIP（详细流程）

1. 登录账号。
2. 进入「用户中心 → 升级 VIP 会员」，页面会显示你的用户 ID、用户名、当前用户类型。
3. 点击「前往爱发电购买 VIP（￥56.00/月）」跳转到爱发电。
4. 在爱发电选择方案「ZIYIT账号VIP独立方案」与赞助期限（1 个月起，支持多个月，期限越长折扣越高）。
5. 下单时在备注里填写：`用户ID/用户名`（用户 ID 见第 2 步）。**填错会发货到别人的账号。**
6. 完成支付，后端收到订单通知后自动开通 VIP，有效期 = 赞助月数 × 30 天。
7. 回到「用户中心 → 升级 VIP 会员」，点击「我已购买，立即查询」确认到账；刷新页面即可看到用户类型变为 VIP。

> 补充：
> - 已是 VIP 再次购买会在原到期时间上**叠加**，不会覆盖。
> - 若填错用户 ID 或长时间未到账，可通过邮件 `ziyitstudio@qq.com` 联系站长人工处理。
> - 购买「RC 普通版 / 专业版密钥」的订单**不会**开通 VIP，密钥请到「用户中心 → 我的 RC 密钥」查看。
> - 购买「DLC 扩展包」的订单**不会**开通 VIP、也不发 RC 密钥，扩展包请在 RC 软件顶部菜单「**DLC → 管理 DLC**」中查看（购买后永久有效，另附赠基础包 CoreToolkit）。DLC 订单备注填的是 `用户ID/扩展包ID`。

### 22.3 在线听歌（完整步骤）

1. 从首页「功能区」点击「音乐」进入音乐页面。
2. 页面会自动加载播放列表，列表按曲目名称排列。
3. 点击任意曲目开始播放：
   - 播放器会出现在页面固定区域（或列表上方）。
   - 使用播放/暂停按钮控制播放。
   - 使用上一曲/下一曲切换曲目。
4. 拖动进度条可以快进/快退，右侧显示当前时间与总时长。
5. 播放期间打开歌词面板，歌词会随播放自动滚动，当前行高亮；点击任意歌词行可跳转播放。
6. 遇到 VIP 曲目：
   - 未开通 VIP 时无法播放/下载，会提示权限不足。
   - 开通 VIP 后刷新页面即可解锁。
7. 下载：曲目详情或列表旁的下载按钮，按权限显示；点击后直接下载文件。

> 小贴士：播放器支持视频类曲目与 B 站视频，播放时会自动切换为视频模式。

### 22.4 下载资源（完整步骤）

1. 从首页「功能区」点击「下载」进入下载中心。
2. 页面以卡片形式展示资源，卡片上有资源名称、简介、权限标签（普通/免费/VIP）。
3. 顶部搜索框输入关键词，可以过滤资源列表。
4. 点击资源卡片查看详情页（或展开详情）。
5. 点击「下载」按钮：
   - 普通资源：直接下载。
   - 免费资源：需要登录后下载（未登录会提示先登录）。
   - VIP 资源：需要 VIP 身份（普通用户按钮不可用或提示升级）。
   - DLC（扩展包）资源：需要**已拥有**该扩展包（爱发电购买后自动到账、永久有效），未拥有时提示「您未拥有该 MOD/DLC」，详见 7.4。
6. 下载完成后，建议校验文件完整性（如资源提供校验值）。

### 22.5 使用巨硬翻译器（完整步骤）

1. 从首页「功能区」点击「巨硬翻译」进入翻译器页面。
2. 页面先进行人机验证（通常无感完成），验证通过后显示翻译界面。
3. 在上方文本框中输入要翻译的内容（不超过 2000 字）。
4. 选择目标语言：
   - 从下拉列表选择常用语言（中文、英文等）。
   - 如果需要其他语言，在"自定义语言代码"框中填写代码（如 `ja`、`ko`、`fr`）。
5. 点击「翻译」按钮，等待翻译结果。
6. 结果会输出到下方结果区域，点击复制按钮即可复制。
7. 查看剩余次数：页面会显示今日剩余次数，用完当天次数需等到次日重置，或升级 VIP（每天 25 次）。

### 22.6 使用 Backrooms 文档创建器（作者详细流程）

**前提**：已登录账号。

1. 从 Backrooms 维基首页或导航进入「文档创建器」。
2. 页面顶部是工具栏，左侧（或上方）是编辑面板，右侧（或下方）是实时预览。
3. **选择生存难度**：
   - 打开生存难度下拉框，选择模板（默认 1 级）。
   - 可选：0-5 级、死区、宜居、未分级、修正、终结、不适用、等待分级、自定义。
   - 在等级名称输入框中可以修改等级名称（如把"死区"改为"天堂"），预览即时更新。
   - 选择"自定义"时，还可以设置自定义颜色。
4. **填写标题**：在标题输入框中填写层级标题（如"霓虹医院"）。
5. **编辑描述**：
   - 在描述区块输入正文内容。
   - 需要多段时，点击「添加段落」按钮新增段落。
6. **编辑其他区块**：
   - 实体、前哨、入口、出口等区块各自独立编辑。
   - 每个区块支持多段落。
7. **添加自定义区块**：
   - 点击「添加自定义区块」。
   - 填写区块标题（如"发现记录"）。
   - 输入该区块内容，支持多段落。
   - 可以添加多个自定义区块。
8. **插入图片**：
   - 点击图片库按钮，选择已上传的图片。
   - 或上传新图片（需登录）。
9. **预览检查**：
   - 实时预览会随编辑更新，检查格式与效果。
   - 点击预览中的可编辑元素可直接修改内容。
10. **导出**：
   - 编辑完成后点击「导出 HTML」。
   - 浏览器会下载一个 `.html` 文件。
   - 用浏览器打开该文件可以查看最终效果。
11. **提交投稿**（登录后即可，见 22.7）。

> 提示：加载模板功能可以把现有层级作为起点快速创建；上传 HTML 功能可以把已有文件解析回编辑器继续编辑。

### 22.7 提交层级投稿（作者详细流程）

**前提**：已登录（**无需后室成员身份**）。

1. 从导航进入「层级提交」页面。
2. **填写层级 ID**：格式为 `Level-` + 编号。
   - 数字层级：如 `Level-706`。
   - 特殊层级：如 `Level-!+`、`Level-NO-DATA`。
3. **填写层级名称**：例如"回忆"、"霓虹医院"。
4. **选择文件**：点击选择已导出的 HTML 文件（≤1MB）。
5. 点击「提交」：
   - 成功：稿件进入"待审核"状态。
   - 提示 ID 已存在：该层级 ID 已被占用，换个 ID 或联系管理员。
   - 提示超出限制：超过每月提交上限或拥有层级上限。
6. 提交后在「我的层级」列表中查看状态。

**修改稿件**：
1. 在「我的层级」找到对应稿件，点击「修改」。
2. 重新选择文件上传。
3. 提交后生成新版本，旧版本自动保留。

**删除稿件**：
1. 在「我的层级」找到对应稿件，点击「删除」。
2. 确认后稿件删除。

### 22.8 使用在线客服（完整步骤）

**AI 客服**：
1. 从导航进入「在线客服」页面。
2. 页面显示顶部状态徽章（AI 助手）、今日剩余次数、消息区与输入区。
3. 在输入框输入问题，按回车（或点击发送）发送。
4. AI 会即时回复，消息显示在消息区。
5. 输入「转人工」可以切换到人工客服（见下）。

**转人工**：
1. 输入「转人工」。
2. 弹出确认提示，点击确认。
3. 状态徽章变为"等待人工客服接入"。
4. 客服接入后，徽章显示"人工客服·姓名"，消息由人工回复。

**结束对话**：
1. 点击「结束对话」按钮。
2. 对话结束，状态徽章显示"对话已结束"，输入区禁用。

### 22.9 发起封禁申诉（完整步骤）

1. 在登录页面，如果账号被封禁，会显示封禁原因与剩余时间。
2. 点击「封禁申诉」链接，进入申诉页面。
3. 输入你的**用户名和密码**（用于验证身份）。
4. 点击提交，成功后自动进入申诉对话页。
5. 在对话框中描述你的申诉理由，例如：
   - 说明被误封的情况。
   - 提供相关证据或解释。
6. 人工客服接入后与你对话，处理结果会直接反映到账号状态。
7. 客服认可申诉后解除封禁，退出页面重新登录即可。

### 22.10 管理员处理层级审核（完整步骤）

**基础审核（所有管理员均可）**：
1. 从后台管理进入「层级审核」页面。
2. 在"待基础审核"列表查看稿件（ID、作者、大小）。
3. 点击「查看」在新标签页查看稿件内容；点击「下载」保存稿件文件。
4. 点击「生成提示词」复制审核提示词。
5. 将提示词与下载的稿件文件一起交给你的 AI 进行初审。
6. 把 AI 的完整回复粘贴到"提交审核结果"文本框。
7. 如果 AI 结论为通过，勾选"直接提交高级审核"后提交；未通过则直接提交，系统会按理由列表通知作者。
8. 提交后稿件移出待审核列表，状态更新。

**高级审核（Lv.3+ 管理员）**：
1. 在"待高级审核"列表查看稿件。
2. 点击「查看」阅读内容，点击「下载」保存。
3. 点击「通过」收录层级。
4. 或点击「拒绝」并填写理由提交。
5. 收录后层级会出现在公开列表。

**管理已收录**：
1. 在"已收录"列表查看所有已收录层级。
2. 「重写」：下架并保留原文件，供作者重写。
3. 「删除」：管理员直接删除该层级。

### 22.11 管理员处理封禁申诉（完整步骤）

1. 从后台管理进入「在线客服」。
2. 在待接入会话队列中，带"封禁申诉"标识的会话即为申诉会话。
3. 点击「接受」接入会话。
4. 消息区顶部会显示**封禁信息卡片**：
   - 用户 ID
   - 封禁理由
   - 剩余封禁时间（"永久封禁"或"剩余 N 天 / 解封时间 xxx"）
5. 与申诉者对话，了解情况。
6. 如认可申诉：进入用户管理，找到该用户，点击「解封」。
7. 如不认可：结束对话，申诉者保留封禁状态。

### 22.12 管理员进行封禁操作（完整步骤）

**封禁用户**：
1. 进入后台「用户管理」。
2. 搜索找到目标用户。
3. 点击「封禁」。
4. 弹窗中填写封禁时长（分钟）：
   - 填写数字：定时封禁，到期自动解封。
   - 留空：永久封禁。
5. 确认后封禁生效，用户下次登录会看到封禁提示。

**封禁 IP**：
1. 进入后台「IP 封禁」区块。
2. 输入要封禁的 IP 地址。
3. 填写封禁时长（分钟，留空为永久）。
4. 确认后该 IP 下的访问受到限制。

**解封**：
1. 在用户列表或 IP 封禁列表找到对应记录。
2. 点击「解封」，立即解除。

---

## 二十三、Backrooms 文档创作指南

> 本章面向想要创作 Backrooms 层级文档的作者，讲解创作规范、生存难度含义与写作建议。

### 23.1 一篇合格层级文档的结构

标准的层级文档通常包含以下部分：

| 区块 | 是否必填 | 说明 |
| --- | --- | --- |
| 标题 | 必填 | 层级的正式名称，如"霓虹医院" |
| 生存难度 | 必填 | 用等级与属性表达该层级的危险程度 |
| 描述 | 必填 | 层级的主体介绍：环境、外观、物理特性 |
| 实体 | 常见 | 该层级中出没的实体列表与说明 |
| 前哨与基地 | 可选 | 该层级中的社区、组织、前哨站 |
| 入口 | 必填 | 如何从其他层级/现实进入该层级 |
| 出口 | 必填 | 如何离开该层级前往其他层级 |
| 附录/发现记录 | 可选 | 补充文档、日志、实验记录等 |

### 23.2 生存难度等级详解

生存难度是层级最重要的标识之一，位于文档顶部，包含**等级名称**与**属性列表**（实体、出口等）。

| 等级 | 含义 | 典型特征 |
| --- | --- | --- |
| 0 级 | 安全且稳定 | 无实体或极少数实体，适合长期居住 |
| 1 级 | 安全 | 基本安全，可能有个别实体，可正常探索 |
| 2 级 | 不安全 | 存在一定危险，实体数量中等，需谨慎 |
| 3 级 | 不安全 | 较高危险，实体较多，不适合久留 |
| 4 级 | 不安全 | 高度危险，实体数量多，生存率低 |
| 5 级 | 生存难度极高 | 几乎无法生存，必须尽快离开 |
| 死区 | 生存难度极高 | 无法存活，进入即面临死亡威胁 |
| 宜居 | 适合居住 | 资源充足，适合长期定居 |
| 未分级 | 未确定 | 属性未测定或无法测定 |
| 修正 | 已修正 | 层级被人工改造或修正过 |
| 终结 | 已终结 | 层级被摧毁或不再存在 |
| 不适用 | 无法分级 | 特殊层级，无法用常规标准分级 |
| 等待分级 | 待测定 | 正在测定中 |

**修改等级名称**：在创建器中，等级名称可以自由修改。例如可以把"死区"改为更贴合文档氛围的名称（如"天堂"），同时保留死区的配色与风格。

### 23.3 属性（指标）说明

生存难度下方通常有属性列表，常见的属性包括：

| 属性 | 说明 | 常见取值 |
| --- | --- | --- |
| 实体数量 | 该层级的实体密度 | 无、极少、极少至中等、较多、极多等 |
| 出口难度 | 离开该层级的难度 | 极易、中等、困难、极难等 |
| 环境风险 | 环境本身的危险程度 | 无、低、中、高、极高 |
| 其他指标 | 自定义指标 | 按文档需要添加 |

属性同样可以在创建器中逐项修改。

### 23.4 写作建议

**内容质量**：
- 原创优先：不要直接复制他人设定，可以借鉴风格但要有自己的创意。
- 设定自洽：层级规则要前后一致，避免自相矛盾。
- 细节丰富：通过环境、声音、气味、光线等细节营造氛围。
- 叙事与说明结合：既要有客观描述，也可以加入日志、录音等叙事元素。

**格式规范**：
- 使用创建器导出的标准结构，不要随意删改必需区块。
- 标题、生存难度、描述、入口、出口齐全。
- 段落分明，每个区块一个或多个段落。
- 图片使用规范：图片清晰、无水印、来源合规。

**常见退稿原因**（基础审核不通过的理由通常按编号列出）：
1. 内容过短或过于敷衍，缺乏实质内容。
2. 设定抄袭或与既有知名文档高度雷同。
3. 格式不完整（缺入口/出口等必填区块）。
4. 存在违规内容（涉及真实攻击、违法违规等）。
5. 文字表达混乱、错别字多、语病明显。
6. 图片违规或无法正常显示。

### 23.5 从投稿到收录的完整生命周期

1. **草稿期**：使用创建器编写、预览、导出。
2. **提交期**：填写层级 ID 与名称，上传 HTML。
3. **待审核**：稿件排队等待基础审核。
4. **基础审核**：管理员用自有 AI 初审，通过/不通过。
5. **高级审核**：管理员人工复审，通过/拒绝。
6. **已收录**：层级出现在公开列表，所有人可访问。
7. **后续**：可修改更新版本；管理员可对已收录层级执行重写或删除。

### 23.6 层级 ID 命名规范

| 类型 | 格式 | 示例 |
| --- | --- | --- |
| 常规数字层级 | Level- + 数字 | Level-706 |
| 负数层级 | Level- + 负号 + 数字 | Level--1（负一级） |
| 特殊符号层级 | Level- + 符号 | Level-!+、Level-?、Level-√ |
| 特殊命名层级 | Level- + 字母/文字 | Level-NO-DATA、Level-End |
| 子层级 | 在母层级下细分 | 由管理员统一规划 |

> 注意：层级 ID 是唯一标识，提交时不可与已有 ID 重复。

---

## 二十四、场景化 FAQ（按使用场景索引）

### 场景一：我忘了密码

1. 打开登录页，点击"忘记密码"入口。
2. 使用注册邮箱进行找回（如果注册时填写了邮箱）。
3. 如果邮箱也无法找回，联系站长（ziyitstudio@qq.com）提供账号信息协助处理。

### 场景二：我想听 VIP 音乐

1. 先开通 VIP（见"详细操作手册 → 开通 VIP"）。
2. 开通后刷新音乐页面。
3. 播放 VIP 曲目不再提示无权限，可正常播放与下载。

### 场景三：我被封禁了，想申诉

1. 在登录页查看封禁原因。
2. 点击「封禁申诉」。
3. 输入用户名密码验证身份。
4. 与人工客服对话说明情况。
5. 等待客服核实并处理（认可后解封）。

### 场景四：我想投稿一个层级

1. 确认已登录（投稿无需后室成员身份）。
2. 登录网站。
3. 使用文档创建器完成文档并导出 HTML。
4. 进入「层级提交」，填写层级 ID、名称，上传文件。
5. 在「我的层级」查看审核进度。

### 场景五：我的稿件被拒了

1. 打开「我的层级」查看被拒稿件的状态。
2. 查看拒绝理由（按 1. 2. 3. 编号列出）。
3. 按理由逐条修改文档。
4. 重新提交（使用修改功能上传新版本）。

### 场景六：我想联系人工客服

1. 打开在线客服页面。
2. 输入「转人工」。
3. 确认后等待客服接入。
4. 与客服对话。

### 场景七：客服 AI 不理我/主动结束了对话

1. 检查消息区提示：AI 可能判定对话结束或话题无关。
2. 重新发起对话，尽量让问题与网站相关、表述清晰。
3. 如果频繁被 AI 结束对话，尝试转人工客服。

### 场景八：管理员想审核稿件

1. 进入后台「层级审核」。
2. 在待基础审核列表处理（基础审核，所有管理员均可）。
3. 在待高级审核列表处理（高级审核，Lv.3+ 管理员）。
4. 使用查看/下载/生成提示词/提交结果等操作完成审核流程。

### 场景九：管理员想封一个用户

1. 进入后台「用户管理」。
2. 搜索用户。
3. 点击封禁，填写时长（分钟，留空=永久）。
4. 确认。用户下次登录会看到封禁提示。

### 场景十：我想知道网站都有什么功能

1. 浏览首页的功能区与图书馆菜单。
2. 查看本手册目录，了解所有模块。
3. 使用站内搜索快速查找。

### 场景十一：翻译一次不过瘾/次数不够

1. 普通用户每天 10 次，VIP 25 次。
2. 次数用完可等次日重置。
3. 或开通 VIP 提升额度。

### 场景十二：手机上看网站不好操作

1. 移动端使用汉堡菜单展开导航。
2. 音乐、客服、翻译等核心功能在手机端均可正常使用。
3. 建议在暗色模式下夜间使用，更护眼。

### 场景十三：账号疑似被盗

1. 立即修改密码。
2. 检查个人资料是否有异常修改。
3. 联系站长说明情况，必要时申请冻结账号。

---

## 二十五、功能规则与参数详解

> 本章汇总网站各功能的规则、限额与参数，方便用户与管理查阅。

### 25.1 每日限额汇总

| 功能 | 普通用户 | VIP 用户 | 1 级管理员 | 2 级管理员 | 3 级管理员 | 4 级（站长） |
| --- | --- | --- | --- | --- | --- | --- |
| 在线客服 AI 问答 | 15 次/天 | 50 次/天 | 20 次/天 | 50 次/天 | 100 次/天 | 不限 |
| 巨硬翻译器 | 10 次/天 | 25 次/天 | 同普通 | 同普通 | 同普通 | 同普通 |
| 在线客服短时限流 | 每分钟有限额 | 每分钟有限额 | 每分钟有限额 | 每分钟有限额 | 每分钟有限额 | 每分钟有限额 |
| 管理员私聊 | 不适用 | 不适用 | 每分钟有限额 | 每分钟有限额 | 每分钟有限额 | 每分钟有限额 |

**通用规则**：
- 每日限额按天计算，跨天自动清零。
- 短时限流按分钟计算，超限后需等待片刻。
- 管理员身份优先于 VIP 身份判定（管理员按管理员档位计算）。

### 25.2 管理员权限矩阵

| 功能区块 | Lv.1 | Lv.2 | Lv.3 | Lv.4 |
| --- | --- | --- | --- | --- |
| 音乐管理（播放控制、曲库） | ✅ | ✅ | ✅ | ✅ |
| 管理员管理（列表/添加/编辑/撤销） | ✅ | ✅ | ✅ | ✅ |
| 管理员私聊 | ✅ | ✅ | ✅ | ✅ |
| 在线客服（队列/回复/结束） | ✅ | ✅ | ✅ | ✅ |
| 层级基础审核 | ✅ | ✅ | ✅ | ✅ |
| 全局消息广播 | ❌ | ✅ | ✅ | ✅ |
| 后室成员管理 | ❌ | ✅ | ✅ | ✅ |
| 层级高级审核 | ❌ | ❌ | ✅ | ✅ |
| 已收录层级重写/删除 | ❌ | ✅ | ✅ | ✅ |
| API Key 管理 | ❌ | ❌ | ✅ | ✅ |
| MOD / DLC 管理（含单用户扩展包发放/撤销） | ❌ | ❌ | ✅ | ✅ |
| RC 软件密钥管理 | ❌ | ❌ | ✅ | ✅ |
| 爱发电订单（台账 / 扫单补发 / 查单补发） | ❌ | ❌ | ✅ | ✅ |
| 日志查看 | ❌ | ❌ | ✅ | ✅ |
| 用户管理（搜索/升级/封禁） | ❌ | ❌ | ❌ | ✅ |
| IP 封禁管理 | ❌ | ❌ | ❌ | ✅ |
| 客服名单管理 | ❌ | ❌ | ❌ | ✅ |

### 25.3 封禁规则说明

| 规则项 | 说明 |
| --- | --- |
| 封禁对象 | 用户账号、IP 地址 |
| 封禁方式 | 定时封禁（按分钟）/ 永久封禁 |
| 定时到期 | 自动解封，无需人工操作 |
| 用户侧提示 | 登录时显示封禁原因、剩余天数、预计解封时间 |
| 申诉通道 | 登录页「封禁申诉」→ 用户名+密码验证 → 对话申诉 |
| AI 自动封禁 | 对话严重违规时触发，仅限当前对话用户，最长 7 天，管理员不可被 AI 封禁 |
| 黑名单 | 与封禁不同：黑名单账号可登录但仅保留病毒库入口 |

### 25.4 层级稿件状态流转

一份层级稿件从提交到收录/未收录的完整流转如下：

**主流程（正向通过）**：提交 → 待审核 → 基础审核通过 → 待高级审核 → 高级审核通过 → **收录（公开可见）**。

**未通过分支**：
- 基础审核未通过 → 状态为"未过基础审核"，作者可修改后重新提交，回到"待审核"。
- 高级审核未通过 → 状态为"未收录"，作者可修改后重新提交。

**收录之后**：
- 作者可更新版本（旧版本自动保留）。
- 管理员可执行"重写"（下架稿件但保留原文件，列表显示重写标记）。
- 管理员可执行"删除"（彻底移除）。

| 状态 | 作者可见 | 公开可见 | 管理员可见 |
| --- | --- | --- | --- |
| 待审核 | ✅ | ❌ | ✅ |
| 已过基础审核 | ✅ | ❌ | ✅ |
| 未过基础审核 | ✅ | ❌ | ✅ |
| 高级审核中 | ✅ | ❌ | ✅ |
| 已收录 | ✅ | ✅ | ✅ |
| 未收录 | ✅ | ❌ | ✅ |
| 已重写 | ✅ | ❌（原文件保留） | ✅ |

### 25.5 在线客服消息类型

对话中的消息分为几种类型，页面以不同样式展示：

| 消息类型 | 显示样式 | 说明 |
| --- | --- | --- |
| AI 回复 | 默认气泡 | AI 助手的回答 |
| 系统提示 | 居中灰字 | 转接成功、对话结束等系统通知 |
| 用户消息 | 右侧气泡 | 自己发送的内容 |
| 人工客服消息 | 客服气泡（带徽标） | 人工客服的回复 |

**结束对话文案**：
- 用户主动结束：「您已结束本次对话」
- AI 主动结束：「AI 已结束本次对话」
- 人工客服结束：「人工客服已结束本次对话」
- 申诉会话结束：「本次申诉已结束」

### 25.6 翻译器规则

| 规则项 | 说明 |
| --- | --- |
| 输入上限 | 2000 字/次 |
| 超时 | 30 秒，超时提示重试 |
| 计次规则 | 仅翻译成功计次，失败不计 |
| 语言选择 | 下拉常用语言 + 自定义语言代码 |
| 前置条件 | 先完成人机验证 |
| 登录要求 | 需要登录（使用每日额度） |

### 25.7 文档创建器规则

| 规则项 | 说明 |
| --- | --- |
| 登录要求 | 必须登录才能使用 |
| 生存难度默认 | 1 级 |
| 等级名称 | 所有预设（含死区/宜居等）均可修改 |
| 自定义颜色 | 自定义预设可设置专属颜色 |
| 区块段落 | 描述/前哨/入口/出口/自定义区块均支持多段落 |
| 自定义区块 | 可添加多个，支持多段落 |
| 模板加载 | 支持加载现有层级模板 |
| HTML 上传 | 支持解析已有 HTML 回编辑器 |
| 导出 | 导出完整 .html 文件 |
| 图片 | 图片库 + 上传（需登录） |
| 评分 | 创建/绑定评分（需登录） |

### 25.8 层级投稿规则

| 规则项 | 说明 |
| --- | --- |
| 身份要求 | 登录即可（**无需后室成员身份**） |
| 登录要求 | 必须登录 |
| 层级 ID 格式 | Level- + 编号（唯一） |
| 层级名称 | 任意字符串（必填） |
| 文件大小 | ≤1MB |
| 拥有上限 | 每人拥有层级数量有上限 |
| 提交频率 | 每月提交次数有上限 |
| 版本 | 修改生成新版本，旧版本保留 |

### 25.9 导航菜单完整清单

| 菜单 | 子项 | 可见性 |
| --- | --- | --- |
| 功能区 | 下载 / 更多 / 病毒库 / 搜索 / 音乐 / 巨硬翻译 / 人机验证 | 全员 |
| 开发者选项 | 日志 / 常部署时间 / 后台管理 / 开发者工具 | 登录后，按角色 |
| 图书馆 | Mod Wiki / Backrooms Wiki 及子分类 | 全员 |
| 用户中心 | 登录 / 注册 / 资料 / VIP / 公告 | 全员（登录后变化） |
| 语言 | 站点语言切换 | 全员 |

### 25.10 登录与安全规则

| 规则项 | 说明 |
| --- | --- |
| 登录限流 | 同一 IP 5 分钟内最多 1 次登录 |
| 临时账号 | cache / temp，约 3.75 分钟 |
| 记住我 | 可选，保持长期登录 |
| 反调试 | 网站包含反调试检测，开开发者工具可能触发拦截 |
| 人机验证 | 翻译器等敏感功能前置 Phantom 验证 |

### 25.11 爱发电商品与备注格式

网站的三类付费商品都在爱发电自助购买，付款后由后端自动发货。**备注格式互不相同，填错会发货到错误账号或无法发货**：

| 商品 | 判定依据 | 备注格式 | 有效期 |
| --- | --- | --- | --- |
| VIP 会员 | VIP 方案 ID（`AFDIAN_PLAN_IDS`） | `用户ID/用户名` | 赞助月数 × 30 天 |
| RC 许可证密钥 | RC 方案 ID（`AFDIAN_RC_STANDARD_PLAN_ID` / `AFDIAN_RC_PRO_PLAN_ID`） | `用户ID/用户名` | 赞助月数 × 30 天 |
| DLC 扩展包 | 扩展包商品 ID（`AFDIAN_DLC_PRODUCT_IDS`，默认 `3f7d2856b3be11f1a6825254001e7c00`） | `用户ID/扩展包ID` | **永久**（另附赠基础包 `modId 1`） |

**判定优先级**：DLC 扩展包 > RC 密钥 > VIP。同一订单只命中一类，不会同时发放。

**备注解析规则**：

- 第一段必须是**纯数字的用户 ID**（注册后可在用户中心查看），与账号 `userId` 一一对应。
- 分隔符用 `/` 或全角 `／` 均可，两侧空白会被忽略。
- 第二段对 VIP / RC 是**用户名**（仅用于核对，与账号用户名不一致时台账会标记 `remarkUsernameMismatch`，但**不阻断发货**）。
- 第二段对 DLC 是**扩展包ID**（编号或包名），不参与用户名核对；解析不出即只记账不发货。

**发货相关参数**

| 参数（环境变量） | 默认 | 说明 |
| --- | --- | --- |
| `AFDIAN_DAYS_PER_MONTH` | 30 | VIP / RC 密钥每月折算的天数（DLC 不受影响，恒为永久） |
| `AFDIAN_SELF_CHECK_COOLDOWN` | 60 | 用户自助查单的冷却秒数 |
| `AFDIAN_SCAN_PAGES` | 3 | 扫单补发时向爱发电拉取的订单页数 |
| `AFDIAN_DLC_PRODUCT_IDS` | 内置扩展包商品 ID | 扩展包商品 / 方案 ID 白名单，逗号分隔可追加多个 |

**发货与补发入口**

| 途径 | 说明 |
| --- | --- |
| Webhook 通知 | 爱发电付款后自动推送，服务端只取订单号再向官方**反查**确认已付款（`status == 2`）才入账 |
| 后台「扫订单补发」 | 管理员主动拉取最近订单补发，用于通知漏收 |
| 后台「查单并补发」 | 按订单号查询并补发 |
| 用户中心「查询我的订单」 | 用户自助查单补发（有冷却） |

**幂等**：以爱发电订单号为准，同一订单**只入账一次**；重复推送 / 重复扫单不会重复发放。

---

## 二十六、Backrooms 层级写作进阶教程

> 本章为想要创作高质量层级的作者提供进阶写作方法。内容均基于后室创作通用规范，与网站功能无关。

### 26.1 如何构思一个好层级

**从"一个念头"开始**：
- 先想一个核心设定：一个词语、一种氛围、一个画面。例如"永远黄昏的废弃机场"、"只有电梯的迷宫"、"不断重复的午夜车厢"。
- 把设定扩展成三要素：**空间长什么样**（环境）、**有什么危险**（威胁）、**怎么进出**（入口/出口）。

**避免"为恐怖而恐怖"**：
- 好的层级不是堆砌吓人元素，而是通过合理的规则与氛围让读者自己感到不安。
- 用"确定感"制造恐惧：明确写清这里的规则（比如"灯光每 6 分钟熄灭一次"），比模糊的描述更令人信服。

**层级不是"副本"**：
- 不要直接复制知名层级（Level 0、Level ! 等）的设定。
- 可以致敬或致敬式参考，但要有足够多的原创内容。

### 26.2 环境描写技巧

**五感描写**：
- 视觉：光线、颜色、材质、视野。如"天花板的荧光灯发出惨白的光，地面瓷砖的缝隙里长满霉斑"。
- 听觉：持续声、间歇声、回声。如"远处传来管道里的滴水声，时近时远"。
- 嗅觉：潮湿、金属、焦糊。如"空气中弥漫着铁锈与消毒水混合的气味"。
- 触觉：温度、湿度、质感。如"墙壁摸起来黏腻，像是覆盖了一层薄膜"。
- 味觉：可用但不强求。

**空间感**：
- 交代层级的大小、边界与结构（是否无限、是否有房间、是否有层）。
- 用"导航线索"帮助读者理解：走廊、门、标志、地图。

**时间感**：
- 层级内部的时间是否正常？是否有日夜循环？是否永久某种光线？
- 时间异常是常见的层级设定，但要前后一致。

### 26.3 实体设计

**实体分类**：
- 敌对实体：主动攻击玩家/流浪者。
- 中立实体：不主动攻击，但可能危险或无害。
- 友好实体：可以帮助流浪者。

**设计要点**：
- 每个实体要有明确的**外观、行为、弱点、危害等级**。
- 实体数量要与层级生存难度匹配：宜居层级不应充满敌对实体。
- 知名实体（如微笑者）可以提及，但不要当作自己原创。

### 26.4 入口与出口设计

**入口**：
- 明确如何进入：无意的（"在 Level 0 中沿墙行走过久会来到这里"）或有意的（"需要集齐某种物品"）。
- 入口要与层级特点相关，不要"碰巧进入"式敷衍。

**出口**：
- 明确如何离开：常规出口、条件出口、随机出口。
- 出口难度要与生存难度属性一致。
- 可以设计"隐藏出口"作为彩蛋，但要在正文中有线索。

### 26.5 叙事与格式技巧

**多段落结构**：
- 创建器支持每个区块多段落，合理分段阅读体验更好。
- 描述区块可以用"概述 → 细节 → 特殊区域"递进。

**附录/日志**：
- 在"发现记录"或附录中加入流浪者日志、实验记录、广播片段，增加真实感。
- 日志作者可以是有名字的角色，前后呼应有惊喜。

**前后呼应**：
- 在正文中埋下的伏笔（奇怪的记号、消失的房间号），在附录里揭开。
- 但不要故弄玄虚，读者找不到线索会觉得困惑。

### 26.6 常见写作误区

| 误区 | 建议 |
| --- | --- |
| 篇幅太短（只有几句话） | 至少完整写清描述、入口、出口 |
| 设定混乱前后矛盾 | 列出层级规则清单再动笔 |
| 全篇只有威胁没有探索价值 | 给层级加入可探索的区域、资源或谜题 |
| 出口"想走就走"过于随意 | 出口要有条件、代价或方式 |
| 用大量生僻词堆砌 | 简洁清晰的文字更有力量 |
| 抄袭他人设定 | 原创是底线，宁短勿抄 |

### 26.7 创作检查清单（投稿前自检）

- [ ] 标题填写正确
- [ ] 生存难度选择并核实（名称、颜色、属性）
- [ ] 描述完整（环境、结构、氛围）
- [ ] 实体区块（如有）写明实体名称与行为
- [ ] 入口与出口都有明确交代
- [ ] 自定义区块（如有）标题与内容正确
- [ ] 图片正常显示、来源合规
- [ ] 全文无违规内容、无明显错别字
- [ ] 已导出并预览确认效果

---

## 二十七、网站提示语与解决办法

> 使用网站过程中，你可能遇到一些提示信息。下表说明每条提示的含义与解决办法（仅列用户可见的提示文案）。

### 27.1 登录相关

| 提示 | 含义 | 解决办法 |
| --- | --- | --- |
| 用户名或密码错误 | 登录信息不正确 | 核对用户名密码；或使用找回密码 |
| 登录尝试过于频繁，请稍后再试 | 登录限流触发 | 等待几分钟后重试 |
| 该账号已被封禁（含原因/剩余天数） | 账号处于封禁状态 | 等待自动解封，或点击「封禁申诉」 |
| 登录已过期 | 登录状态失效 | 重新登录 |

### 27.2 在线客服相关

| 提示 | 含义 | 解决办法 |
| --- | --- | --- |
| 今日 AI 问答次数已用完，请稍后再试 | 每日限额用尽 | 次日重置；或开通 VIP 提升额度 |
| 请联系 ziyitstudio@qq.com 升级为 VIP | 升级提示 | 邮件联系站长（价格同 RC 专业版） |
| 已为您转接人工客服（姓名） | 转人工成功 | 等待人工回复 |
| 暂无可用客服 | 当前无客服在线 | 消息会保留，等客服上线处理 |
| 您已结束本次对话 / AI 已结束 / 人工客服已结束 | 会话已结束 | 重新发起对话 |
| 本次申诉已结束 | 申诉会话结束 | 申诉结果由客服决定 |
| 转人工 | 触发转人工指令 | 输入「转人工」并确认 |

### 27.3 音乐与下载相关

| 提示 | 含义 | 解决办法 |
| --- | --- | --- |
| 无权限播放/下载 | 曲目或资源需要 VIP | 开通 VIP 后刷新 |
| 请先登录 | 需要登录后操作 | 登录后再试 |
| 资源不存在 | 资源已下架或链接失效 | 联系管理员反馈 |

### 27.4 翻译器相关

| 提示 | 含义 | 解决办法 |
| --- | --- | --- |
| 今日翻译次数已用完，请稍后再试 | 每日限额用尽 | 次日重置；或开通 VIP |
| 翻译超时 | 等待超过 30 秒 | 重试或缩短文本 |
| 请先完成人机验证 | 验证未通过 | 关闭开发者工具刷新重试 |

### 27.5 Backrooms 投稿相关

| 提示 | 含义 | 解决办法 |
| --- | --- | --- |
| 账号已被封禁，无法使用该功能 | 账号处于封禁状态 | 通过「封禁申诉」渠道联系管理员 |
| ID 已存在 | 层级 ID 被占用 | 更换 ID |
| 超出每月上限 / 拥有上限 | 投稿限额用尽 | 等下月；或删除旧稿件释放名额 |
| 文件过大 | 文件超过 1MB | 压缩后重新上传 |
| 正在审核中 | 稿件进入审核 | 等待审核结果 |
| 无法访问 | 层级未收录或已下架 | 公开只显示已收录层级 |

---

## 二十八、管理员日常运营指南

> 本章面向各级管理员，介绍日常运营的完整工作流程与规范。

### 28.1 日常运营任务清单

| 任务 | 频率 | 涉及区块 | 等级 |
| --- | --- | --- | --- |
| 处理层级投稿审核 | 按投稿量 | 层级审核 | Lv.1+ |
| 查看并回复在线客服 | 每天 | 在线客服 | Lv.1+ |
| 处理封禁申诉 | 按申诉量 | 在线客服 + 用户管理 | Lv.1+（解封需 Lv.4） |
| 处理管理员私聊 | 随时 | 管理员管理 | Lv.1+ |
| 维护音乐库 | 按需 | 音乐管理 | Lv.1+ |
| 广播全局消息 | 按需 | 管理员管理 | Lv.2+ |
| 维护后室成员 | 按申请量 | 后室成员管理 | Lv.2+ |
| 审核 MOD/DLC | 按提交量 | MOD/DLC 管理 | Lv.3+ |
| 发放 RC 密钥 | 按需求 | RC 密钥管理 | Lv.3+ |
| 处理爱发电订单（含 DLC 发货异常补发） | 按订单量 | 爱发电订单 | Lv.3+ |
| 查看系统日志 | 每天 | 日志查看 | Lv.3+ |
| 封禁违规用户/IP | 按需 | 用户管理 | Lv.4 |

### 28.2 在线客服值班规范

1. **上线即查看**：进入后台在线客服，先看待接入会话队列。
2. **及时接入**：看到新的转人工会话，尽快点击「接受」，避免用户久等。
3. **文明回复**：语气礼貌，明确给出结论或下一步。
4. **处理申诉**：申诉会话会显示封禁信息卡片（用户编号、理由、剩余时间）。与申诉者沟通后，由 Lv.4 管理员执行解封。
5. **结束会话**：问题解决后结束对话，并告知用户"如有其他问题随时联系"。
6. **离线处理**：如果当时不在线，上线后在"离线确认"中处理错过的会话，避免离线次数累积。

### 28.3 层级审核规范

**基础审核（所有管理员均可）**：
- 使用自己的 AI 初审时，务必把**下载的稿件文件**完整提供给 AI，不要只给链接。
- 提交结果前检查 AI 回复是否包含明确结论（通过/未通过）。
- 未通过时，AI 输出的理由会按编号发给作者，请确保理由具体、可执行。

**高级审核（Lv.3+）**：
- 高级审核是最终把关，即使基础审核通过也要人工复核内容。
- 拒绝时填写清晰理由，方便作者修改。

**已收录管理**：
- 重写操作会下架层级并保留原文件，适合需要大改的稿件。
- 删除是最终操作，谨慎使用。

### 28.4 封禁与解封规范

- **封禁前确认**：核实违规事实，避免误封。
- **封禁时长**：轻微违规用短时封禁（如几小时到几天）；严重违规用长时封禁；极端情况（广告轰炸、恶意攻击）用永久封禁。
- **定时 vs 永久**：能确定改正时间的用定时封禁，到期自动解封省心。
- **IP 封禁**：适用于恶意脚本、批量注册等情况；注意家庭/校园共用 IP 可能误伤。
- **解封**：申诉核实后及时解封。

### 28.5 管理员私聊与广播

- 私聊仅限可见范围内的管理员之间（可见规则见"后台管理"章节）。
- 私聊消息刷新后会自动恢复，不用担心中断。
- 全局消息广播（Lv.2+）面向下级管理员，用于通知运营事项。

### 28.6 运营小贴士

- **分工明确**：Lv.4 负责用户管理与最终决策，Lv.1-3 负责日常审核与客服。
- **及时响应**：在线客服的响应速度直接影响用户体验。
- **记录习惯**：重要操作（封禁、下架）可备注原因，方便回溯。
- **关注日志**：定期查看系统日志，发现异常及时处理。

---

## 二十九、附录：网站页面清单

> 本章面向**在线客服 AI**：用户问「某个功能在哪 / 网址是什么 / 这个页面里有什么」时，直接查本章，把完整 URL 给用户，不要凭空猜地址。
> 站点域名统一为 `https://ziyit-hacker.github.io/`，下表 URL 均为可直接访问的完整地址（`/` 结尾表示目录页，等价于该目录下的 `index.html`）。
> **Backrooms 单个内容页不逐条列出**（1000+ 篇）：用户要找某一篇层级 / 实体 / 物品 / 现象 / 团体 / 子区文档时，请给下面的**列表页**地址，让用户从列表里找到目标条目。

### 29.1 首页与全局

| 页面 | URL | 主要内容 |
| --- | --- | --- |
| 首页 | `https://ziyit-hacker.github.io/` | 站点入口：轮播图、功能区（下载 / 更多 / 病毒库 / 搜索 / 音乐 / 巨硬翻译 / 人机验证）、图书馆（Mod Wiki、Backrooms）、开发者选项（按身份显隐）、Cookie 提示、语言切换 |
| 搜索页 | `https://ziyit-hacker.github.io/search.html` | 站内搜索 + 必应全网搜索 + 精选推荐网站与在线工具导航 |
| 在线客服 | `https://ziyit-hacker.github.io/guide.html` | AI 智能助手 + 人工客服对话：剩余次数、转人工、结束对话 |
| 用户中心 | `https://ziyit-hacker.github.io/user.html` | 登录 / 注册 / 个人资料 / VIP 等用户入口聚合页 |
| 人机验证系统 | `https://ziyit-hacker.github.io/Human_verification.html` | Phantom 人机验证系统介绍与在线体验 |
| 人机验证使用方式 | `https://ziyit-hacker.github.io/人机验证使用方式.html` | Phantom 使用方式说明（含接入第三方网站的用法） |
| 巨硬翻译器 | `https://ziyit-hacker.github.io/Translator.html` | 在线翻译（需人机验证；普通 / VIP 每日限额不同） |
| 必应翻译恶搞工具 | `https://ziyit-hacker.github.io/translate.html` | 必应翻译循环恶搞小工具 |
| 只因游戏用户信息 | `https://ziyit-hacker.github.io/ztg.html` | 只因游戏相关的用户信息页 |

### 29.2 用户中心

| 页面 | URL | 主要内容 |
| --- | --- | --- |
| 登录页 | `https://ziyit-hacker.github.io/user/` | 登录（支持记住我）、跳转注册、封禁申诉入口 |
| 注册页 | `https://ziyit-hacker.github.io/user/register.html` | 注册新账号（用户名 / 邮箱 / 密码，含人机验证） |
| 修改密码 | `https://ziyit-hacker.github.io/user/password.html` | 申请修改登录密码 |
| 修改用户名 | `https://ziyit-hacker.github.io/user/username.html` | 修改展示用户名 |
| 用户信息 | `https://ziyit-hacker.github.io/user/profile.html` | 账号资料、登录记录、注销账号等 |
| VIP 升级 | `https://ziyit-hacker.github.io/user/VIP.html` | 爱发电自助购买 VIP；「查询我的订单」可自助补发 VIP / RC 密钥 / DLC 扩展包 |
| 我的 RC 密钥 | `https://ziyit-hacker.github.io/user/rc-key.html` | 查看自己名下 RC 许可证密钥的明文、权限、激活与到期时间 |
| 我的扩展包 | `https://ziyit-hacker.github.io/user/dlc.html` | 已拥有的 DLC 扩展包（编号 / 版本 / 说明 / 永久有效 / 来源）并可下载 |
| 我的文件密钥 | `https://ziyit-hacker.github.io/user/file-keys.html` | RC 文件加密的联网密钥 / 云加密用量与恢复密钥管理 |
| 我的密钥（API） | `https://ziyit-hacker.github.io/user/api-key.html` | 人机验证 API 密钥的用量与设置 |
| 封禁申诉 | `https://ziyit-hacker.github.io/user/appeal.html` | 用户名 + 密码验证后与人工客服实时对话申诉 |
| 更新公告 | `https://ziyit-hacker.github.io/user/gg.html` | 网站大更新公告与系统通知 |

### 29.3 内容与工具

| 页面 | URL | 主要内容 |
| --- | --- | --- |
| 音乐分享 | `https://ziyit-hacker.github.io/music/` | 在线播放、歌词同步、播放列表、按权限下载 |
| 下载中心 | `https://ziyit-hacker.github.io/download/` | 软件 / MOD / DLC 资源列表与下载（DLC 需账号已拥有，否则提示「您未拥有该 MOD/DLC」） |
| RC 下载页 | `https://ziyit-hacker.github.io/download/RC.html` | Release Control 介绍与下载、扩展包说明、**已知 BUG 列表与反馈入口** |
| 高级下载 | `https://ziyit-hacker.github.io/download/errordiv.html` | 备用 / 高级下载入口 |
| 更多工具 | `https://ziyit-hacker.github.io/move/` | 工具与资源汇总入口 |
| 病毒库 | `https://ziyit-hacker.github.io/move/mod.html` | 病毒样本资料 |
| Mod Wiki | `https://ziyit-hacker.github.io/ModWiki/` | Minecraft 模组百科首页 |
| Flight Management 模组 Wiki | `https://ziyit-hacker.github.io/ModWiki/Flight-Management/` | 单个模组的 Wiki 文档 |
| UI 设计体系测试页 | `https://ziyit-hacker.github.io/design/ui-test.html` | 站点 UI 设计体系的内部测试页（非正式功能） |

### 29.4 Backrooms 维基

| 页面 | URL | 主要内容 |
| --- | --- | --- |
| 维基主页 | `https://ziyit-hacker.github.io/backrooms/` | 各分区入口总览（层级 / 子区集 / 实体 / 物品 / 现象 / 团体） |
| 必读内容 | `https://ziyit-hacker.github.io/backrooms/必读/` | 新读者必读目录 |
| 站点规则 | `https://ziyit-hacker.github.io/backrooms/必读/site-rules.html` | 站点规则全文 |
| 批评政策 | `https://ziyit-hacker.github.io/backrooms/必读/criticism-policy.html` | 批评与点评规范 |
| 层级列表（总表） | `https://ziyit-hacker.github.io/backrooms/all-levels.html` | 层级总表 |
| 层级列表（实时） | `https://ziyit-hacker.github.io/backrooms/normal-levels/` | 层级实时列表（支持 `?open_level=<编号>` 直接展开某一层） |
| 子区集列表 | `https://ziyit-hacker.github.io/backrooms/sub-layers/` | 子区集（Level x.y）目录 |
| 实体列表 | `https://ziyit-hacker.github.io/backrooms/entities/` | 实体目录 |
| 物品列表 | `https://ziyit-hacker.github.io/backrooms/objects/` | 物品（Object）目录 |
| 现象列表 | `https://ziyit-hacker.github.io/backrooms/phenomena/` | 现象（Phenomenon）目录 |
| 层级列表（备用） | `https://ziyit-hacker.github.io/backrooms/phenomena/normal-levels-i.html` | 层级列表的历史 / 备用页 |
| 团体列表 | `https://ziyit-hacker.github.io/backrooms/groups-list.html` | 后室各团体目录 |
| IETS 实体分级说明 | `https://ziyit-hacker.github.io/backrooms/entities/iets.html` | 实体分级系统（IETS）语法说明（站务） |
| IPTS 现象分级说明 | `https://ziyit-hacker.github.io/backrooms/phenomena/ipts.html` | 现象分级系统（IPTS）语法说明（站务） |
| 文档创建器 | `https://ziyit-hacker.github.io/backrooms/create-document.html` | 在线编写 / 预览 / 导出层级文档（需登录） |
| 层级提交页 | `https://ziyit-hacker.github.io/backrooms/submit.html` | 提交文档 / 我的文档管理（登录即可） |
| 加入网站 | `https://ziyit-hacker.github.io/backrooms/join.html` | 后室成员申请 |
| 收录规范 | `https://ziyit-hacker.github.io/backrooms/InclusionR.html` | 投稿标准与收录要求 |
| 部署时刻表 | `https://ziyit-hacker.github.io/backrooms/Deployment.html` | 常部署时间说明 |
| 层级页模板 | `https://ziyit-hacker.github.io/backrooms/levels.html` | 层级页排版模板（`[NODATA]` 占位示例） |
| 404 提示页 | `https://ziyit-hacker.github.io/backrooms/error.html` | 页面不存在时的提示页 |

**关于 Backrooms 的单个内容页（重要）**

- 单个层级 / 实体 / 物品 / 现象 / 团体 / 子区文档共 **1000 篇以上**，URL 形态如下（不在本表逐一列出）：
  - 层级与子区：`/backrooms/sub-layers/rooms/<层级名>.html`，如 `level-0-1.html`、`level-1-1.html`
  - 实体：`/backrooms/entities/entity-<编号>.html`，如 `entity-1.html`
  - 物品：`/backrooms/objects/object-<编号>.html`，如 `object-1.html`
  - 现象：`/backrooms/phenomena/phenomenon-<编号>.html`，如 `phenomenon-1.html`
  - 团体：`/backrooms/groups/<团体名>.html`，如 `the-pantheon.html`
- **不要凭猜测拼 URL 直接给用户**。正确做法：先给用户**维基主页**或对应的**列表页**地址（见上表），让用户从列表里找到目标条目再点进去。

### 29.5 后台管理相关页面（仅管理员）

| 页面 | URL | 主要内容 |
| --- | --- | --- |
| 后台管理 | `https://ziyit-hacker.github.io/music/admin.html` | 管理员后台主界面：音乐管理、管理员管理、在线客服、层级审核、用户管理、MOD/DLC 管理、RC 密钥、爱发电订单、系统设置、日志入口等（按等级显隐） |
| 层级审核管理 | `https://ziyit-hacker.github.io/backrooms/review.html` | 基础审核 Lv.1+ / 高级审核 Lv.3+ / 已收录管理 Lv.2+ |
| 评分管理 | `https://ziyit-hacker.github.io/backrooms/ratings-admin.html` | 层级评分管理 |
| 开发者日志 | `https://ziyit-hacker.github.io/developers/` | 系统日志查看（Lv.3+） |
| 网页代码评判 | `https://ziyit-hacker.github.io/developers/code.html` | 网页代码评判工具 |

### 29.6 其他子站与特殊页面

| 页面 | URL | 主要内容 |
| --- | --- | --- |
| 只因游戏主站 | `https://ziyit-hacker.github.io/ikun/` | 独立子站首页 |
| 只因游戏下载页 | `https://ziyit-hacker.github.io/ikun/download/` | 子站下载页 |
| 导航子站 | `https://ziyit-hacker.github.io/wsh/` | 独立导航页面 |
| 子站用户页 | `https://ziyit-hacker.github.io/wsh/user.html` | 子站用户信息 |
| 子站登录页 | `https://ziyit-hacker.github.io/wsh/user/` | 子站登录 |
| 子站改密页 | `https://ziyit-hacker.github.io/wsh/user/password.html` | 子站修改密码 |
| 404 提示页 | `https://ziyit-hacker.github.io/tas/` | 页面不存在时的提示页 |

---

## 三十、不同角色功能地图

> 根据你的身份，快速了解你能做什么。

### 30.1 访客（未登录）

| 能做的事 | 说明 |
| --- | --- |
| 浏览首页与轮播 | 了解网站内容 |
| 浏览 Backrooms 已收录层级 | 公开可访问 |
| 浏览 Mod Wiki | 模组资料 |
| 搜索（站内 + 全网） | 站内搜索可能要求有限访问 |
| 浏览下载中心资源列表 | 普通资源可下载 |
| 收听免费音乐 | 播放不受登录限制 |
| 查看病毒库 | 公开资料 |
| 注册账号 | 成为正式用户 |

### 30.2 普通用户（已登录）

在访客基础上增加：

| 能做的事 | 说明 |
| --- | --- |
| 下载免费资源 | 需登录的免费资源 |
| 使用翻译器 | 每天 10 次 |
| 使用在线客服 | 每天 15 次 AI 问答，可转人工 |
| 发起封禁申诉 | 被封禁时 |
| 申请加入后室 | 填写加入申请 |
| 在线客服求助 | 转人工 |
| 修改个人资料 | 密码/用户名/资料 |
| 申请 VIP | 发邮件升级 |

### 30.3 VIP 用户

在普通用户基础上增加：

| 能做的事 | 说明 |
| --- | --- |
| 播放/下载全部音乐 | 含 VIP 曲目 |
| 下载 VIP 资源 | 下载中心 VIP 档 |
| 翻译器每天 25 次 | 提升额度 |
| 在线客服 AI 每天 50 次 | 提升额度 |
| 显示开发者选项菜单 | 部分菜单可见 |

> 记住：VIP 标识（ZC / zC）不等于管理员。

### 30.4 后室成员

在普通用户（或 VIP）基础上增加：

| 能做的事 | 说明 |
| --- | --- |
| 成员身份标识 | 在成员名单内，页面上显示为「成员」 |
| 管理自己的稿件 | 查看 / 修改 / 删除自己提交的文档（含审核中稿件） |

> **投稿本身不再要求成员身份**：任何登录用户都能提交文档并管理自己的稿件，成员身份只影响身份标识与名单管理能力。

### 30.5 管理员（Lv.1-4）

在后室成员等基础上增加（按等级递进）：

| 等级 | 新增能力 |
| --- | --- |
| Lv.1 | 音乐管理、管理员管理、在线客服、层级基础审核 |
| Lv.2 | + 全局广播、后室成员管理、已收录管理 |
| Lv.3 | + 层级高级审核、API Key、MOD/DLC、RC 密钥、日志查看 |
| Lv.4 | + 用户管理、封禁/IP 封禁、解封、客服名单管理 |

### 30.6 功能速查表

| 功能 | 访客 | 普通 | VIP | 后室成员 | 管理员 |
| --- | --- | --- | --- | --- | --- |
| 浏览已收录层级 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 听免费音乐 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 下载免费资源 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 下载 VIP 资源 | ❌ | ❌ | ✅ | ✅ | ✅ |
| 使用翻译器 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 在线客服 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 提交文档 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 后台管理 | ❌ | ❌ | ❌ | ❌ | ✅ |
| 封禁/解封 | ❌ | ❌ | ❌ | ❌ | Lv.4 才有 |

---

## 三十一、Backrooms 内容概览（读者指南）

> 本章面向 Backrooms 维基的读者，介绍维基各分区收录的内容类型，帮助你快速找到感兴趣的文档。

### 31.1 层级（Levels）

层级是后室的核心设定，每一个层级都是一个独立的空间，有编号、名称、生存难度和详细文档。常见的层级类型：

| 类型 | 特点 | 示例风格 |
| --- | --- | --- |
| 常规层级 | 有明确编号的稳定空间 | Level 0、Level 109 |
| 特殊层级 | 编号含符号或文字的层级 | Level !+、Level ！ |
| 宜居层级 | 适合居住、资源充足 | 生存难度为"宜居"的层级 |
| 死区层级 | 极度危险 | 生存难度为"死区"的层级 |
| 异常层级 | 规则异常、时间空间扭曲 | 需要特殊方法进出 |

### 31.2 实体（Entities）

实体是后室中存在的各类存在，从无害到致命：

| 实体类型 | 危险程度 | 说明 |
| --- | --- | --- |
| 友好实体 | 低 | 可能提供帮助 |
| 中立实体 | 中 | 不主动攻击但保持距离 |
| 敌对实体 | 高 | 主动攻击流浪者 |
| 未分类实体 | 未知 | 行为未知 |

### 31.3 物品（Objects）

物品是后室中的特殊道具与资源：

- **生存物资**：食物、水、医疗用品。
- **特殊物品**：有特殊功能的道具（如指南针、钥匙、装置）。
- **危险物品**：接触可能致命的物品。

### 31.4 现象（Phenomena）

现象是后室中的异常事件与自然规律：

- **空间现象**：空间扭曲、回廊循环。
- **时间现象**：时间流速异常、时间停滞。
- **精神现象**：幻听、幻视、记忆篡改。

### 31.5 团体（Groups）

团体是后室中的组织与社区：

- **人类组织**：幸存者社区、探索队、科研组织。
- **非人组织**：后室中的其他存在建立的团体。
- **敌对组织**：与流浪者敌对的势力。

### 31.6 如何阅读一份层级文档

阅读层级文档时，建议按以下顺序：

1. **看标题与生存难度**：先了解层级的危险程度。
2. **读描述**：了解环境与规则。
3. **看实体区块**：了解会遇到什么。
4. **看入口与出口**：了解如何进入和离开。
5. **看附录**：了解补充背景与故事。

> 提醒：公开列表只展示已收录的层级；收录中的稿件作者自己可以看到（页面带"正在审核中"提示）。

---

## 三十二、巨硬翻译器语言支持说明

### 32.1 常用语言下拉选项

翻译器内置的常用语言下拉框包含以下语言（以页面实际显示为准）：

| 语言 | 说明 |
| --- | --- |
| 中文 | 简体中文，站内默认目标语言之一 |
| 英文 | 国际通用语言 |
| 日文 | 日语 |
| 韩文 | 韩语 |
| 法文 | 法语 |
| 德文 | 德语 |
| 西班牙文 | 西班牙语 |
| 俄文 | 俄语 |
| 泰文 | 泰语 |
| 越南文 | 越南语 |

### 32.2 自定义语言代码

如果下拉列表没有你需要的语言，可以在"自定义语言代码"框中填写语言代码。常用代码示例：

| 语言代码 | 语言 |
| --- | --- |
| zh | 中文 |
| en | 英文 |
| ja | 日文 |
| ko | 韩文 |
| fr | 法文 |
| de | 德文 |
| es | 西班牙文 |
| ru | 俄文 |
| it | 意大利文 |
| pt | 葡萄牙文 |
| ar | 阿拉伯文 |
| th | 泰文 |
| vi | 越南文 |
| hi | 印地文 |
| id | 印尼文 |
| tr | 土耳其文 |
| nl | 荷兰文 |
| pl | 波兰文 |
| uk | 乌克兰文 |

### 32.3 翻译建议

- 短文本翻译质量更高；长文本建议分段翻译。
- 专有名词（人名、地名、品牌）翻译可能与常见译法不同，请自行校对。
- 翻译结果仅供参考，重要内容请以原文为准。

---

## 三十三、补充常见问题（第二辑）

### 33.1 账号类（补充）

**Q：注册时提示用户名已被占用？**
A：换一个用户名即可。用户名具有唯一性。

**Q：邮箱填错了怎么办？**
A：尽快联系站长协助修改，否则可能影响找回密码与注销。

**Q：登录后看不到用户名？**
A：可能是页面缓存，强制刷新（Ctrl+F5）即可。

**Q：为什么我退出登录了？**
A：可能原因：登录态过期、账号被登出、使用临时账号到期。重新登录即可。

**Q：同一个账号可以在手机上登录吗？**
A：可以，登录状态互不影响。

### 33.2 音乐类（补充）

**Q：播放器不显示播放列表？**
A：请确认网络正常并刷新页面；音乐清单加载需要一点时间。

**Q：切歌后没有声音？**
A：点击播放器上的播放按钮；部分视频源需要缓冲。

**Q：歌词能关掉吗？**
A：歌词面板可以折叠或关闭，不影响播放。

**Q：VIP 曲目能不能试听？**
A：目前 VIP 曲目需要开通 VIP 后播放。

### 33.3 下载类（补充）

**Q：下载的压缩包损坏？**
A：建议重新下载；如资源提供校验值，请核对。

**Q：资源更新了，下载的是新版吗？**
A：以页面显示的资源版本为准，管理员维护更新。

**Q：MOD 和 DLC 有什么区别？**
A：都是下载中心的资源分类：**MOD 是免费模组**（登录即可下载），**DLC 是 RC 软件的付费扩展包**（需已在爱发电购买、账号已拥有后才能下载，购买后永久有效）。

### 33.4 翻译类（补充）

**Q：翻译结果和中文意思对不上？**
A：机器翻译对语境敏感，特别是口语、双关语。建议调整表述后重试。

**Q：自定义语言代码填了没反应？**
A：请确认填写的代码格式正确（如 `ja`），且该语言在服务端受支持。

**Q：能翻译文件吗？**
A：目前只支持文本翻译，不支持文件上传翻译。

### 33.5 在线客服类（补充）

**Q：转人工后消息没人回？**
A：可能客服暂时不在线，消息会保留等待。也可以稍后再次发起。

**Q：AI 回答的不对？**
A：可以补充细节重新提问，AI 只了解网站相关内容。

**Q：会话能保存吗？**
A：会话在对话期间可见，结束后自动清理。

### 33.6 Backrooms 类（补充）

**Q：怎么知道自己的稿件审核进度？**
A：在「我的层级」查看状态：待审核 → 已过基础审核 → 高级审核中 → 已收录/未收录。

**Q：可以把别人写的层级改一改投稿吗？**
A：不可以。投稿必须是原创内容，抄袭会被拒绝。

**Q：层级名称可以改吗？**
A：可以在修改稿件时更新名称。

**Q：为什么我的层级在公开列表找不到？**
A：只有"已收录"状态才公开展示；"已重写"的层级也会从公开列表移除（原文件保留）。

**Q：投稿后能撤回吗？**
A：可以在「我的层级」删除稿件。

### 33.7 管理员类（补充）

**Q：为什么我看不到某个区块？**
A：该区块可能要求更高管理员等级，请确认等级权限（见权限矩阵）。

**Q：封禁后用户还能申诉吗？**
A：可以，被封禁用户在登录页有「封禁申诉」入口。

**Q：处理申诉需要什么权限？**
A：查看/对话申诉会话 Lv.1 即可；解封需要 Lv.4。

**Q：客服名单怎么管理？**
A：仅站长（Lv.4）可以增删客服名单成员。

---

## 三十四、网站使用规范与用户约定

### 34.1 内容规范

**用户产生的内容**（投稿、对话、资料等）应遵守：

- 不得包含违法违规内容。
- 不得包含色情、暴力、人身攻击等不当内容。
- 不得侵犯他人知识产权（抄袭、盗版等）。
- 不得发布广告、垃圾信息。
- 投稿内容需为原创，符合维基收录规范。

**违反后果**：视情节轻重，可能：提示修改、拒绝稿件、警告、临时封禁、永久封禁、加入黑名单。

### 34.2 行为规范

- 文明交流，尊重他人。
- 不使用外挂、脚本、批量工具恶意刷取功能额度。
- 不攻击、破坏网站系统。
- 不利用漏洞获取不当权限。
- 不冒用他人身份。

### 34.3 版权说明

- 网站页面设计、代码版权归 ZIYIT STUDIO 所有。
- 用户投稿的层级文档版权归作者本人，授权网站收录展示。
- 引用的第三方资源（如背景音乐、图片）版权归原作者。
- 如需转载网站内容，请注明来源。

### 34.4 免责声明

- 网站内容仅供学习交流与娱乐。
- 下载资源请自行甄别安全性，安装前建议查杀。
- 翻译结果仅供参考。
- 因用户自身行为造成的损失，网站不承担责任。

### 34.5 申诉与投诉渠道

- 封禁申诉：登录页「封禁申诉」。
- 账号问题：邮件联系站长（ziyitstudio@qq.com）。
- 内容投诉：邮件联系站长，说明违规内容与理由。

---

## 三十五、账号安全与找回指南

> **2026-09-25 新增**：账号可以要求「**两种验证方式**」了（密码 / 邮箱验证码 / 2FA 动态码任选两类，
> 管理员账号被**强制**两种且关不掉），并可用 **10 个一次性恢复码**兜底。开通方式、绑定二维码、
> 恢复码保存与「无密码登录」的前提，见 **[六十一、账号安全与多种验证方式](#六十一账号安全与多种验证方式含前端对接要求)**。

### 35.1 账号安全最佳实践

1. **密码设置**：
   - 至少 8 位，包含大小写字母、数字、符号。
   - 不要使用生日、手机号、简单单词。
   - 不同平台使用不同密码。
2. **登录习惯**：
   - 公共电脑使用后退出登录。
   - 不要勾选"记住我"在公共电脑上。
   - 定期修改密码。
3. **环境安全**：
   - 不要点击不明链接输入账号。
   - 使用官方网址访问网站。
   - 注意浏览器是否有异常插件。

### 35.2 密码忘记怎么办

1. 打开登录页，寻找"忘记密码"功能。
2. 使用注册邮箱完成验证与重置。
3. 如果无法自助找回（如邮箱不可用），联系站长并提供可证明账号归属的信息。

### 35.3 账号被盗怎么办

1. 尽快尝试通过找回密码功能重置密码。
2. 如果无法操作，立即邮件联系站长，说明：
   - 用户名 / 注册邮箱。
   - 发现异常的时间与表现。
   - 期望的处理方式（冻结 / 重置）。
3. 站长核实身份后会协助处理。

### 35.4 邮箱变更

- 修改邮箱请联系站长处理。
- 邮箱用于找回密码、注销账号、保留账号，请保持可用。

### 35.5 账号注销

1. 在用户中心发起注销申请。
2. 按照提示完成验证。
3. 如改变主意，可以取消注销。

> 注意：注销后账号数据将不可恢复，请谨慎操作。

---

## 三十六、Backrooms 收录规范详解

> 本章为投稿作者提供详细的收录标准说明。投稿前请逐条核对。

### 36.1 基本要求

1. **原创性**：内容必须为作者原创，不得抄袭、翻译他人作品冒充原创（翻译需注明来源并获授权）。
2. **完整性**：文档需包含标题、生存难度、描述、入口、出口等基本区块。
3. **格式规范**：使用文档创建器导出的标准结构，不要破坏必要的区块与样式。
4. **文字质量**：语句通顺，无明显错别字与病句，分段合理。

### 36.2 内容红线（一票否决）

以下内容直接不通过：

- 违反法律法规的内容。
- 涉及现实人物/事件的恶意影射。
- 色情、极端暴力、恐怖主义内容。
- 广告、引流、商业推广内容。
- 明显的水文/占位内容。
- 恶意攻击他人的内容。

### 36.3 质量参考标准

| 维度 | 合格标准 |
| --- | --- |
| 篇幅 | 描述至少形成完整段落，纯几句话视为过短 |
| 设定 | 前后一致，规则清晰，无重大矛盾 |
| 氛围 | 有明确的氛围营造，而非平铺直叙 |
| 细节 | 有环境、感官、结构等细节描写 |
| 创新 | 不与他人知名设定高度雷同 |
| 配图 | 图片清晰、无水印、来源合规（如用） |

### 36.4 修改建议流程

- 收到"未通过"通知后，认真阅读理由列表。
- 逐条修改（补充内容、修正格式、调整设定等）。
- 在「我的层级」使用「修改」上传新版本。
- 重新进入审核队列。

### 36.5 已收录后的维护

- 作者可以随时更新自己的已收录层级（产生新版本）。
- 管理员可以对已收录层级执行重写（下架+保留原文件）或删除。
- 已收录层级出现违规内容的，管理员有权下架。

### 36.6 关于"重写"标记

- 被重写的层级在列表中会带有重写标记。
- 重写意味着原版本被替换/下架，公开访问不再显示旧版。
- 原文件会保留存档，作者或管理员可在此基础上继续修改。

---

## 三十七、补充常见问题（第三辑）

### 37.1 账号与登录（续）

**Q：忘记用户名怎么办？**
A：如果记得注册邮箱，联系站长按邮箱查询用户名。

**Q：可以注册中文用户名吗？**
A：可以，但注意部分系统输入场景（如复制粘贴）中英文均支持。

**Q：为什么注册后不能立即使用某些功能？**
A：投稿只要登录即可（**不再要求后室成员身份**）；只有后台管理等功能需要额外的管理员身份，与注册无关。

### 37.2 音乐与下载（续）

**Q：播放器卡顿？**
A：请检查网络；视频类曲目需要更多带宽。

**Q：下载到一半断了？**
A：重新下载即可，断点续传由浏览器决定。

**Q：能上传音乐到网站吗？**
A：音乐库由管理员维护，暂不支持用户上传。

### 37.3 翻译器（续）

**Q：翻译支持代码/编程语言吗？**
A：翻译器面向自然语言，不建议翻译代码。

**Q：为什么提示次数还有但翻译失败？**
A：可能是网络或服务临时问题，稍后重试；失败不计次数。

### 37.4 在线客服（续）

**Q：AI 客服和我在别处用的 AI 一样吗？**
A：本站 AI 是网站专用客服，只了解网站相关内容，与其他平台 AI 无关。

**Q：对话内容会被别人看到吗？**
A：会话仅客服相关人员在后台可见，结束后自动清理。

**Q：AI 封禁会封我的 IP 吗？**
A：AI 只能封禁当前对话的用户账号（最长 7 天），不能封 IP、不能封管理员。

### 37.5 Backrooms（续）

**Q：能否提交实体/物品/现象条目？**
A：投稿系统现已支持**层级、实体、物品、现象四类**文档；其中实体文档必须带 IETS 实体分级标识、现象文档必须带 IPTS 现象分级标识。

**Q：层级 ID 能自定义为中文吗？**
A：层级 ID 采用"Level-编号"格式（编号可为数字或符号/文字），提交时以规范提示为准。

**Q：投稿需要版权声明吗？**
A：投稿即表示授权网站收录展示；作者保留署名权。

### 37.6 管理员（续）

**Q：添加管理员需要什么？**
A：需要站长权限（Lv.4），填写用户编号与等级。

**Q：管理员等级能自己改吗？**
A：不能，等级由上级管理员设置。

**Q：撤销管理员会影响私聊记录吗？**
A：撤销后不再进入管理员列表，历史消息以本地保存为准。

### 37.7 通用（续）

**Q：网站会收集我的什么信息？**
A：网站收集账号信息（用户名、邮箱）与必要的使用数据，用于账号与安全功能。

**Q：如何彻底不再使用网站？**
A：注销账号（见"账号安全与找回指南 → 账号注销"）。

---

## 三十八、功能常见问题速查（总表）

> 快速定位问题：找到你的现象 → 查看原因 → 按建议操作。

### 38.1 登录/账号速查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 提示用户名或密码错误 | 输入错误 | 核对后重试 |
| 提示登录过于频繁 | 限流 | 等几分钟 |
| 提示已封禁 | 账号被封 | 等待解封或申诉 |
| 菜单没有登录状态 | 未登录/缓存 | 登录或强制刷新 |
| 自动退出 | 临时账号到期/态过期 | 重新登录 |
| 显示 [object Object] | 旧版本问题 | 已修复，强制刷新 |

### 38.2 音乐速查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 播放器无列表 | 加载中/网络 | 刷新等待 |
| VIP 曲目无法播放 | 无 VIP | 开通 VIP |
| 下载按钮不可用 | 权限不足 | 登录或开通 VIP |
| 无声音 | 未播放/缓冲 | 点击播放 |
| 歌词不同步 | 无歌词/网络 | 点击歌词跳转 |

### 38.3 下载速查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 下载失败 | 网络/权限 | 登录或重试 |
| 文件损坏 | 下载不完整 | 重新下载并核对校验值 |
| 提示无权限 | 资源档位 | 见权限说明 |
| 找不到资源 | 已下架 | 联系管理员 |

### 38.4 翻译器速查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 进不去界面 | 验证未过 | 关 F12 刷新 |
| 今日次数已用完 | 限额 | 次日或开 VIP |
| 翻译超时 | 等待过久 | 重试/缩短文本 |
| 结果不准确 | 语境敏感 | 调整表述 |
| 自定义代码无效 | 格式/支持 | 核对代码 |

### 38.5 在线客服速查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| AI 不回答 | 无关话题/拦截 | 提网站相关问题 |
| AI 主动结束 | 自主判断 | 重新对话 |
| 转人工没人接 | 客服不在线 | 等待/稍后再试 |
| 今日次数用尽 | 限额 | 次日或开 VIP |
| 被 AI 封禁 | 违规触发 | 到期解封或申诉 |
| 申诉会话结束 | 客服结束 | 等客服处理结果 |

### 38.6 Backrooms 速查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 提示账号已被封禁 | 账号处于封禁状态 | 走「封禁申诉」联系管理员 |
| 提示 ID 已存在 | 被占用 | 换 ID |
| 稿件一直待审核 | 排队中 | 耐心等待 |
| 稿件被拒 | 未达标 | 看理由修改重提 |
| 公开列表找不到 | 未收录 | 已收录才公开 |
| 提示无法访问 | 未收录/已下架 | 作者可登录查看 |

### 38.7 管理员速查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 看不到某区块 | 等级不足 | 见权限矩阵 |
| 无法结束申诉 | 旧限制 | 已修复，可结束 |
| 解封失败 | 需 Lv.4 | 由站长操作 |
| 客服名单无法管理 | 需站长 | 站长操作 |

---

## 三十九、Backrooms 维基阅读指南（进阶）

### 39.1 从哪开始读

- **新手**：从"已收录"的常规层级开始（如 Level 0 相关文档），了解层级文档的基本结构。
- **进阶**：阅读特殊层级与高难度层级，感受不同风格。
- **体系**：结合实体、物品、现象、团体条目，理解后室世界观。

### 39.2 层级编号规律

| 编号范围 | 含义 |
| --- | --- |
| Level 0-999 | 常规层级区间 |
| Level 1000+ | 扩展区间 |
| Level 负数 | 负数层级（如 Level -1） |
| Level 特殊符号 | 特殊层级（如 Level !+、Level ！） |
| Level 文字 | 特殊命名层级（如 Level NO-DATA） |

### 39.3 生存难度与阅读预期

- **0-1 级**：安全，适合了解后室基本设定。
- **2-3 级**：有危险，出现实体与冲突。
- **4-5 级**：高危险，生存主题。
- **死区**：极端危险，常伴随恐怖氛围。
- **宜居**：舒缓向内容，资源与社区。
- **未分级/修正/终结/不适用**：特殊设定，阅读时注意文档说明。

### 39.4 常见叙事元素

后室文档中常见的元素与套路：

- **杏仁水**：后室中的常见补给品。
- **切入点**：从现实/其他层级进入的方式。
- **基地/前哨站**：人类在层级中建立的据点。
- **流浪者**：被困在后室中的人类。
- **M.E.G.（或类似组织）**：后室中的探索组织。
- **实体袭击**：层级的主要威胁来源。

### 39.5 收藏与管理

- 建议把想看的文档加入浏览器收藏夹。
- 使用站内搜索按关键词查找（如"医院"、"电梯"）。
- 关注更新：新增层级会出现在动态层级列表。

---

## 四十、音乐与下载中心使用详解

### 40.1 音乐页面完整说明

**页面布局**：
- 顶部：站点导航。
- 中部：播放器（固定区域），包含播放控制、进度、歌词。
- 下方：播放列表。

**播放列表信息**：
- 每首曲目显示名称、作者/来源、时长（如有）。
- VIP 曲目有特殊标记。

**播放器控制**：
| 控件 | 功能 |
| --- | --- |
| 播放/暂停 | 开始或暂停当前曲目 |
| 上一曲 | 切换到上一首 |
| 下一曲 | 切换到下一首 |
| 进度条 | 拖拽定位播放位置 |
| 时间 | 当前播放时间 / 总时长 |
| 歌词按钮 | 打开/关闭歌词面板 |

**播放源类型**：
- 音频：MP3 等格式，网页内播放。
- 视频：直接播放视频文件。
- B 站视频：内嵌 B 站播放器。

### 40.2 音乐下载说明

- 下载按钮位置：播放列表每行 / 曲目详情。
- 普通曲目：登录后即可下载。
- VIP 曲目：需要 VIP 身份。
- 下载格式：与曲目源格式一致。

### 40.3 下载中心完整说明

**资源卡片**：
- 资源名称与版本。
- 资源简介。
- 权限标签（普通/免费/VIP）。
- 下载按钮。

**搜索**：
- 顶部搜索框支持关键词过滤。
- 搜索范围：资源名称与简介。

**资源类型**：
| 类型 | 说明 |
| --- | --- |
| 软件 | 网站出品的程序工具 |
| MOD | 免费的模组资源（登录后可下载） |
| DLC | RC 软件的付费扩展包，需已拥有才能下载（爱发电购买后永久有效） |
| 其他 | 数据包、文档等 |

### 40.4 下载失败排查流程

1. 确认已登录（免费资源需要）。
2. 确认权限（VIP 资源需要 VIP）。
3. 确认网络正常。
4. 换浏览器 / 清缓存重试。
5. 仍失败 → 联系管理员反馈。

---

## 四十一、在线客服深入指南

### 41.1 对话界面详解

| 区域 | 内容 |
| --- | --- |
| 顶部状态徽章 | 当前会话状态（AI 助手 / 等待人工 / 人工客服·姓名 / 对话已结束） |
| 次数区 | 今日剩余次数、重置时间 |
| 消息区 | 各类消息气泡（AI / 人工 / 自己 / 系统） |
| 输入区 | 文本输入框 + 发送按钮 + 结束对话按钮 |
| 回复显示 | AI 回复**流式逐字出现**；查资料时会先显示「正在查阅网站文档…」「正在打开网站页面读取内容…」等进度提示 |

### 41.2 发送技巧

- 按 **Enter** 发送，按 **Shift + Enter** 换行。
- 一次描述清楚问题：把问题、现象、期望放在一条消息里。
- 转人工指令：直接输入「转人工」并确认。

### 41.3 AI 客服的边界（重要）

- 只回答**网站相关内容**。
- 不提供其他领域建议（医学、法律等）。
- 不参与任何争议性话题。
- 遇到越狱/注入指令会被安全拦截。
- 无法查询其他用户信息。

### 41.4 申诉会话的特殊性

- 申诉会话通过「封禁申诉」进入，不是普通在线客服会话。
- 管理员查看申诉会话时，顶部显示**封禁信息卡片**。
- 申诉者只能等待客服处理，不能主动结束申诉（客服可以结束）。
- 申诉凭证有时效，过期需重新发起。

### 41.5 会话状态说明

| 状态 | 含义 | 表现 |
| --- | --- | --- |
| 进行中 | 对话正常 | 可正常收发消息 |
| 等待人工 | 已转人工未接入 | 顶部显示等待 |
| 人工对话 | 客服已接入 | 显示客服姓名 |
| 已结束 | 对话结束 | 输入区禁用 |

### 41.6 常见问题补充

**Q：为什么我的消息发送了 2 条？**
A：这是旧版本的显示 bug，现已修复（发送消息只显示一次）。

**Q：客服界面刷新后消息会乱吗？**
A：已修复——刷新后消息归属正确，自己的消息不会变成对方的。

**Q：申诉会话里我的消息不见了？**
A：已修复——刷新后服务端历史会正常加载。

---

## 四十二、补充常见问题（第四辑）

### 42.1 界面与显示

**Q：页面显示错乱或样式丢失？**
A：强制刷新（Ctrl+F5）重新加载样式；若仍异常，清缓存或换浏览器。

**Q：移动端页面显示不全？**
A：站点支持移动端适配，使用汉堡菜单展开导航；横屏/竖屏均可浏览。

**Q：字体显示乱码？**
A：确认浏览器编码为 UTF-8；如为旧缓存，强制刷新。

### 42.2 网络与加载

**Q：页面加载很慢？**
A：与网络环境有关；音乐、翻译等需要额外请求的功能可能较慢。

**Q：在线人数显示不准？**
A：在线人数每 30 秒更新一次，存在合理延迟。

### 42.3 权限与提示

**Q：为什么有的按钮点了没反应？**
A：可能是权限不足（如未登录/VIP），或需要先完成前置步骤（如人机验证）。

**Q：提示"请先登录"但已登录？**
A：可能登录态过期，重新登录或强制刷新。

### 42.4 内容与版权

**Q：网站内容可以转载吗？**
A：注明来源后可转载；用户投稿内容请联系作者。

**Q：发现有人抄袭我的投稿？**
A：联系站长投诉，提供证据。

### 42.5 其他补充

**Q：网站有 App 吗？**
A：目前为网页版，支持手机浏览器直接访问。

**Q：能提功能建议吗？**
A：欢迎通过站长邮箱提出建议，管理员会评估是否采纳。

**Q：遇到 Bug 怎么反馈？**
A：描述现象、操作步骤、浏览器与时间，邮件发给站长即可。

---

## 四十三、系统状态与维护说明

### 43.1 服务状态说明

- 网站以网页形式提供，正常情况 7×24 小时可访问。
- 部分功能依赖外部服务（音乐源、翻译服务等），外部故障时对应功能可能临时不可用。
- 升级维护时网站可能短暂不可访问，敬请谅解。

### 43.2 数据与隐私

- 账号数据、会话数据由服务器保存，正常情况不对外公开。
- 用户投稿内容的收录状态由管理员管理。
- 会话记录会在结束后自动清理。

### 43.3 备份与恢复

- 网站数据由管理员定期备份。
- 用户账号、稿件等数据以服务器记录为准。
- 建议用户自行保存重要内容（如已投稿的 HTML 文件）。

### 43.4 安全公告

- 网站会及时修复已知安全问题。
- 如果发现网站异常或安全漏洞，请联系站长反馈。
- 请勿利用漏洞进行恶意操作，否则将被严肃处理。

---

## 四十四、联系方式与支持渠道

### 44.1 站长邮箱

**ziyitstudio@qq.com**

适用场景：
- VIP 升级申请与咨询。
- 账号找回、邮箱变更。
- 封禁/黑名单申诉（也可走在线申诉通道）。
- 内容投诉。
- Bug 反馈与功能建议。

### 44.2 在线客服

适用场景：
- 网站功能使用咨询。
- 转人工客服。
- 封禁申诉对话。

### 44.3 封禁申诉

适用场景：
- 登录页 →「封禁申诉」→ 用户名密码验证 → 对话申诉。

### 44.4 反馈建议模板

给站长发邮件时，建议包含以下信息，处理更快：

- **问题类型**：Bug / 建议 / 申诉 / VIP
- **页面/功能**：如"音乐页面"、"在线客服"
- **现象**：描述你看到的内容
- **操作步骤**：如何复现
- **环境**：浏览器 + 设备 + 时间
- **截图/附件**：（如有）

---

## 四十五、全功能使用场景图文说明

> 以具体场景为例，串联多个功能的使用。

### 45.1 场景：新用户完整上手

1. 打开首页 → 浏览轮播了解近期更新。
2. 进入「搜索页」搜索"音乐"，找到音乐入口。
3. 先不登录试听免费音乐。
4. 点击「登录」→ 注册账号。
5. 登录后使用翻译器（每天 10 次）。
6. 遇到问题 → 在线客服咨询（转人工）。
7. 想听全部音乐 → VIP 申请。

### 45.2 场景：Backrooms 作者完整流程

1. 阅读「收录规范」了解要求。
2. 用「文档创建器」创作并导出 HTML。
3. 确认已登录（投稿无需后室成员身份）。
4. 进入「层级提交」填写 ID + 名称 + 上传。
5. 「我的层级」跟踪状态。
6. 被拒 → 按理由修改 → 重新提交。
7. 收录后公开可访问。

### 45.3 场景：管理员一天的工作

1. 进入后台，先看在线客服队列，处理新会话与申诉。
2. 打开「层级审核」处理待审核稿件（基础/高级）。
3. 处理管理员私聊与全局消息。
4. 查看系统日志确认无异常。
5. 处理成员申请、资源审核等按需任务。

### 45.4 场景：被封禁用户自救

1. 尝试登录 → 看到封禁原因与剩余时间。
2. 点击「封禁申诉」→ 输入用户名密码。
3. 与客服沟通说明情况。
4. 客服认可 → 解封 → 重新登录。
5. 客服不认可 → 等待到期自动解封（定时封禁）。

### 45.5 场景：内容创作者推广

1. 上传作品到下载中心（管理员审核）。
2. 使用音乐库分享音乐（管理员维护）。
3. 投稿 Backrooms 层级扩大影响力。
4. 通过在线客服与用户互动。

---

## 四十六、层级投稿后的常见问题（专题）

### 46.1 我的稿件状态一直没变

- 审核是人工流程，时间不定。
- 待审核队列可能较长，请耐心等待。
- 长时间无变化可联系管理员催办。

### 46.2 我的稿件被拒了，如何提高过稿率

1. 认真阅读理由列表（编号 1. 2. 3.）。
2. 逐条对照修改：
   - "内容过短" → 补充环境、结构、氛围描写。
   - "设定矛盾" → 整理规则清单，统一设定。
   - "格式不全" → 补全入口/出口等区块。
   - "抄袭" → 重写为原创内容。
3. 使用创建器重新导出，确保格式规范。
4. 重新提交新版本。

### 46.3 稿件已收录，想大幅修改

- 正常更新：用「修改」上传新版本（旧版保留）。
- 需要重写的：联系管理员说明需求，管理员执行重写（下架+保留原文件）。

### 46.4 稿件被管理员删除

- 删除是管理员操作（可能因违规或管理需要）。
- 如有异议，通过在线客服或邮件联系管理员沟通。

### 46.5 关于"已重写"状态

- 被重写的层级在列表带重写标记。
- 公开访问不再显示旧版，原文件存档保留。
- 作者可以在重写基础上继续修改。

---

## 四十七、巨硬翻译器深入指南

### 47.1 翻译器整体介绍

巨硬翻译器是 ZIYIT STUDIO 提供的在线翻译工具，支持多语言互译。与普通翻译软件不同，本站翻译器在保留原意的基础上，会尽量让译文自然流畅，避免"机翻腔"。

### 47.2 支持的语言

- 中文（简体）
- 英语
- 日语
- 韩语
- 法语
- 德语
- 西班牙语
- 俄语
- 更多语言持续接入中

各语言翻译方向均为双向互译，使用时选择"源语言"和"目标语言"即可。

### 47.3 翻译质量提示

- 短句翻译质量最高：一句话内的语义更完整，机器更容易理解。
- 段落翻译建议分段进行：超长段落可能出现语义割裂。
- 专业术语：人名、地名、专有名词建议保留原文或先翻译一次查看效果。
- 特殊格式：包含代码、标记符号的内容，翻译后可能需要手动修正符号。

### 47.4 每日限额说明

- 普通用户每日可用次数有限，用完需等待次日重置。
- VIP 用户每日可用次数更高，翻译更畅快。
- 每日额度按自然日计算，当日 0 点重置。
- 已用次数会在界面显示，方便你掌握剩余额度。

### 47.5 遇到"暂时无法翻译"怎么办

- 可能是瞬时请求过多，稍等片刻再试。
- 若持续提示，可联系 ziyitstudio@qq.com 咨询，或考虑升级为 VIP。
- VIP 的价格与 RC 专业版一致，详情见 VIP 章节。

### 47.6 常见问题

Q：翻译结果和我想要的不一样怎么办？
A：可以换一种措辞重新表达后再翻译，或者将长句拆成短句逐句翻译。

Q：翻译会泄露我的内容吗？
A：翻译内容仅用于本次翻译处理，不对外展示，请放心使用。

Q：可以批量翻译吗？
A：目前暂不支持文件批量翻译，请分段复制粘贴。

Q：为什么有的语言没有？
A：语言列表持续扩充中，敬请期待。

---

## 四十八、音乐与下载中心权益问答

### 48.1 音乐板块权益说明

- 游客：可以浏览音乐页面，部分功能受限。
- 注册用户：可以正常听歌、查看歌单。
- VIP 用户：享受完整音乐权益，具体以页面展示为准。

### 48.2 音乐无法播放的排查步骤

1. 检查网络连接是否正常。
2. 刷新页面重试。
3. 更换浏览器（推荐最新版 Chrome / Edge）。
4. 若仍无法播放，联系管理员反馈具体歌曲。

### 48.3 下载中心权益说明

- 游客：可浏览下载中心页面，具体资源是否可下载以页面提示为准。
- 注册用户：大部分资源可正常下载。
- VIP 用户：享受更高下载权限与优先资源。

### 48.4 下载速度慢怎么办

- 建议使用稳定的宽带网络。
- 避免同时下载多个文件。
- 可尝试更换下载时间段（避开高峰）。
- 部分资源体积较大，下载时间较长属正常现象。

### 48.5 关于资源的更新

- 音乐库和下载中心会不定期更新。
- 资源更新后无需手动刷新，重新进入页面即可看到最新内容。
- 如果你有希望上架的资源，可以通过在线客服或邮箱建议。

### 48.6 常见问题

Q：游客能听歌吗？
A：可以，游客可以正常浏览和试听，具体权限以页面显示为准。

Q：资源解压密码是什么？
A：一般情况下资源直接可用；如需密码，页面通常会有提示，请仔细查看页面说明。

Q：下载的文件损坏了怎么办？
A：请检查文件大小是否完整，必要时重新下载一次；仍失败请反馈管理员。

---

## 四十九、Backrooms 创作全流程指南

### 49.1 创作流程总览

1. 阅读收录规范，确认创作方向。
2. 使用层级文档创建器编写文档。
3. 导出 HTML 文件（注意文件大小不超过 1MB）。
4. 进入"层级提交"页面上传稿件。
5. 等待审核（待审核 → 已收录 / 已拒绝）。
6. 已收录后可在动态列表看到你的层级。

### 49.2 选题与灵感

- 新人建议从 Level-1 ~ Level-99 之间的数字区间选择，竞争较小。
- 可以围绕"主题层级"（如节日、事件）创作，更容易被收录。
- 不要直接模仿已有经典层级，原创性更重要。

### 49.3 写作要点回顾

- 标题：简洁有辨识度。
- 生存难度：用创建器的难度组件生成，保持规范。
- 描述：写清环境、氛围、实体、资源。
- 入口与出口：逻辑清晰，可操作。
- 实体与基地：与主世界观不冲突。

### 49.4 提交前的自查清单

- [ ] 文件名是否为 HTML 格式
- [ ] 文件大小是否在 1MB 以内
- [ ] 是否填写了层级 ID（Level-xxx）
- [ ] 是否填写了层级名称
- [ ] 文档是否有明显的格式错乱
- [ ] 是否有敏感违规内容
- [ ] 是否已完成登录（登录即可投稿）

### 49.5 审核时间与催办

- 审核为人工流程，没有固定时限。
- 如果稿件长时间停留在"待审核"，可以通过在线客服联系管理员催办。
- 催办时请提供你的层级 ID 和名称，方便管理员快速定位。

### 49.6 投稿被拒后的补救流程

1. 查看拒绝理由（编号列表）。
2. 逐条修改稿件。
3. 重新导出并上传新版本。
4. 新版本会进入待审核队列，重新排队。

### 49.7 创作中的常见误区

- 误区一：把生存难度写得过高（如死区）来"吸引眼球"——审核会重点检查难度是否与内容匹配。
- 误区二：大量堆砌代码、表格等非写作内容——文档应以文字为主。
- 误区三：直接套用其他作品的设定——注意原创性。
- 误区四：层级 ID 与名称不符——名称应与内容呼应。

### 49.8 与其他创作者交流

- 通过本站联系管理员获取投稿建议。
- 关注已收录的优秀层级，学习其结构与写法。
- 注意：抄袭行为会被严肃处理，请坚持原创。

---

## 五十、补充常见问题（第五辑）

### 50.1 账号相关

Q：注册时提示用户名已被占用怎么办？
A：换一个用户名即可；用户名唯一，不能重复注册。

Q：登录时提示密码错误，但我确定密码是对的？
A：检查是否开启了大小写锁定；也可通过找回流程重置密码。

Q：账号登录后掉线了怎么办？
A：网络不稳定可能导致会话失效，重新登录即可。

Q：我能在多台设备登录吗？
A：可以正常登录使用；同一账号请勿在多处频繁切换，以免触发安全保护。

### 50.2 VIP 相关

Q：VIP 是永久的吗？
A：VIP 有对应的有效期，具体时长以开通时说明为准，到期后权益自动结束。

Q：VIP 到期后我的数据会丢失吗？
A：不会，你的账号数据完整保留，续费后权益立即恢复。

Q：VIP 标识（ZC）和别人的一样吗？
A：VIP 标识统一显示，仅代表你的 VIP 身份，不代表任何管理权限。

### 50.3 翻译相关

Q：翻译次数什么时候重置？
A：每日 0 点重置，跨天自动恢复额度。

Q：为什么翻译一段长文会被限制？
A：长文消耗的次数更多，请控制单次翻译长度，或拆分翻译。

### 50.4 音乐与下载

Q：音乐页面加载不出歌单？
A：刷新页面重试；仍异常可联系管理员。

Q：下载中心某个资源不见了？
A：资源可能因更新或管理需要下架，请以页面当前展示为准。

### 50.5 客服与申诉

Q：AI 客服为什么不理我了？
A：AI 每日有使用次数限制，用完后需等待次日重置；也可能是 AI 主动结束了本次对话，重新发起即可。

Q：我发了违规内容，账号被临时封禁怎么办？
A：请通过封禁申诉入口提交申诉，说明情况；如属严重违规，AI 可能已自动处理，申诉时如实说明即可。

Q：人工客服不在线怎么办？
A：可以留言，或使用邮件 ziyitstudio@qq.com 联系。

### 50.6 Backrooms 相关

Q：为什么我看不到某些层级？
A：公开列表只展示已收录（审核通过）的层级；待审核稿件仅作者和管理员可见。

Q：我能给别人的层级提意见吗？
A：目前没有直接的评论功能，你可以通过客服反馈给管理员转达。

Q：创建器导出的文件打开是乱码？
A：请使用现代浏览器打开 HTML 文件；乱码多为编码问题，可更换浏览器或重新导出。

---

## 五十一、网站使用场景速查表

### 51.1 按需求找功能

| 你的需求 | 去这里 | 备注 |
| --- | --- | --- |
| 听歌 | 音乐分享板块 | 游客可听，登录体验更完整 |
| 下载资源 | 下载中心 | 部分资源需登录或 VIP |
| 翻译文本 | 巨硬翻译器 | 每日限额，VIP 额度更高 |
| 阅读 Backrooms 层级 | Backrooms 中文维基 | 公开列表仅展示已收录 |
| 创作层级文档 | 层级文档创建器 | 需登录后使用 |
| 投稿文档 | 文档提交页面 | 登录即可 |
| 管理员审核稿件 | 管理后台 → 层级审核 | 仅管理员可见入口 |
| 联系客服 | 在线客服 | AI 客服 24 小时在线 |
| 封禁申诉 | 登录页 → 封禁申诉 | 被封禁后使用 |
| 反馈建议 | 在线客服 / 邮箱 | ziyitstudio@qq.com |
| 查看更新日志 | 开发者日志 / 功能更新记录 | 文档内也有收录 |
| 搜索站内内容 | 站内搜索 | 支持关键词 |
| 搜索全网内容 | 全网搜索 | 聚合结果 |

### 51.2 按角色找功能

**游客**
- 浏览音乐、下载中心、维基内容
- 使用翻译器（有限额度）
- 阅读文档与说明

**注册用户**
- 游客全部功能
- 在线客服
- 层级文档创建器
- 查看/管理自己的层级稿件

**VIP 用户**
- 注册用户全部功能
- 更高翻译额度
- 更完整音乐与下载权益
- 专属 VIP 标识（ZC）

**登录用户（任何已登录账号）**
- 提交新文档（层级 / 实体 / 物品 / 现象）
- 管理自己已提交的稿件

**后室成员（加入成员名单的用户）**
- 成员身份标识与名单相关能力（投稿资格已对所有登录用户开放）

**管理员（按等级分级）**
- 审核层级稿件
- 管理用户、封禁、解封
- 音乐管理、下载管理
- 在线客服（人工客服）
- 后台各管理功能

### 51.3 按问题场景找答案

| 遇到的问题 | 查阅章节 |
| --- | --- |
| 登录失败 / 账号异常 | 二、用户系统；三十五、账号安全与找回指南 |
| 被封禁 | 2.4 账号被封禁怎么办；十四、后台管理 |
| 翻译次数用完 | 八、巨硬翻译器；四十七、翻译器深入指南 |
| 稿件被拒 | 四十六、层级投稿后的常见问题 |
| AI 客服不理我 | 十五、在线客服系统；四十一、在线客服深入指南 |
| 想开通 VIP | 三、VIP 会员体系 |
| 不会写层级 | 二十三、二十六、四十九、五十二 |

### 51.4 新手七天上手建议

**第一天**：浏览网站，了解音乐、下载、翻译等功能，注册账号。
**第二天**：阅读 Backrooms 维基，看看已有的优秀层级。
**第三天**：阅读收录规范，确定想创作的方向。
**第四天**：使用创建器完成第一版草稿。
**第五天**：对照自查清单修改草稿，导出文件。
**第六天**：提交稿件，等待审核。
**第七天**：若被拒，按理由修改重新提交；若通过，完善更多内容。

---

## 五十二、层级文档格式详解（面向写作者）

### 52.1 一篇合格层级文档的标准结构

1. 标题：Level-xxx（名称）
2. 生存难度（用创建器难度组件）
3. 描述：2 段以上，写清环境与氛围
4. 基地、前哨与社区（可选）
5. 实体（如存在）
6. 入口与出口（各 2 条以上建议）

### 52.2 标题写作

- 格式固定为"Level-数字 名称"，例如"Level-706 跨年失败"。
- 名称要短、好记、贴合内容。
- 不要使用"Level-xxxx"这种纯占位编号。

### 52.3 生存难度怎么定

- 简单层级：环境安全，资源充足 → 生存难度 0~1
- 一般层级：有轻微威胁 → 生存难度 2~3
- 危险层级：实体威胁明显 → 生存难度 4~5
- 极危层级：极度危险 → 生存难度 6+ 或特殊难度
- 注意：难度要与内容匹配，审核会重点检查这一点。

### 52.4 描述怎么写

- 先写总体印象（空间、光线、声音、气味）。
- 再写具体区域与结构。
- 补充资源、陷阱、异常现象。
- 最后写实体与危险性。
- 长度建议：300 字以上，越丰富越好，但不要注水。

### 52.5 入口与出口怎么设计

- 入口：说明如何进入该层级（从哪切出、什么条件下）。
- 出口：说明如何离开（切出条件、前往何处）。
- 逻辑要自洽，禁止"出口随机"这种偷懒写法。

### 52.6 文件名规范

- 文件名建议为层级 ID，如"Level-706.html"。
- 大小不超过 1MB。
- 请确保是完整 HTML 文件，打开后排版正常。

### 52.7 常见的扣分项（审核重点）

- 内容过短（少于 200 字）。
- 格式错乱（表格、组件显示异常）。
- 设定与 Backrooms 主世界观冲突。
- 明显的抄袭痕迹。
- 标题/难度与实际内容不符。

### 52.8 文档语言风格

- 使用简洁、严谨的中文。
- 避免口语化、网络用语堆砌。
- 善用小标题分段。
- 允许使用少量图片辅助说明（注意体积）。

---

## 五十三、在线客服深度体验指南

### 53.1 什么时候该找 AI 客服

- 想快速了解网站功能。
- 遇到常见问题（登录、翻译、投稿等）。
- 需要查找某篇文档或资源的位置。
- 反馈建议、留言。

### 53.2 什么时候该找人工客服

- AI 无法解决的特殊问题。
- 涉及账号、封禁、申诉等需要人工处理的事务。
- 举报违规内容。
- 商务合作、资源上架等。

### 53.3 AI 客服的使用礼仪与限制

- 每日有次数限制：普通用户 15 次、VIP 用户 50 次，每日 0 点重置。
- 请一次说清楚问题，避免多次无效提问浪费次数。
- AI 可能会主动结束对话（例如问题已解决或对话偏离主题），这是正常现象，重新发起即可。
- 管理员在对话中如发现严重违规，可能对当前对话用户执行临时封禁，最长 7 天，请遵守社区规范。

### 53.4 人工客服会话说明

- 提交问题后，人工客服接入即可对话。
- 人工客服也可以主动结束对话。
- 对话结束后，如果还有问题，可以重新发起。
- 未及时回复时请耐心等待，客服可能同时在处理其他会话。

### 53.5 被封禁后的申诉

- 登录时被提示已封禁，可以点击"封禁申诉"进入申诉流程。
- 按提示输入用户名和密码验证身份。
- 验证通过后进入申诉对话页，说明申诉理由。
- 管理员处理后会给出结论；认可申诉的会解除封禁。
- 申诉会话同样支持对话，处理完成后会话结束。

### 53.6 让客服更快解决问题的技巧

1. 一句话说清楚问题（什么功能 + 什么现象 + 期望结果）。
2. 附上必要的截图或页面地址（如果方便）。
3. 涉及账号时，说明账号名（注意保护密码，不要发密码）。
4. 涉及投稿时，提供层级 ID 和名称。

### 53.7 常见对话模板

**问功能**：你好，我想了解一下怎样提交一个 Backrooms 层级？
**反馈问题**：你好，我在使用翻译器时提示次数用完，能帮忙看看吗？
**申诉**：你好，我的账号被封了，我想申诉，我登录的时候点了封禁申诉进入的这里。
**建议**：你好，建议音乐板块增加一个排行榜功能。

---

## 五十四、网站安全与隐私说明

### 54.1 我们如何保护你的账号

- 登录凭据经过加密处理后传输。
- 密码不会以明文形式展示或存储。
- 请勿将账号借给他人使用。
- 发现异常登录时建议尽快修改密码。

### 54.2 哪些信息会被记录

- 必要的账号信息（用户名、ID 等）。
- 功能使用记录（如翻译次数、投稿记录）用于额度与统计。
- 登录与安全相关日志用于异常检测。

### 54.3 你可以做什么来保护自己

- 使用独立、复杂的密码，不要与其他网站相同。
- 不要向任何人（包括自称客服的人）透露密码。
- 谨慎处理陌生链接与文件。
- 离开公共电脑时记得退出登录。

### 54.4 关于封禁与黑名单

- 违规行为可能导致账号或 IP 被封禁。
- 封禁分临时（可指定时长）与永久两种。
- 账号被封禁后可通过申诉流程申请解封。
- 请遵守网站使用规范，共同维护良好的社区环境。

### 54.5 关于内容的版权

- 本站收录的层级文档版权归原作者所有。
- 请勿未经授权转载他人作品。
- 创作者投稿即视为同意在站内展示与收录。
- 如有版权争议，请联系 ziyitstudio@qq.com。

---

## 五十五、全功能索引表

### 55.1 功能总览表

| 功能名称 | 入口位置 | 是否需登录 | 面向用户 | 相关章节 |
| --- | --- | --- | --- | --- |
| 注册 / 登录 | 用户系统 | 否（注册需填写信息） | 所有人 | 二 |
| 音乐分享 | 音乐板块 | 否（游客可听） | 所有人 | 五、四十 |
| 下载中心 | 下载板块 | 视资源而定 | 所有人 | 六、四十 |
| 巨硬翻译器 | 翻译器 | 否 | 所有人 | 八、三十二、四十七 |
| Backrooms 阅读 | 中文维基 | 否 | 所有人 | 九、三十一、三十九 |
| 层级创建器 | 文档创建器 | 是 | 注册用户 | 九、二十三、二十六 |
| 文档提交 | 提交页面 | 是（登录即可） | 投稿者 | 九、四十九 |
| 层级审核 | 管理后台 | 是（管理员） | 管理员 | 九、二十八 |
| 在线客服 | 客服入口 | 否（推荐登录） | 所有人 | 十五、四十一、五十三 |
| 封禁申诉 | 登录页/申诉页 | 身份验证 | 被封禁用户 | 五十三 |
| 站内搜索 | 搜索栏 | 否 | 所有人 | 十一 |
| 全网搜索 | 搜索栏 | 否 | 所有人 | 十一 |
| 开发者日志 | 日志页面 | 否 | 所有人 | 十三 |
| 后台管理 | 管理后台 | 是（管理员） | 管理员 | 十四、二十八 |

### 55.2 每日限额汇总

| 功能 | 普通用户 | VIP 用户 | 管理员参考 | 重置时间 |
| --- | --- | --- | --- | --- |
| 翻译器 | 每日有限 | 每日更高 | 以实际等级为准 | 每日 0 点 |
| AI 客服 | 15 次/日 | 50 次/日 | 按等级 20~100 不等，高等级不限 | 每日 0 点 |
| 文档投稿 | 不限（需登录） | 不限 | — | — |

说明：以上数字可能随运营调整，以页面实际显示为准。

### 55.3 重要状态说明

**账号状态**
- 正常：可正常使用全部权限内功能。
- 已封禁：登录受限，显示封禁原因与剩余时间；可申诉。
- 未激活：注册后需按要求完成验证（如 Phantom 人机验证）。

**稿件状态**
- 待审核：已提交，等待管理员审核。
- 已收录：审核通过，公开可见。
- 已拒绝：未通过，可修改后重新提交。
- 已重写：管理员下架并保留原档，可在其基础上继续修改。

**对话状态（客服）**
- 进行中：等待回复或正在对话。
- 已结束：对话结束（AI、人工或用户本人均可结束）。

### 55.4 关键约定

- ZC / zC 标识 = VIP 身份标识，不是管理员。
- 管理员身份由后台等级系统独立决定，与 VIP 无关。
- AI 客服可以主动结束对话。
- 管理员和 AI 均可结束对话，界面会区分结束方。
- 封禁支持定时与永久两种；账号被封可申诉。
- 所有限额按自然日重置，不跨天累计。

### 55.5 结语

ZIYIT STUDIO 致力于为每一位用户提供稳定、好用、开放的服务。本文档面向所有使用者，只讲"怎么用"，帮助大家快速上手。如果还有未覆盖的问题，欢迎通过在线客服或邮箱 ziyitstudio@qq.com 反馈，我们会持续完善。

感谢你的阅读与支持！

---

## 五十六、补充常见问题（第六辑）

### 56.1 账号与登录

Q：为什么我注册完还要做人机验证？
A：这是网站的安全机制（Phantom），用于防止机器人批量注册和恶意刷取，验证一次即可，不影响后续使用。

Q：忘记密码了怎么办？
A：目前请通过在线客服或邮件 ziyitstudio@qq.com 联系管理员协助处理，联系时请提供账号名，方便核实身份。

Q：我的账号显示"未激活"是什么意思？
A：说明账号还没有完成必要的验证流程，按页面提示完成验证即可正常使用。

Q：为什么换个设备登录要重新验证？
A：设备环境变化时，出于安全考虑需要重新确认身份，属于正常现象。

### 56.2 功能使用

Q：翻译器和 AI 客服的剩余次数在哪里看？
A：在对应功能的界面上会显示剩余次数或额度提示，用完会给出明确提示。

Q：音乐播放列表可以收藏吗？
A：以当前音乐板块实际功能为准；如有收藏需求，可通过客服反馈建议。

Q：下载的资源需要付费吗？
A：大部分资源免费；部分资源可能需要登录或 VIP 权限，以页面提示为准。

Q：站内搜索和全网搜索有什么区别？
A：站内搜索只搜本站内容；全网搜索会聚合外部结果，适合查找站外资料。

### 56.3 Backrooms 投稿

Q：我不是后室成员，能投稿吗？
A：可以，投稿只需要登录（不再要求后室成员身份）；成员身份不再影响投稿权限，想加入成员名单可走「加入申请」。

Q：一个账号可以投稿多个层级吗？
A：可以，投稿数量不限，但每个稿件都需要单独提交并审核。

Q：投稿后能撤回吗？
A：在"我的层级"中可以管理自己的稿件；审核通过后如需下架，请联系管理员。

Q：审核会收钱吗？
A：不会，审核完全免费，也没有加急付费渠道，请勿轻信任何收费承诺。

### 56.4 封禁与申诉

Q：被永久封禁还能解封吗？
A：可以通过申诉流程说明情况，由管理员判断是否解封；严重违规通常无法解封。

Q：封禁申诉提交后多久有结果？
A：人工处理，时间不定；请耐心等待，也可通过其他渠道联系管理员。

Q：申诉对话也会被 AI 处理吗？
A：申诉由人工管理员处理，处理过程中会与你对话确认情况。

### 56.5 其他

Q：网站会一直免费吗？
A：基础功能长期免费；部分增值权益（如 VIP）按运营策略提供，是否开通由你决定。

Q：网站更新会不会清空我的数据？
A：不会，账号与投稿数据都会保留。

Q：我想加入 ZIYIT 团队帮忙维护怎么办？
A：欢迎通过 ziyitstudio@qq.com 联系，说明你的想法与特长。

Q：文档里没写到的功能怎么办？
A：以网站实际功能为准；如有疑问，欢迎通过在线客服或邮箱咨询。

---

## 五十七、老用户常见认知误区澄清

### 57.1 关于 ZC 标识

- 误区：ZC 标识 = 管理员。
- 事实：ZC / zC 是 VIP 身份的标识，仅代表该用户开通了 VIP，不代表任何后台管理权限。
- 管理员身份由网站后台的等级系统独立决定，与是否拥有 ZC 标识没有任何关系。
- 请勿因为某个用户带有 ZC 标识就认为其拥有管理权限，也不要向其索要管理相关服务。

### 57.2 关于 AI 客服

- 误区：AI 客服只会回答问题，不会主动做任何事。
- 事实：AI 客服具备一定的自主行为：
  - AI 可以主动结束对话（例如问题已解决或对话不适合继续时）。
  - AI 在发现严重违规时，可以对当前对话用户执行临时封禁（最长 7 天）。
  - AI 无法封禁管理员，也只会针对当前对话的用户进行限制。
  - AI 每日有次数限额，按身份分级：普通 15 次 / VIP 50 次；管理员按等级 20~100 次不等，高等级不限。
- 这些行为是系统设计的一部分，属于正常现象。

### 57.3 关于对话结束

- 误区：对话结束一定是"系统/用户"结束的。
- 事实：对话可以由任意一方结束：
  - AI 主动结束 → 界面显示"AI 已结束本次对话"。
  - 人工客服结束 → 界面显示"人工客服已结束本次对话"。
  - 用户自己结束 → 界面显示"您已结束本次对话"。
- 对话结束后重新发起即可，不影响再次使用。

### 57.4 关于封禁

- 误区：封禁都是永久封禁。
- 事实：封禁支持定时与永久两种：
  - 定时封禁：管理员或 AI 指定时长（如若干分钟、若干天），到期自动解封。
  - 永久封禁：不指定时长，需要管理员手动处理。
  - 被封禁后界面会显示封禁原因与到期时间（永久则显示"永久封禁"）。
  - 无论哪种封禁，都可以通过封禁申诉流程申请解封。

### 57.5 关于层级审核

- 误区：投稿后马上就能看到自己的层级。
- 事实：投稿后稿件进入"待审核"状态，只有作者自己和管理员可以看到；审核通过后才会公开。
- 误区：审核是机器自动完成的。
- 事实：审核由管理员人工完成，分为基础审核与高级审核两个阶段，时间不定。

### 57.6 关于翻译与限额

- 误区：次数用完换个账号就能继续用。
- 事实：限额按账号计算，用完后需等待次日重置；这是为了保障服务的稳定与公平。

### 57.7 关于账号安全

- 误区：把账号借给朋友使用没关系。
- 事实：账号与他人共享可能触发安全保护，且他人使用你账号产生的违规行为会记录在你的账号下，风险由你自己承担，请务必保管好账号。

### 57.8 关于投稿与收录

- 误区：提交后稿件就永久属于网站。
- 事实：稿件版权归作者本人所有，网站仅在本站范围内展示与收录；作者可联系管理员下架。
- 误区：已收录的层级不能再修改。
- 事实：作者可以上传新版本更新自己的层级，旧版本会保留存档。

### 57.9 关于联系方式

- 官方邮箱：ziyitstudio@qq.com
- 在线客服：网站内直接发起
- 请通过官方渠道反馈问题，谨防冒充官方人员的诈骗信息。

### 57.10 最后的话

ZIYIT STUDIO 的每一项功能都在持续演进，文档中的说明可能与最新版本存在细微差异，请以网站实际体验为准。如果你在使用中发现文档与实际情况不符，欢迎反馈，我们会及时修正。祝你在 ZIYIT 玩得开心，也期待看到你的优秀层级作品。

### 57.11 文档使用说明

- 本文档按章节组织，阅读时可直接使用页面内的目录快速跳转。
- 各章节相对独立，可按需查阅，无需从头读到尾。
- 涉及"限额""价格"等动态信息的内容，以网站页面实际展示为准。
- 如文档与页面不一致，请优先以页面实际功能为准，并欢迎向我们反馈以便修正。
- 本文档最后更新于网站近期版本，完整章节请以目录为准，祝你使用愉快。感谢阅读与反馈，我们下次再见，常来玩！

（完）

---

## 五十八、RC BUG 反馈系统（含前端 / RC 端对接要求）

> 本章对应后端 **v0.3.17** 新增的 RC BUG 反馈系统（**v0.3.21** 起新增 RCVE 漏洞编号：确定解决版本后由系统强制编号并加在标题前）。
> 后端接口与 Markdown 成文逻辑**已全部实现并部署**；前端（RC 网页、管理员后台）与 RC 软件客户端按本章要求对接即可。
> 说明：已有的 RC 软件问题报告仍在 `H:\Code\Python\Release_control\BUGs` 目录（《漏洞说明报告》系列）。本系统**只读取其排版格式**用于生成新报告，**不读取、不上传、不修改**那些既有文件。

### 58.1 功能概述

RC 的 BUG 反馈由**三个入口**提交、汇入**同一个公开列表**：

| 入口 | 位置 | 凭据形式 | `source` |
| --- | --- | --- | --- |
| RC 网页 | `download/RC.html` | `Authorization: Bearer <JWT>` | `web` |
| 管理员后台 | 后台管理页面 | `Authorization: Bearer <JWT>`（管理员） | `admin` |
| RC 软件 | RC 客户端「反馈 / 报告 BUG」 | 许可证 `username` + `key_hash` | `rc` |

- `GET /rc/bugs` **公开**（无需登录）：任何访客都能在 RC 网页看到已知 BUG 列表，**列表展示提交者**（用户名 + 用户ID）。
- 每条反馈都会被后端**自动成文**为 Markdown 报告（对齐《漏洞说明报告》排版），随接口回包，前端 `marked` 转 HTML 显示。
- 提交者由**服务端**从凭据解析，客户端不能自称身份；提交者是否管理员同样由服务端判定。

### 58.2 状态机（7 个状态）

| 状态 key | 显示名 | 说明 |
| --- | --- | --- |
| `pending` | 正在审批 | **提交后的默认状态**；普通用户提交后若管理员不改，就一直保持此状态 |
| `reviewing` | 正在重新审批 | 需要重新走一遍审批时 |
| `planned` | 正在规划 | 确认为问题，已排入计划 |
| `fixing` | 正在修复 | 正在处理 |
| `fixed` | 已修复 | 已修复 |
| `not_a_bug` | 无问题 | 经核实不是问题 |
| `wontfix` | 永不修复 | 确认存在但不会修复 |

- **只有管理员（Lv.3+）能改状态**，普通用户只能提交。
- 每次改状态会追加一条**状态变更记录**（时间 / 状态 / 操作人 / 说明），并重写该条的 Markdown 报告。
- 状态枚举随 `GET /rc/bugs` 回包一起下发（`statuses` 字段），**前端不要写死中文**，按下发的 `key` / `label` 渲染筛选与徽标。

### 58.3 数据存储与部署隔离

- 结构化数据：`rc_bugs.json`（**数据文件**，不在「只搬 .py / .md」的部署同步范围内）。
- Markdown 报告：`rc_bugs/<编号>-BUG反馈报告.md`（文件名形如 `RCFB-0001-BUG反馈报告.md`）。
- `rc_bugs/` 目录已被部署脚本 `sync_ziyit_to_prod.py` 列入 `SYNC_ONLY_SKIP_DIRS`：**只跳过同步、仍然参与备份** —— 正式环境的报告属于正式环境自己的数据，不能被测试环境覆盖。

#### 58.3.1 两套编号：反馈编号 `RCFB-` 与漏洞编号 `RCVE-`

| 编号 | 何时产生 | 形态 | 谁来定 |
| --- | --- | --- | --- |
| **反馈编号** `code` | 提交即分配，**永久不变**（与是否修复无关） | 自增整数 `id`，`RCFB-<id 补零到 4 位>`，如 `RCFB-0007` | 系统 |
| **漏洞编号** `rcveCode` | **管理员确定「解决版本」后**才分配 | `RCVE-出现版本-解决版本-该解决版本的BUG编号`，如 `RCVE-26.9-26.9-001` | 系统 |

- **编号由系统定，用户不能自己加**：提交时标题字段里若自己写了 `[RCVE-…]` 前缀，服务端会**去掉**；对外标题由服务端按 `[漏洞编号] 原标题` 拼装（见 58.5 的 `title` / `baseTitle`）。
- **出现版本**：默认取提交时「涉及版本」的**版本段**（`26.9-snapshot-26W35A` → `26.9`）；管理员调查后可通过 `PATCH` 修改。带快照后缀也能正确归属（编号里第一段是出现版本、最后一段是序号，中间整段都是解决版本，故解决版本本身可含连字符，如 `26.7-snapshot-26W26B`）。
- **解决版本**：由管理员在确认修复后填写（如 `26.9` 或 `26.9-snapshot-26W35A`）；**填了就分配编号，分配后不可清空**。
- **第三段按解决版本分段自增**：同一解决版本内从 `001` 起（`26.9` 修的就是 `26.9` 段的编号，`26.8` 修的是 `26.8` 段的编号）；换解决版本重新从 `001` 起。出现版本不同不影响归段。
- 编号**一旦分配不再变更**（只改状态不会动编号；管理员修正出现版本时只换第一段，保留原序号）。
- 兼容旧数据：没有这些字段的历史记录，后端读取时会从标题里的 `[RCVE-…]` 前缀反推编号与出现 / 解决版本，标题与编号都不会变。

### 58.4 接口清单

| 方法 | 路径 | 权限 | 用途 |
| --- | --- | --- | --- |
| `GET` | `/rc/bugs` | 公开 | 已知 BUG 列表 + 状态枚举 |
| `GET` | `/rc/bugs/{bug_id}` | 公开 | 单条反馈详情 |
| `POST` | `/rc/bugs` | 需凭据（JWT 或 username+key_hash） | 提交反馈 |
| `GET` | `/admin/rc/bugs` | 管理员 Lv.3+ | 管理视图（额外含联系方式） |
| `PATCH` | `/admin/rc/bugs/{bug_id}` | 管理员 Lv.3+ | 修改状态 / 填「出现版本、解决版本」（自动编号） |

#### 58.4.1 `GET /rc/bugs`（公开）

回包结构：

```json
{
  "count": 12,
  "statuses":   [{ "key": "pending", "label": "正在审批" }],
  "severities": [{ "key": "low", "label": "低" }],
  "sources":    [{ "key": "web", "label": "RC 网页" }],
  "statusCounts": { "pending": 3, "fixed": 9 },
  "bugs": [ { "...": "见 58.5" } ]
}
```

- `bugs` 按 `id` **从新到旧**排列。
- `statusCounts` 可直接用于筛选标签上的计数。
- 回包**不含联系方式**（`contact` 只在管理员视图出现）。

#### 58.4.2 `GET /rc/bugs/{bug_id}`（公开）

- 命中回单条对象（结构同 58.5）；不存在回 **404**，`detail = "未找到该反馈编号"`。

#### 58.4.3 `POST /rc/bugs`（提交）

请求体（`Content-Type: application/json`）：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `title` | ✅ | 问题标题，最长 **120** 字 |
| `description` | ✅ | 问题描述，最长 **4000** 字 |
| `version` | ｜ | 涉及版本，如 `26.9-snapshot-26W35A`，最长 100 字 |
| `module` | ｜ | 涉及模块 / 功能，最长 100 字 |
| `severity` | ｜ | `low` / `medium` / `high` / `critical`，**默认 `medium`**；非法值回落 `medium` |
| `steps` | ｜ | 复现步骤，最长 3000 字 |
| `expected` | ｜ | 期望结果，最长 3000 字 |
| `actual` | ｜ | 实际结果，最长 3000 字 |
| `extra` | ｜ | 补充信息（系统信息、日志等），最长 3000 字 |
| `source` | ｜ | `web` / `admin` / `rc`；**不传时由服务端按凭据类型推断** |
| `contact` | ｜ | 联系方式，最长 120 字；**只在管理员视图可见**，不出现在公开列表 |
| `username` | RC 端必填 | 许可证用户名（网页端不要传） |
| `key_hash` | RC 端必填 | 许可证 keyHash（网页端不要传） |

凭据（二选一）：

- 网页端 / 管理员后台：`Authorization: Bearer <登录后的 JWT>`
- RC 软件端：请求体带 `username` + `key_hash`（与 RC 激活校验同一套）

服务端行为：

1. 按 IP 限流：**每 IP 每 5 分钟最多 5 条**，超出回 **429**，`detail` 形如「提交过于频繁：每 IP 每 5 分钟最多提交 5 条反馈」。
2. 无有效凭据 → **401**，`detail = "提交反馈需要登录凭据（Bearer token 或 username+key_hash）"`。
3. 账号被封禁 → **403**。
4. `title` / `description` 为空 → **400**，`detail = "请填写问题标题"` / `"请填写问题描述"`。
5. **标题里不许自带编号**：服务端会去掉 `title` 开头的 `[RCVE-…]` 前缀（编号只能由系统在确定解决版本后分配，见 58.3.1）。提交时**标题不带前缀**，`rcveCode` 为 `null`。
6. 通过后分配编号（`code = RCFB-####`）、出现版本取「涉及版本」的版本段、状态置 `pending`、写 `rc_bugs.json` + 生成 Markdown 报告、写审计日志。

成功回包：

```json
{
  "message": "已提交，反馈编号 RCFB-0001，当前状态：正在审批",
  "bug": { "...": "见 58.5" }
}
```

#### 58.4.4 `GET /admin/rc/bugs`（管理员 Lv.3+）

- 结构同上（`count` + `bugs`），但每条**额外带 `contact`**。

#### 58.4.5 `PATCH /admin/rc/bugs/{bug_id}`（管理员 Lv.3+）

请求体（字段都是可选的，但**至少要传一个**，否则回 **400**「没有要更新的内容…」）：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `status` | ｜ | 7 个状态 key 之一（见 58.2）；**不传表示只改版本信息、状态保持不变** |
| `note` | ｜ | 附一句说明，最长 3000 字；会写进报告的变更记录 |
| `appearedVersion` | ｜ | **出现版本**（如 `26.9`）；不传不变。默认取提交时「涉及版本」的版本段，管理员调查后可修正 |
| `fixedVersion` | ｜ | **解决版本**（如 `26.9`、`26.9-snapshot-26W35A`）；**填了即由系统分配 `RCVE-出现版本-解决版本-序号` 编号并把 `[编号]` 强制加在标题前**，分配后不可清空、编号本身不会变 |

- `status` 非法 → **400**，`detail` 会列出全部可选中文名。
- 编号不存在 → **404**，`detail = "未找到该反馈编号"`。
- **只填 `fixedVersion` 而该条没有出现版本** → **400**，`detail = "请先填「出现版本」（编号格式为 RCVE-出现版本-解决版本-该解决版本的BUG编号）"`（把出现版本一起传即可）。
- 该条**已分配编号**后再传空白 `fixedVersion` → **400**「该反馈已分配 RCVE 编号，解决版本不可清空」。
- 分段自增与「编号不再变更」的细节见 58.3.1。
- 成功回包：`{ "message": "RCFB-0007 状态已改为「已修复」，漏洞编号 RCVE-26.9-26.9-001", "bug": { ... } }`（只改版本时为「RCFB-0007 已更新版本信息，漏洞编号 …」）。

### 58.5 单条反馈对象（`bug` / `bugs[]` 的字段）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int | 内部自增编号 |
| `code` | string | **反馈编号** `RCFB-0007`（提交即分配，永久不变） |
| `title` | string | **对外标题**：有漏洞编号时为 `[RCVE-出现版本-解决版本-序号] 原标题`，否则就是原标题。由服务端拼装，前端直接渲染即可 |
| `baseTitle` | string | 原标题（不含编号前缀），管理员后台编辑标题时用这个 |
| `rcveCode` | string \| null | **漏洞编号** `RCVE-26.9-26.9-001`；**未确定解决版本时为 `null`** |
| `appearedVersion` | string \| null | **出现版本**（默认取提交时「涉及版本」的版本段，如 `26.9`） |
| `fixedVersion` | string \| null | **解决版本**；未确定时为 `null` |
| `version` | string \| null | 提交时填的「涉及版本」（原始文本，未归一化） |
| `module` | string \| null | 涉及模块 |
| `severity` / `severityText` | string | 危险等级 key / 中文 |
| `status` / `statusText` | string | 状态 key / 中文 |
| `source` / `sourceText` | string | 来源 key / 中文 |
| `submitUserId` | int | 提交者用户ID |
| `submitUsername` | string | 提交者用户名 |
| `submitterIsAdmin` | bool | 提交者是否为管理员 |
| `submittedAt` / `submittedAtText` | string | 提交时间（ISO / 北京时间「YYYY-MM-DD HH:MM」） |
| `updatedAt` / `updatedAtText` | string | 最近更新（同上） |
| `lastStatusBy` | string \| null | 最后改状态的管理员用户名 |
| `statusHistory` | array | 状态变更记录 `[{at, status, by, note}]` |
| `markdown` | string | **该条的完整 Markdown 报告正文**（前端 `marked` 转 HTML） |

> 公开接口**不返回** `contact`；管理员接口额外返回 `contact`。提交时间显示一律以北京时间（UTC+8）为准。

### 58.6 Markdown 报告格式

后端按《漏洞说明报告》排版生成，结构如下（`_rc_bug_write_md` 落盘一份，接口同时回 `markdown` 字段）：

```markdown
# Release Control BUG 反馈报告

| 项目 | 内容 |
| --- | --- |
| 反馈编号 | RCFB-0007 |
| 漏洞编号 | RCVE-26.9-26.9-001（未编号（确定解决版本后由系统分配）） |
| 反馈标题 | ……（原标题，不含编号前缀） |
| 提交者 | 用户名（用户ID 7 · 普通用户） |
| 提交来源 | RC 网页 |
| 提交时间 | 2026-09-24 15:20 |
| 涉及版本 | 26.9-snapshot-26W35A |
| 出现版本 | 26.9（未确定为「未确定」） |
| 解决版本 | 26.9（未确定为「未确定」） |
| 涉及模块 | MOD 管理 |
| 危险等级 | 高 |
| 当前状态 | 正在审批 |
| 最近更新 | 2026-09-24 15:20 |

> 说明：本报告由 ZIYIT 网站后端自动生成……

***

## 1. 问题描述
## 2. 复现步骤
## 3. 期望结果
## 4. 实际结果
## 5. 补充信息

## 附录：状态变更记录
| 时间 | 状态 | 操作人 | 说明 |
```

- 报告里所有时间均为**北京时间**。
- 用户输入在入库时已做**基础 HTML 转义**（`&` `<` `>`）并去除控制字符、超长截断（截断处追加「…（内容过长，已截断）」）。
- ⚠ **基础转义不等于安全**：Markdown 允许多种绕过写法，前端把 `markdown` 转 HTML 时**必须再做一次白名单净化**（见 58.7）。

### 58.7 安全要求（前后端共同遵守）

- **凭据必填，身份服务端定**：`POST /rc/bugs` 的提交者一律由服务端从凭据解析；前端**不要**自行传「提交者」字段，也不要展示客户端自称的身份。
- **MD → HTML 必须净化**：前端用 `marked.parse(markdown)` 后，务必经 **DOMPurify**（或等价白名单库）净化再 `innerHTML`。仅依赖服务端转义不够。
- **联系方式只在管理员视图**：`contact` 不出现在公开列表；前端公开页面不要尝试渲染该字段。
- **限流与错误**：429 时按 `detail` 文案提示用户稍后再试；401 时引导登录（RC 网页）/ 检查许可证（RC 软件端）。

### 58.8 给前端的要求（RC 网页 `download/RC.html` + 管理员后台）

> 前后端分属不同编写者，以下仅为**对接要求**。前端仓库里既有文件由前端维护者处理，后端不代改。

1. **已知 BUG 列表（RC 网页，公开）**
   - 调 `GET /rc/bugs`，渲染列表：编号 `code`、标题、**提交者**（`submitUsername` + `submitUserId`）、`statusText` 状态徽标、`sourceText` 来源、`severityText` 危险等级、`submittedAtText` 提交时间、`updatedAtText` 最近更新。
   - **标题直接渲染 `title`**：它由服务端拼好，**已修复/已定版本的条目会自动带 `[RCVE-出现版本-解决版本-序号]` 前缀**（如 `[RCVE-26.9-26.9-001] 管理员权限运行时 MOD/DLC 管理界面拖放安装功能失效`），**前端不要自己拼编号**，也不要拼 `[` `]`；未定版本的条目 `title` 就是纯标题、`rcveCode` 为 `null`。
   - `code`（`RCFB-####`）是**永久不变的反馈编号**，适合放在列表首行与「查看详情」链接上；想额外显示漏洞编号就用 `rcveCode`（为空时不显示那一行）。**别把 `code` 也换成 RCVE**：两者是两套编号，见 58.3.1。
   - **筛选/统计项不要写死中文**：用回包里的 `statuses` / `severities` / `sources` 生成选项，计数用 `statusCounts`。
   - 点击某条查看详情：可调 `GET /rc/bugs/{id}` 或用列表里的 `markdown`。
2. **Markdown 渲染**
   - RC.html **已引入** `https://cdn.jsdelivr.net/npm/marked/marked.min.js`，直接 `marked.parse(item.markdown)` 即可，**必须**再经 DOMPurify 净化后再插入 DOM。
3. **提交表单（RC 网页）**
   - 表单字段与 58.4.3 一致：标题、问题描述、涉及版本、涉及模块、危险等级、复现步骤、期望结果、实际结果、补充信息（联系方式可选）。
   - **不要提供「编号 / RCVE 编号」输入框**，也**不要在前端给标题加任何前缀**：编号是系统定的（提交时是 `RCFB-####`，确定解决版本后才由系统分配 `RCVE-…` 并加在标题前）。用户就算自己写上 `[RCVE-…]`，服务端也会去掉，不必前端拦。
   - 请求头带 `Authorization: Bearer <JWT>`；`source` 可省略（服务端按登录身份推断 `web` / `admin`）。
   - 成功后提示回包的 `message` 并刷新列表；429 / 401 按 `detail` 提示；「涉及版本」建议给出手填提示（如 `26.9-snapshot-26W35A`），它决定默认的**出现版本**。
4. **管理员后台**
   - 「RC BUG 反馈」区块调 `GET /admin/rc/bugs`（Lv.3+），表格展示全部字段并**额外展示 `contact`**。
   - 每条提供状态下拉（7 态，用 `PATCH /admin/rc/bugs/{id}`），可附 `note`；成功后提示 `message` 并刷新。
   - 状态可选项建议取自 `GET /rc/bugs` 的 `statuses`，避免前端与后端枚举漂移。
   - **新增两个输入框**：出现版本（`appearedVersion`，默认已有，可修正）、解决版本（`fixedVersion`，确认修复后填写）。提交 `PATCH` 时把改过的字段一起传（`status` 可省）；填了解决版本后回包 `message` 会带**系统分配的漏洞编号**，把它提示给管理员即可。只填解决版本而该条没有出现版本会被 **400** 挡下（提示文案直接用 `detail`）。
   - 对应的接口封装要跟着改：`assets/ziyit_api.js` 里的 `adminRcBugStatus(bugId, status, note)` 目前只传 `status` / `note`，需能再传 `appearedVersion` / `fixedVersion`（例如改成 `adminRcBugStatus(bugId, opts)`，`opts` 里空字段不传）。列表里可直接用回包的 `rcveCode` / `appearedVersion` / `fixedVersion` 回填输入框（`rcveCode` 为空表示还没编号）。
5. **待补的接口封装（前端 `assets/ziyit_api.js`）**
   - 现有页面 `user/rc-key.html`、`user/VIP.html` 已在调用 `ZIYIT_API.rcMyKeys()` 与 `ZIYIT_API.afdianSelfCheck()`，但该文件里**这两个封装尚不存在**（会报错）。请补上：
     - `rcMyKeys()` → `GET /rc/keys/mine`（需登录）：回 `{ userId, username, keys: [ ... ] }`，每把密钥含 `productKey`（明文）、`permission` / `permissionName`、`activatedAt`、`expireAt`、`permanent`、`expired`、`source`、`planId`。
     - `afdianSelfCheck()` → `POST /afdian/self-check`（需登录、无请求体）：服务端主动拉最近订单并补发 VIP / RC 密钥 / DLC；回 `{ message, scanned, granted, rcKeys, ...VIP 状态 }`。**有冷却**（默认 60 秒，服务端可配），过于频繁回 **429** 并带「请 N 秒后再试」文案，前端需按 `detail` 提示。
   - 参考：本次同批新增的 `myDlc()` → `GET /dlc/mine` 已是正确写法，可照抄风格。

### 58.9 给 RC 软件端的要求

1. **提交入口**：RC 客户端内提供「反馈 BUG」入口，字段同上（标题、描述、版本、模块、危险等级、复现步骤、期望结果、实际结果、补充信息）。
2. **凭据**：必须带上当前用户的许可证 `username` 与 `key_hash`（`keyHash`），放在请求体里；**不要**传 `source`（服务端会推断为 `rc`）。
3. **版本信息**：把当前 RC 版本号（如 `26.9-snapshot-26W35A`）自动填入 `version`，减少用户手填出错。该字段决定默认的**出现版本**（取版本段 `26.9`），是 RCVE 编号的第一段。
4. **标题里不要写编号**：`[RCVE-出现版本-解决版本-序号]` 由系统在管理员确定解决版本后分配并加在标题前，客户端**不要**自己拼、也不要在界面上让用户填；用户在标题里写了也会被服务端去掉。RC 端只需要把「问题标题」原样提交。
5. **结果处理**：成功时把回包 `message`（含反馈编号 `RCFB-####`）展示给用户并可复制；429 时提示稍后再试；凭证失效时按既有激活流程引导重新激活。
6. **不要在客户端生成/上传 Markdown 报告**：报告一律由服务端成文，客户端只提交结构化字段。

### 58.10 与既有《漏洞说明报告》的关系

- `H:\Code\Python\Release_control\BUGs` 下的《漏洞说明报告》是**历史存档**：本系统只借鉴其**排版格式**，**不读取、不上传、不修改**其中任何文件。
- 新反馈统一走本系统（公开列表 + `rc_bugs/` 报告目录），沿用**同一套漏洞编号命名规则**：提交时对外编号是 `RCFB-####`（反馈编号，永久不变），**管理员确定解决版本后**由系统分配 `RCVE-出现版本-解决版本-该解决版本的BUG编号`（如 `RCVE-26.9-26.9-001`），并把 `[编号]` 强制加在标题前；既有 `RCVE-` 编号段（26.7 / 26.8 / 26.9）继续按段续号，不会重号。

---

## 五十九、Backrooms 四类文档系统（含前端对接要求）

> 本章对应后端 **v0.3.19**：文档类型由「层级」一类扩展为**四类**（层级 / 实体 / 物品 / 现象），并把全站维基文档**全量迁移到后端存储**。
> 后端接口与迁移工具**已全部实现并部署**；前端页面按本章要求改从后端接口取正文与编写者信息。

### 59.1 四类文档与接口前缀

| 类型 key | 中文 | 接口前缀 | ID 前缀 | ID 示例 | 强制标识 |
| --- | --- | --- | --- | --- | --- |
| `level` | 层级 | `/backrooms/levels` | `Level-` | `Level-706` | 无 |
| `entity` | 实体 | `/backrooms/entities` | `Entity-` | `Entity-13` | **必须带 IETS 实体分级标识** |
| `object` | 物品 | `/backrooms/objects` | `Object-` | `Object-6` | 无 |
| `phenomenon` | 现象 | `/backrooms/phenomena` | `Phenomenon-` | `Phenomenon-34` | **必须带 IPTS 现象分级标识** |

- 文档类型**由接口前缀决定**，提交 / 修改表单里**不用传 `type`**。
- 四类文档**共用同一份索引**（`backrooms_levels/index.json`）与同一套状态机、同一套文件目录，仅靠记录里的 `type` 字段区分。因此「**非层级文档上传后出现在对应分类列表**」是天然的：`GET /backrooms/entities` 只会列出 `type = entity` 的记录。
- 传错前缀按**不存在**处理：`GET /backrooms/entities/Level-1` 返回 404（不会跨类型读到层级文档）。
- 顺带说明：`normal-levels`（常规层级 / 子区集）、`entities`、`objects`、`phenomena` 这些**分级说明页与索引页不进文档库**；`the-m-e-g`、`groups` 这类**组织类文档也不迁移**。

### 59.2 ID / 名称 / 强制标识校验

- **ID**：不能为空，且必须以对应前缀开头（大小写不敏感）；不符 → **400**，`detail` 形如「实体 ID 必须以 Entity- 开头（如 Entity-13）」。
- **名称**：不能为空。
- **文件**：只支持 `.html`，单文件 **≤ 1MB**（超限 → **413**；后缀不符 → **400**）。
- **强制标识（实体 / 现象）**：正文**任意位置命中**即算通过，两种写法都认：
  - 分级声明：`IETS：2E` / `IPTS：3B+`（中英文冒号均可，分级为 1–4 位字母数字与 `+ - ?`）；
  - 分级块容器 class：`iets-wrapper` / `iets-before`（现象同理 `ipts-wrapper` / `ipts-before`）。
  - 未检测到 → **400**，`detail` 形如「实体文档必须携带IETS 实体分级标识：正文中未检测到 IETS 标识（写法如 IETS：2E / IETS-wrapper）」。
- **修改（`PUT`）时同样校验标识**：实体的新稿也必须带 IETS、现象的新稿也必须带 IPTS。

### 59.3 编写者信息（实时更新）

- 索引里**只存 `ownerId`**；**用户名与头像每次请求实时从 `users.json` 解析** —— 用户改名 / 换头像后，所有文档上的编写者信息**立刻同步**，不留旧快照。
- 回包中的编写者字段：

| 字段 | 说明 |
| --- | --- |
| `ownerName` | 编写者用户名（**保留的旧字段名**，内容与 `author.username` 一致，既有前端不改也能用） |
| `author.userId` | 编写者用户ID |
| `author.username` | 编写者用户名（实时解析） |
| `author.avatarId` | 头像图片 id（为空表示未设置头像） |
| `author.avatarUrl` | 头像地址 `/img/{avatarId}`（有站点基址时自动补全为完整地址）；无头像时为空串 |

- `ownerId = 0` 或该用户已被删除 → 统一显示「**未知用户**」（`userId = 0`）。

### 59.4 全量迁移（约 950 篇静态文档 → 后端文档库）

- 迁移工具：`migrate_backrooms_docs.py`，**一次性 / 可重复执行**。
- 迁移结果：`backrooms_levels/index.json` 共 **956** 条 —— 层级 615 / 实体 197 / 物品 95 / 现象 49；其中归「未知用户」的（`ownerId = 0`）**955** 条，另 1 条为既有用户投稿（迁移**永不覆盖** `ownerId ≠ 0` 的记录）。
- 归属与状态：`ownerId = 0`（未知用户）、`status = approved`（已收录）、`version = 1`。
- **类型判定**：类型取自文档 `<title>` 首段（其次才看所在目录）——标题写 `Entity 64 ...` 就按实体收录，哪怕它放在 levels 目录。
- **ID 优先取自标题编号**：`Entity 257 - “虚渊凌日”` → `Entity-257`；标题无编号时用文件名派生（`the-red-knight` → `Entity-The-Red-Knight`、`level-706.html` → `Level-706`）。
- **名称取自 `<title>`**：`Entity 13 - “传送者” - The Backrooms中文维基` → 传送者。
- **相对引用已解析为原绝对地址**：正文里的 `../normal-levels/levels/level-2.html` 之类在导入时解析成 `https://ziyit-hacker.github.io/backrooms/...`，因此改走后端接口后，文内**图片与站内跳转仍指向原位置**，阅读体验与迁移前一致。
- **与用户投稿口径一致**：导入时注入右下角在线客服按钮、正文统一转成 UTF-8。
- **跳过项**（列在迁移报告里，不入库）：索引页、分级说明页（IETS / IPTS）、模板与功能页、组织类文档（`the-m-e-g` / `groups`）。
- 用法：

```
python migrate_backrooms_docs.py                            # 只体检，不写任何文件（默认）
python migrate_backrooms_docs.py --apply                    # 真正导入
python migrate_backrooms_docs.py --apply --force            # 覆盖已存在的同名迁移文档
python migrate_backrooms_docs.py --apply --reset-migrated   # 先清掉旧的迁移记录再全量重导
python migrate_backrooms_docs.py --source H:\Code\html\ziyit\backrooms --apply
```

- ⚠ **迁移数据不在部署同步范围内**：`sync_ziyit_to_prod.py` 只搬 `.py` 与 `.md`，`backrooms_levels/` 下的 JSON 索引与 HTML 文件**不会被同步**。正式环境需要**各自执行一次**迁移脚本（数据属于各环境自己的数据）。

### 59.5 接口清单

四类文档接口**同构**：把下表 `{前缀}` 换成 59.1 中的接口前缀即可（层级为既有实现，规则一致）。

| 方法 | 路径 | 权限 | 用途 |
| --- | --- | --- | --- |
| `GET` | `{前缀}` | 公开 | 文档列表 |
| `GET` | `{前缀}/{id}/meta` | 按可见性 | 元数据 JSON（含编写者 / 审核进度） |
| `GET` | `{前缀}/{id}` | 按可见性 | 文档 HTML 正文 |
| `POST` | `{前缀}` | 登录用户 | 提交文档 |
| `PUT` | `{前缀}/{id}` | 作者本人 | 修改文档（旧版留档、重新走审核） |
| `DELETE` | `{前缀}/{id}` | 作者本人 | 删除个人文档 |
| `PUT` | `{前缀}/{id}/rewrite` | 管理员 Lv.2+ | 管理员重写（下架 + 保留原文件） |
| `DELETE` | `{前缀}/{id}/admin` | 管理员 Lv.2+ | 管理员删除 |

跨类型**通用**接口：

| 方法 | 路径 | 权限 | 用途 |
| --- | --- | --- | --- |
| `POST` | `/backrooms/review/ai` | 管理员 | 基础审核（`action=prompt` 生成给免费 AI 的提示词 / `action=submit` 粘贴 AI 回复并解析） |
| `POST` | `/backrooms/review/advanced` | 管理员 Lv.3+ | 高级审核（`approve` / `reject`，结果邮件通知作者） |
| `GET` | `/backrooms/normal-levels/slyq.md` | 公开 | 下载审核标准 |

> **投稿权限（v0.3.20 起）**：提交、修改、删除自己的稿件**只要求登录**，**不再要求后室成员身份**（不再校验 `user.json` 成员名单）。因此投稿相关接口的 **403 只剩两种情况**：**账号已被封禁**，或**改/删的不是自己提交的稿件**（`只能删除自己提交的层级` 之类）；未登录仍是 **401**。权限可用 `GET /auth/user-type` 自查（返回 超级管理员 / 管理员 / 成员 / VIP用户 / 普通用户）。

#### 59.5.1 列表 `GET {前缀}`

```json
{
  "items": [ { "...": "见 59.6" } ],
  "total": 197,
  "type": "entity",
  "typeLabel": "实体"
}
```

- 层级列表为兼容既有前端**同时**返回 `levels` 与 `items`（内容完全相同），另外带 `total` / `type` / `typeLabel`。
- **可见性**：公开只返回 `approved`（已收录）与 `rewritten`（已重写，供前端显示 rew 标记）；**作者**额外看到自己的各状态稿件；**管理员**看到全部。
- **排序**：按 ID 末尾数字排（`Entity-13` 排在 `Entity-2` 之后），无数字时按字符串排。

#### 59.5.2 元数据 `GET {前缀}/{id}/meta`

- 在 59.6 的全部字段基础上**额外**返回：
  - `fileName`：后端存储的文件名；
  - `aiReview`：`{ result, reasons }`（基础审核结论）；
  - `advancedReview`：`{ result, reason, reviewerId, reviewerName, at }`（高级审核结论）。
- 可见性规则与 HTML 查看完全一致；不可见 → **404**，`detail = "文档不存在或未收录"`。

#### 59.5.3 查看正文 `GET {前缀}/{id}`

- **公开**：仅 `approved`（`rewritten` 已下架 → **404**）。
- **作者本人**：可查看自己任意状态的稿件，非 `approved` 时页面会注入「正在审核中」提示条。
- **管理员（Lv.1+）**：可查看待审核稿件，并注入状态提示条。

#### 59.5.4 提交 `POST {前缀}`（`multipart/form-data`）

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `docId` | ✅ | 文档 ID，须以对应前缀开头（**层级仍为 `levelId`**，保持既有前端兼容） |
| `name` | ✅ | 文档名称 |
| `file` | ✅ | `.html` 文件，≤ 1MB |

成功回包（ID 字段名：非层级为 `docId`，层级为 `levelId`）：

```json
{
  "message": "提交成功，正在等待审核",
  "docId": "Entity-13",
  "type": "entity",
  "typeLabel": "实体",
  "name": "传送者",
  "status": "pending"
}
```

错误约定：

| 情况 | 状态码 |
| --- | --- |
| ID 前缀不符 / 名称为空 / 缺 IETS、IPTS 标识 / 非 `.html` | **400** |
| ID 已存在 | **409** |
| 文件超 1MB | **413** |
| 拥有文档数达上限（四类合计 **50** 个） | **429** |
| 当月上传次数超限（四类合计 **30** 次/月） | **429** |

#### 59.5.5 修改 `PUT {前缀}/{id}`（`multipart/form-data`）

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `name` | ｜ | 留空则保留原名称 |
| `file` | ✅ | 新稿 `.html`，≤ 1MB；实体 / 现象的新稿同样必须带标识 |

- 仅**作者本人**可改（否则 **403**）；旧版保留到 `backrooms_levels/versions/`；`version + 1`、状态回到 `pending`（重新审核）、清空上一轮审核结论。
- 回包：`{ "message": "修改成功，已重新进入审核", "docId": ..., "type": ..., "name": ..., "status": "pending", "version": 2 }`

#### 59.5.6 删除 `DELETE {前缀}/{id}` / 管理员删除 `DELETE {前缀}/{id}/admin`

- 用户端仅**作者本人**可删（否则 403）；管理员删除需 **Lv.2+**，回包额外带 `deletedBy`。
- 都会同时删除索引记录与对应文件（含 `versions/` 下的历史备份）。

#### 59.5.7 管理员重写 `PUT {前缀}/{id}/rewrite`

- 权限 **Lv.2+**；作用：**直接下架**（状态置 `rewritten`、`rewritten = true`）、**保留原文件**、列表上带 rew 标记；若已是重写状态则幂等返回。
- 与「删除」的区别：**重写保留原文件与索引记录**，只是不再对外展示。

### 59.6 列表 / 元数据条目字段（`items[]`）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 文档 ID |
| `name` | string | 文档名称 |
| `type` | string | `level` / `entity` / `object` / `phenomenon` |
| `typeLabel` | string | 类型中文（层级 / 实体 / 物品 / 现象） |
| `ownerName` | string | 编写者用户名（旧字段名，兼容既有前端） |
| `author` | object | `{ userId, username, avatarId, avatarUrl }`，**每次请求实时解析** |
| `status` | string | 状态 key（见 59.7） |
| `statusText` | string | 状态中文（**前端不要写死映射**，用回包值渲染） |
| `rewritten` | bool | 是否已被管理员重写（列表据此显示 rew 标记） |
| `version` | int | 版本号（每次修改 +1） |
| `size` | int | 文件字节数 |
| `createdAt` / `updatedAt` / `mountedAt` | string \| null | 创建 / 更新 / 挂载时间（ISO 字符串） |

### 59.7 状态机（四类共用）

```
pending ──基础审核(管理员用免费 AI 审核)──> ai_passed / ai_failed
ai_failed ──作者修改──> pending
ai_passed ──管理员(1-2级)提交──> advanced_pending
advanced_pending ──管理员(2级及以上)──> approved（挂载 + 邮件通知作者）
                                      └─> rejected（邮件通知理由）
approved ──作者修改──> pending（旧版保留在 versions/，审核通过后替换）
任意状态 ──管理员重写 rewrite──> rewritten（下架 + 保留原文件 + 列表 rew 标记）
```

| 状态 key | 中文 |
| --- | --- |
| `pending` | 待审核 |
| `ai_passed` | 基础审核通过 |
| `ai_failed` | 基础审核未通过 |
| `advanced_pending` | 待高级审核 |
| `approved` | 已收录 |
| `rejected` | 已驳回 |
| `rewritten` | 已重写（下架） |

### 59.8 给前端的要求

> 前后端分属不同编写者，以下仅为**对接要求**。前端仓库里既有文件由前端维护者处理，后端不代改。

1. **正文改从后端取**：维基文档页不再读前端静态 HTML，改为调 `GET /backrooms/{levels|entities|objects|phenomena}/{id}` 拿正文（按分类选前缀）。URL 里的 `?open_level=` / `?open_entity=` / `?open_object=` / `?open_phenomenon=` 参数照旧由前端解析后拼 ID。
2. **分类列表改读接口**：层级用 `GET /backrooms/levels`（回包 `levels` 与 `items` 等价），实体 / 物品 / 现象分别用 `GET /backrooms/entities` / `/backrooms/objects` / `/backrooms/phenomena`；统一按回包的 `items` / `total` / `type` / `typeLabel` 渲染，**不要**在前端写死分类名，也不要自己按目录过滤类型。
3. **编写者信息**：列表与文档页展示 `author.username` + `author.avatarUrl`（`avatarUrl` 已是可用地址，不必再拼 `/img/`；为空时用默认占位图）。不要本地长期缓存用户名"快照"——服务端是实时解析的，按返回值渲染即可。
4. **状态与 rew 标记**：状态徽标用回包的 `statusText`（**不要写死中文**）；`rewritten = true` 的条目按「已重写」展示标记。
5. **提交 / 修改表单**：接口前缀即类型，表单**不用传 `type`**；ID 字段名非层级页为 `docId`（层级页仍用 `levelId`）。实体页与现象页需要在提交前提示「必须带 IETS / IPTS 标识」，并在收到 400 时把 `detail` 原文告诉用户（后端已说明正确写法）。
6. **投稿不再看成员身份（v0.3.20）**：提交 / 修改 / 删除只要求登录；**不要再按「是否后室成员」拦人或提示「仅限后室成员使用」**。403 现在只表示**账号被封禁**或**操作的不是自己的稿件**，提示语要按此改写（建议直接显示后端 `detail`）。想按权限区分界面的话，可调 `GET /auth/user-type`（返回 超级管理员 / 管理员 / 成员 / VIP用户 / 普通用户）。`backrooms/submit.html` 已按此改好（显示当前身份并按身份给出提示），其余投稿入口页面按本要求处理。
7. **头像设置入口**：用户资料页新增头像设置 / 清除，调 `PUT /users/avatar`（需登录），请求体 `{ "avatar": "<图片ID>" }`，传空串表示清除；图片 ID 须是**该用户自己上传过**的图片（64 位 hex），回包带 `avatar` 与 `avatarUrl`。设置后**所有文档上的头像会立刻同步**（无需重新提交文档）。
8. **列表分页**：接口目前一次返回全量 `items`（附 `total`）；若后续列表变大，前端可先按 `total` 展示数量，分页参数由后端另行提供。

### 59.9 给 RC 端的要求

- 本批文档系统**与 RC 软件端无关**，RC 客户端**无需改动**。

---

## 六十、人机验证体验页与视频分包下发（含前端对接要求）

> 本章对应后端 **v0.3.19**（含 **v0.3.18** 起引入的体验模式）。
> 后端接口**已全部实现并部署**；前端验证页面（含人机验证体验页）按本章要求对接。

### 60.1 三种凭据通道

| 通道 | 请求头 | 适用场景 | 计费归属 |
| --- | --- | --- | --- |
| 第一方浏览器票据 | `x-phantom-ticket` | 官网验证页面（`Human_verification.html`、`Translator.html`、注册页等） | 服务端按请求 Origin 解析出的密钥 userId |
| 第三方服务端直连 | `api-key` | 第三方在自己服务器调用 | 该 API 密钥 |
| 体验页 | `x-phantom-exp-key` | 人机验证体验页 | **当前登录 JWT 用户**名下的 API 密钥 |

- 三个通道**都适用**本章的分包下发、严格串行游标与实时流规则；发起 `/challenge` 时同样要过浏览器环境证据校验。
- `/video/ready` 与 `/video/chunk` 另有一层**会话绑定校验**：必须与领题时是同一个 `sessionId`、同一个 IP + User-Agent（不符一律 403）。
- `x-phantom-exp-key` 的请求头名由服务端配置决定（回包里的 `expKeyHeader`），**前端不要写死**。

### 60.2 体验页开场查询 `GET /experience/status`

- 凭据：**只认** `Authorization: Bearer <网站登录 JWT>`（服务端签名校验 + 仍在滑动有效期内）；无 / 失效 → **401**。
- 本接口**只读，不消耗额度**。
- 回包：

```json
{
  "allowed": true,
  "reason": "ok",
  "message": "",
  "hasKey": true,
  "monthlyLimit": 1000,
  "monthRemaining": 873,
  "dailyLimit": 50,
  "dayRemaining": 41,
  "apiKeyPattern": "^[A-Za-z0-9]{5}(?:-[A-Za-z0-9]{5}){4}$",
  "expKeyHeader": "x-phantom-exp-key",
  "user": { "userId": 7, "username": "……" }
}
```

- `allowed = false` 时，前端应**禁用「开始验证」按钮**并显示 `message`。`reason` 取值与对应文案：

| `reason` | 含义 | 文案（`message`） |
| --- | --- | --- |
| `no_key` | 账号名下没有可用的 API 密钥 | 该账号名下还没有可用的 API 密钥 |
| `disabled` | 名下密钥已停用 | 该账号的 API 密钥已停用 |
| `monthly` | 本月额度用尽 | API 密钥本月可用额度已用尽 |
| `daily` | 今日额度用尽 | API 密钥今日可用额度已用尽 |

- `apiKeyPattern` 发给前端用于**输入框即时格式校验**；`expKeyHeader` 是请求头名。
- **密钥本体与密钥明文一律不下发**。
- 后端关闭体验模式（环境变量 `PHANTOM_EXPERIENCE=0`）→ **404**（`experience mode disabled`）。

### 60.3 体验页额度规则（重要）

- 体验页允许**任意输入一个格式合法的密钥**（通过 `apiKeyPattern` 校验即可）来试用；**该密钥本身不参与授权判定**。
- 真正的额度扣减对象是**当前登录账号名下**的 API 密钥 —— 因此「**伪造成别人的 API 密钥所有者**」这条攻击路走不通：填别人的密钥，花的仍是**自己**账号的额度（服务端按签名校验过的 JWT 定身份）。
- 密钥格式非法 → **400**（`Invalid API key format`）；未登录 → **401**；额度为 0 → **403**，`detail` 为 `{ reason, message, monthRemaining, dayRemaining }`。
- 每次验证（通过后计费一次）从**当前账号名下**密钥的月额度与日额度中各扣 1。

### 60.4 视频就绪握手 `POST /video/ready`

对应需求：「后端视频生成完成后，前端需**先确认准备就绪**，再向后端请求视频数据」。

请求体：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `challengeId` | ✅ | `/challenge` 返回的题目 ID |
| `sessionId` | ｜ | `/challenge` 返回的 `sessionId`，用于会话绑定校验 |

服务端行为：

1. 校验请求与该 `challenge` **同源**（`sessionId` 的 sha256 与 IP + User-Agent 指纹都要一致）；
2. 把**分包游标归零**（允许从第 0 包开始拉）；
3. 记 `video_ready_ms` / `video_sent_ms`（作为「首批数据到达延迟」的基准）。

回包：

```json
{
  "ready": true,
  "mode": "chunked",
  "chunkFrames": 5,
  "chunkCount": 48,
  "totalFrames": 240,
  "chunkDurationMs": 167,
  "durationMs": 8000,
  "fps": 30,
  "mime": "video/mp4",
  "codec": "avc1.64001E"
}
```

错误约定：

| 情况 | 状态码 |
| --- | --- |
| 后端关闭分包（`PHANTOM_VIDEO_CHUNKED=0`） | **404** |
| `challengeId` 不存在 / 已过期 / 已使用 | **410** |
| 会话绑定不符（`sessionId` 或 IP + UA） | **403** |
| 无凭据 | **401** |
| 分包数据不存在（未分包或已过期） | **404** |

- ⚠ **握手之前任何 `/video/chunk` 都会被拒**（游标未开）。

### 60.5 分包拉取 `POST /video/chunk`（严格串行）

对应需求：「**每 5 帧发送一次视频数据**，确保**不能同时发起请求和发送数据**」。

请求体：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `challengeId` | ✅ | 题目 ID |
| `index` | ✅ | 包序号，从 **0** 开始，必须**严格连续** |
| `sessionId` | ｜ | 会话绑定 |

回包：

```json
{
  "index": 0,
  "data": "<base64(fMP4 分片)>",
  "mime": "video/mp4",
  "frames": 5,
  "final": false,
  "nextIndex": 1,
  "chunkCount": 48
}
```

行为要点：

- **严格串行游标**：服务端只接受 `index == 当前游标` 的那一包；**跳号 / 抢跑 / 并发预取一律 409**（`detail = "video chunks must be fetched strictly in order, one at a time"`）。取走一包游标才前进，因此**下一包必须等这一包返回之后才可能成功** —— 整段视频**无法**被并行或成批一次性拿走。
- **重传幂等**：同一包的**重复请求**（网络抖动重发）按「已下发过」**重发同一份内容**、**不推进游标**，不会因为一次抖动让用户重来。
- **包形态**：**首包 = `ftyp` + `moov` 初始化段 + 第 1 个分片**，之后每包 = 一个 `moof` + `mdat` 媒体分片，**都以 IDR 关键帧开头**（GOP 与 `chunkFrames` 对齐）—— 前端用 MediaSource **依序 append 即可无缝播放**，不会出现"某包解不开"的情况。
- 每包帧数 = 回包的 `frames` = `chunkFrames`（默认 **5**）。

错误约定：

| 情况 | 状态码 |
| --- | --- |
| `index` 越界（< 0 或 ≥ `chunkCount`） | **400** |
| 游标不符（跳号 / 抢跑 / 并发预取） | **409** |
| 分包数据过期（默认 TTL **120** 秒）或缺失 | **410**（需重新走一次验证） |
| `challengeId` 过期 / 已使用 | **410** |
| 会话绑定不符 | **403** |
| 后端关闭分包 | **404** |

### 60.6 `/challenge` 回包变化

**默认（分包模式）**：回包里 `video = null`，视频信息改由 `videoStream` 提供：

```json
{
  "challengeId": "……",
  "sessionId": "……",
  "serverPublicJwk": { "……": "……" },
  "salt": "……",
  "encryptedParams": { "……": "……" },
  "video": null,
  "videoMime": "video/mp4",
  "videoDurationMs": 8000,
  "videoStream": {
    "mode": "chunked",
    "chunkFrames": 5,
    "chunkCount": 48,
    "totalFrames": 240,
    "chunkDurationMs": 167,
    "durationMs": 8000,
    "fps": 30,
    "mime": "video/mp4",
    "codec": "avc1.64001E"
  }
}
```

- `videoStream.codec` 由后端**从码流的 `avcC` box 读出**（如 `avc1.64001E`），**前端拼 codec 串必须用它**（自己拼错会导致 `MediaSource.isTypeSupported` 失败、黑屏）。
- **不再兼容旧的整段下发**：本版已选定「**强制走分包**」，`/challenge` 不再下发整段 `video` 字节。仅当后端显式关闭 `PHANTOM_VIDEO_CHUNKED` 时才回到旧的整段 base64 下发（供兜底，前端不必实现）。

### 60.7 实时流参数变化（更密、更宽容抖动）

| 参数 | 旧值 | 现值 | 说明 |
| --- | --- | --- | --- |
| `stream.intervalMs`（上报间隔） | 100ms | **50ms** | 对应需求「减少每次验证间隔时间、增加实时流验证次数」 |
| 期望批次数上限 | 400 | **800** | 期望批次数 = 视频时长(ms) ÷ 50，上限放宽以留约 40 秒余量 |
| 批间隔抖动比例上限 | 3.0 | **6.0** | 绝对容差仍为 300ms；放宽比例以适配网络抖动与性能较弱的设备，避免误伤真人 |

- 前端**应按下发的 `stream.intervalMs`（当前 50ms）上报**，不要为了"抗抖动"自行放大间隔——间隔变大会导致批次数不足而被否决。

### 60.8 给前端的要求

> 前后端分属不同编写者，以下仅为**对接要求**。前端仓库里既有文件由前端维护者处理，后端不代改。

1. **体验页凭据与开场**：调 `GET /experience/status` 前必须带 `Authorization: Bearer <JWT>`；用回包的 `apiKeyPattern` 做输入框即时校验、用 `expKeyHeader` 作请求头名（**不要写死** `x-phantom-exp-key`）。`allowed = false` 时禁用「开始验证」并显示 `message`。
2. **体验页的请求头**：发起 `/challenge`、`/video/ready`、`/video/chunk`、`/verify/chunk`、`/verify` 时**同时**带上 `Authorization: Bearer <JWT>` 与 `{expKeyHeader}: <用户输入的密钥>`（密钥只做格式校验，用户输入什么就传什么）。
3. **改读 `videoStream`**：`/challenge` 回包里的 `video` 现在是 `null`，**不要**再读它；改用 `videoStream` 的 `chunkFrames` / `chunkCount` / `totalFrames` / `codec` / `mime`。
4. **就绪握手**：`MediaSource` + `SourceBuffer` 准备就绪后，先 `POST /video/ready`（带 `challengeId` + `sessionId`），拿到 `ready = true` 再开始拉包。
5. **串行拉包（关键，不要改）**：
   - 用 `index = 0,1,2,…` **逐包** `POST /video/chunk`；
   - **必须串行**：拿到第 n 包的响应并 `appendBuffer` 之后，才发起第 n+1 包的请求；**禁止**并发预取、一次性批量请求、跳号补包（都会被 409 拒）；
   - 用回包的 `nextIndex` 作为下一次请求的 `index`，用 `final` 判断是否取完；
   - 收到 **409** 说明本地序号与服务端游标不同步，应按提示重取**当前游标指向的那一包**（同一包的重复请求是幂等的，不会推进游标）。
6. **MSE 播放**：用 `MediaSource`，`addSourceBuffer('video/mp4; codecs="' + videoStream.codec + '"')`；**首包**（含 `ftyp + moov`）作为初始化段先 append，之后每包依序 append；`SourceBuffer.mode` 保持默认 `segments`。
7. **实时流上报**：按 `stream.intervalMs`（当前 **50ms**）上报增量样本，别再按 100ms 上报。
8. **错误处理**：
   - **401** → 重新登录 / 重新取票据（第一方页面），或检查体验页登录态；
   - **403** → 会话绑定不符（换了 IP / UA，或未带对 `sessionId`），需**重新取题**；
   - **409** → 拉包序号错乱，按第 5 条重取；
   - **410** → 题目或分包已过期（默认 120 秒），需**重新开始验证**；
   - **429** → 触发限流，按 `detail` 文案提示用户稍后再试。

### 60.9 给 RC 端的要求

- 本批改动**与 RC 软件端无关**（视频分包与体验页均属网页侧人机验证），RC 客户端**无需改动**。

---

## 六十一、账号安全与多种验证方式（含前端对接要求）

> 本章对应后端 **v0.3.26**（密码 / 邮箱验证码 / 2FA）与 **v0.3.27**（Passkey / WebAuthn）。
> 后端接口**已全部实现**，前端按本章对接即可。**四种验证方式全部可用**。

### 61.1 四种验证方式（因子）

登录不再只有「用户名 + 密码」一条路。后端把验证方式抽象成 4 类**因子（factor）**，
登录要求凑够 **N 类**（不是 N 次）——**同一类做两遍不算两种**：

| `factor` | 名称 | 用户怎么验 | 是否可用 |
| --- | --- | --- | --- |
| `password` | 密码 | 用户名 + 密码，就是原来的登录 | ✅ 可用 |
| `email` | 邮箱验证码 | 往**已验证邮箱**发 6 位一次性验证码 | ✅ 可用（**用户自己开**，默认关） |
| `totp` | 2FA | 微软 / Google Authenticator 等扫二维码，输 6 位动态码 | ✅ 可用 |
| `passkey` | 通行密钥 | Windows Hello / 指纹 / 安全密钥（WebAuthn），**在官网页面上直接刷脸按指纹** | ✅ 可用 |

**同一类因子不能重复计数**：已经用密码验过，再输一次密码不算第二类。

**Passkey 的三个关键事实**（决定了它为什么必须绑前端域名）：

1. **RP ID 绑的是「页面自己的域名」，不是后端域名**。本站前端是 GitHub Pages
   （`ziyit-hacker.github.io`），所以 Passkey 只认这个域名——**后端换 ngrok 域名不会让已绑的
   Passkey 失效**，反过来在别的域名下也刷不了（这是浏览器强制的，防钓鱼）。
   后端默认按 `HOME_URL` 自动推导，可用 `.env` 的 `WEBAUTHN_RP_ID` / `WEBAUTHN_ORIGINS` 覆盖。
2. **私钥永不离开用户设备**，后端只存公钥；登录是「后端发随机数 → 设备用私钥签名 → 后端验签」。
3. **必须有 HTTPS**（GitHub Pages 天然有）。`http://` 或 `file://` 打开页面时浏览器直接不给用。

### 61.2 策略：要求几种验证方式

- `requiredFactors` **只能是 1 或 2**（传更大按 2 算，传更小按 1 算）：
  - **1（默认）** —— 与改造前**完全一致**：老用户照旧「用户名 + 密码」直接登录，多一个字段都不会多。
  - **2** —— 必须凑够**两类**才能拿到登录凭据，组合随意：
    `密码+邮箱` / `密码+2FA` / `密码+Passkey` / `邮箱+2FA` / `邮箱+Passkey` / 密码+恢复码 等。
- `policySource` 记录这条策略是**谁定的**：`user`（用户自己设）/ `admin`（管理员身份强制）。
- **管理员账号（有后台权限的）强制 2 种，且无法关闭**：登录时后端**自动落库**（惰性写入 `requiredFactors = 2`、`policySource = "admin"`），
  调低、关闭 2FA / 删最后一个 Passkey 都会被 **403** 拦下。
- **「无密码登录」的前提**（缺一不可）：**已验证邮箱 + 已绑 2FA + 已绑 Passkey** 三者齐备，
  此时才能把 `passwordEnabled` 关掉（`/auth/security` 的 `canDisablePassword` 会告诉前端能不能关）。
- **兜底**：关不掉也不怕丢号 —— 有 **10 个一次性恢复码**，还能换**另一类因子**登录
  （`密码+邮箱` / `邮箱+2FA` / `密码+Passkey` 任意组合都行）。因此**不做**服务器本地解锁脚本。

### 61.3 登录流程（前端重点）

#### 第一步 `POST /auth/login`

请求体**没变**：`{"username": "...", "password": "<md5>"}`。返回有**三种**形态：

```json
// ① 够了（老用户 / 只要求 1 种）—— 与改造前完全一样
{ "accessToken": "…", "userId": 1, "username": "xxx" }

// ② 还差第二类因子
{
  "mfaRequired": true,
  "mfaToken": "…",              // 登录中间票据，下一步的凭据
  "requiredFactors": 2,
  "satisfied": ["password"],    // 已经过了哪几类
  "methods": ["totp"],          // 还能用哪些方法补齐（前端按这个渲染按钮）
  "message": "请继续完成第二重验证。"
}

// ③ 策略要求 2 种，可这个账号只启用了一种（管理员第一次登录就是这种）→ 必须先绑第二方式
{
  "mfaRequired": true,
  "enrollRequired": true,
  "enrollToken": "enroll_…",    // 受限票据，只能进安全中心
  "requiredFactors": 2,
  "methods": [],
  "message": "该账号要求两种验证方式，请先在「账号安全」里绑定第二方式（推荐 2FA 验证器）。"
}
```

> `enrollRequired` 时**没有 `accessToken`**，前端必须把用户领到「账号安全」页面绑 2FA（用 `enrollToken`）。

#### 邮箱验证码：`POST /auth/login/email/start`

```json
// 当第二类因子用：带第一步拿到的票据
{ "mfaToken": "…" }
// 当第一类因子用（邮箱+2FA 这种组合）：带用户名
{ "username": "xxx" }
```

回包**永远是同一句话**（防用户名枚举，账号不存在 / 没开邮箱验证都一样）：

```json
{
  "message": "如果该账号存在且已开启邮箱验证，验证码已发送到它已验证的邮箱。",
  "mfaToken": "…",
  "methods": ["email"]
}
```

- 请把回包里的 `mfaToken` **替换掉**本地保存的那个（后端可能给了一张新票据）。
- **同一个验证码会一直用到过期为止**（有效期 300 秒）：这期间再次点「发送」**拿到的还是同一个码** ——
  用户翻到哪封邮件都对得上，不会出现「刚收到的码反而无效」。
  - 验证码**按账号存**、不绑在票据上。所以中途每补一个因子换一张新票据（旧票据作废）后，
    再用新票据发码也**照样能用最初那封邮件里的码**。
  - 冷却：同一账号**60 秒内不重复发信**（码不变、只是不再发一封）；过了 60 秒才真再发一封、
    码仍不变。同一 IP **5 分钟 5 条**，超了 429。
  - 前端应给「发送验证码」按钮加 **60 秒倒计时**，倒计时内点它不会收到新邮件（后端静默跳过）。
  - 验证码**用一次即废**；用掉后再发，才会生成一个新码（此时冷却也重置）。

#### 补齐因子：`POST /auth/login/factor`

```json
// 验证码类（2FA / 恢复码 / 邮箱码）
{ "mfaToken": "…", "factor": "totp", "code": "123456" }
// 密码类（第二因子用密码）
{ "mfaToken": "…", "factor": "password", "password": "<md5>" }
```

- `factor` 取值：
  - `totp` —— 2FA 动态码；
  - `recovery` —— 一次性恢复码（算 `totp` 这一类）；
  - `email` —— 邮箱验证码；
  - **`password`** —— 密码（传 `password` 字段，md5，和 `/auth/login` 一样）。
    **第一因子用邮箱验证码的账号，第二因子就可以用密码**（`邮箱+密码` 这种组合）；
    密码错误同样累计 `failedLoginAttempts`，**连续 5 次锁定 15 分钟**，与 `/auth/login` 完全一致。
- **首因子决定剩下能选什么**（后端按 `availableFactors - 已满足` 算，回包 `methods` 里就是这些）：
  | 第一因子 | 第二因子可选 |
  | --- | --- |
  | 密码 | 邮箱验证码 / 2FA / Passkey（**不含密码**，同一类不重复计数） |
  | Passkey | 密码 / 邮箱验证码 / 2FA |
  | 邮箱验证码 | **密码 / 2FA / Passkey**（也**不含邮箱**） |
- 返回**还是上面那三种形态**：凑够了就给 `accessToken`，还差就给新的 `mfaToken` + 剩下的 `methods`。
- 错误码：
  - **400** —— 方法不支持 / 这一类已经验过了 / 该账号已关闭密码登录；
  - **401** —— 验证码或密码不对，或票据过期；**错满 5 次票据直接作废**，要重新走第一步（防 6 位码被暴力枚举）；
  - **403** —— 账号被封禁，或密码连续错 5 次后账号被锁定 15 分钟；
  - **429** —— 错误次数太多。

#### 票据纪律（`mfaToken`）

- 有效期 **300 秒**；**换 IP 或换 User-Agent 立即作废**；**用一次就废**（下一步会发新票据）。
- 票据**不是**登录凭据：拿它去调 `/auth/me` 等任何正式接口都会 **401**。

#### Passkey 登录：`POST /auth/login/passkey/start`（第一步）

Passkey 走的是**浏览器标准流程**：后端发随机数（challenge）→ 浏览器弹 Windows Hello / 指纹 →
设备用私钥签名 → 后端验签。所以要**两个接口**。

请求体二选一（都可以带）：

```json
// 当第二类因子：带上第一步 /auth/login 给的票据
{ "mfaToken": "…" }
// 当第一类因子（邮箱+Passkey 这种组合，只输用户名就能刷脸）
{ "username": "xxx" }
```

回包：

```json
{
  "challengeId": "…",              // 第二步的凭据（一次性、绑 IP+UA、300 秒）
  "rpId": "ziyit-hacker.github.io",
  "satisfied": [],                 // 已经过掉的因子类别
  "publicKey": {                   // 直接喂给 navigator.credentials.get()
    "challenge": "…",              // base64url，**必须原样用**
    "rpId": "ziyit-hacker.github.io",
    "timeout": 120000,
    "userVerification": "preferred",
    "allowCredentials": [ { "type": "public-key", "id": "…", "transports": ["internal"] } ]
  }
}
```

- **账号不存在 / 没绑过 Passkey 时一律返回 400**（两者文案**完全相同**，防用户名枚举）：
  ```json
  { "detail": "该账号没有可用的 Passkey，请改用密码或邮箱验证码登录" }
  ```
  > ⚠ **不要**在收到 400 之后还去调 `navigator.credentials.get()`。修复前这里回的是
  > 「200 + `allowCredentials` 空数组」，浏览器拿着空列表只会让 Windows Hello 弹
  > 「**使用通行证密钥登录出现问题**」，用户完全看不懂（**这就是之前线上那个报错的根因**）。
  > 正确做法：直接把这句 `detail` 提示给用户。
- 带 `mfaToken` 且**这一类已经通过**时 → **400**「这一类验证方式已经完成了」。
- **注意字段名**：这里是 `challengeId`（不是注册那边的 `ticketId`）。

#### Passkey 登录：`POST /auth/login/passkey/finish`（第二步）

```json
{
  "challengeId": "…",              // 上一步的 challengeId
  "credential": {                  // navigator.credentials.get() 的结果，原样回传
    "id": "…",                     // base64url
    "type": "public-key",
    "rawId": "…",
    "response": {
      "clientDataJSON": "…",       // base64url
      "authenticatorData": "…",    // base64url
      "signature": "…",            // base64url
      "userHandle": null
    }
  }
}
```

返回**还是 61.3 开头那三种形态**（`accessToken` / 还差因子的 `mfaToken` / `enrollRequired`）。
错误码：

- **401** —— 验签不通过 / 凭据未注册 / 票据过期或已用（**不区分原因**，防探测）；
- **400** —— 返回数据不是合法 base64url、`challengeId` 无效；
- **403** —— 账号被封禁。

> **`mfaToken` 与 Passkey 的顺序**：当第二因子时，`finish` 成功后后端会把那张 `mfaToken`
> **消费掉**（`satisfied` 合并计算）。因此**不要**在 `finish` 之后再拿旧 `mfaToken` 去调别的接口。

### 61.4 安全中心接口

全部在 `/auth/security*` 下，凭据是 `Authorization: Bearer <登录凭据>`。
**被强制绑定的用户用 `enrollToken` 也一样能调**（受限票据只在安全中心被认，别的接口一律 401）。

#### `GET /auth/security` —— 总览（不消耗任何东西）

```json
{
  "userId": 7,
  "requiredFactors": 2,
  "policySource": "user",          // user / admin
  "isAdmin": false,
  "email": "a@b.com",
  "emailVerified": true,
  "emailFactor": false,            // 邮箱验证码这条通道开没开
  "passwordEnabled": true,
  "totpEnabled": false,
  "totpEnabledAt": null,
  "passkeyCount": 0,               // 已绑定的 Passkey 个数
  "passkeys": [                    // 每个 Passkey 的展示用元信息（**不含公钥**）
    {
      "credentialId": "…",         // base64url，删除时要用它
      "name": "本机 Windows Hello / 指纹",
      "createdAt": "2026-09-25T…Z",
      "lastUsedAt": null,
      "backedUp": false          // true = 已同步到云端（iCloud / Google 密码管理器）
    }
  ],
  "recoveryCodesLeft": 0,
  "availableFactors": ["password", "email"],
  "canDisablePassword": false,     // 是否满足「无密码登录」的三个前提
  "enrollRequired": false          // true = 现在用的是受限票据，必须先绑第二方式
}
```

> **TOTP 密钥、恢复码明文/hash 一律不下发**；`users.json` 里的 `security` 字段也**不会**出现在
> `/auth/me`、`/users` 等任何公开视图里（后端已整体屏蔽）。

#### `POST /auth/security/policy` —— 改策略

```json
{
  "requiredFactors": 2,            // 可选：1 / 2
  "emailFactor": true,             // 可选：开/关邮箱验证码
  "passwordEnabled": false,        // 可选：关/开密码登录（前提是 canDisablePassword=true，否则 400）
  "factor": "password",            // 身份证明方式：password / totp / recovery
  "code": "",                      // factor=totp/recovery 时填 6 位码 / 恢复码
  "password": "<md5>"              // factor=password 时填
}
```

- **所有改动都要出示身份证明**（防「JWT 被偷就能改安全设置」）。规则：
  - 还没绑 2FA 的账号只有密码可用 —— `factor` 留默认 `totp` 且带了 `password` 时会**自动按密码处理**，前端不必先查总览；
  - **已绑 2FA 的账号，做「弱化」操作（调低到 1 种 / 关邮箱验证 / 关密码登录）时，不接受只用密码**，必须给 2FA 码或恢复码；加强操作（开邮箱验证、调到 2 种）用密码即可。
- **自家不给自家挖坑**：改完之后如果「可用验证方式」少于 `requiredFactors`，后端直接 **400** 拒绝（不会把用户锁在门外）。
- **403** = 身份证明失败 / 该账号强制两种不能调低；**400** = 条件不满足（如邮箱没验证就想开邮箱验证）。
- 返回体同 `GET /auth/security`。

#### `POST /auth/security/totp/setup` —— 生成 2FA 密钥（还没启用）

```json
{
  "secret": "JBSWY3DPEHPK3PXP…",
  "otpauthUrl": "otpauth://totp/ZIYIT%20STUDIO:xxx?secret=…&issuer=…&algorithm=SHA1&digits=6&period=30",
  "qrPng": "data:image/png;base64,…",
  "issuer": "ZIYIT STUDIO",
  "account": "xxx",
  "period": 30,
  "digits": 6,
  "message": "用身份验证器（微软 / Google Authenticator 等）扫描二维码，然后输入验证码完成绑定"
}
```

- **`qrPng` 直接就是可用 `<img src="…">` 的 data URI**，前端**不需要**再引任何二维码库。
- 密钥 15 分钟内有效（存在 `security.totpPending`，过期要重新生成）；已启用 2FA 的账号要换密钥必须先关。

#### `POST /auth/security/totp/enable` —— 确认绑定并启用

```json
{ "code": "123456" }
```

- 回包**只在此刻一次性给恢复码**：

```json
{
  "message": "2FA 已启用。请立即保存下面 10 个恢复码（每个只能用一次，用于手机丢失时登录）",
  "totpEnabled": true,
  "recoveryCodes": ["A2C4E6G8K9", "…"],   // 共 10 个，每个 10 位
  "recoveryCodesLeft": 10
}
```

- **请务必让用户抄下来 / 存好**：后端只存 hash，**之后再也不可能查回来**。
- 恢复码字符集去掉了易看错的 `I O 0 1`。
- 如果这次是**被强制绑定**（用 `enrollToken` 来的），绑定成功后回包里会**顺带带上 `accessToken`**
  （登录流程已经算完），前端可直接当登录成功处理。

#### `POST /auth/security/totp/disable` —— 关闭 2FA

```json
{ "factor": "totp", "code": "123456" }   // 或 {"factor": "recovery", "code": "<恢复码>"}
```

- **不接受只用密码**（开了 2FA 之后，密码单独不能关掉它）。
- 如果关闭后会不够 `requiredFactors`（**管理员必然如此**）→ **403**，并提示先绑其他第二方式。

#### `POST /auth/security/recovery-codes` —— 重新生成恢复码

- 入参同上（2FA 码或恢复码）。**旧的立刻全部作废**，同样只回这一次。

#### `POST /auth/security/passkey/setup` —— 绑 Passkey 第一步

无入参（就一个空 body / `{}`）。返回**直接能喂给 `navigator.credentials.create()` 的参数**：

```json
{
  "ticketId": "…",                 // 下一步 enable 要用；一次性、300 秒
  "rpId": "ziyit-hacker.github.io",
  "publicKey": {
    "challenge": "…",              // base64url，**必须转成 ArrayBuffer 再传**
    "rp": { "id": "ziyit-hacker.github.io", "name": "ZIYIT STUDIO" },
    "user": { "id": "…", "name": "xxx", "displayName": "xxx" },   // id 是 base64url，**必须转 ArrayBuffer**
    "pubKeyCredParams": [ { "type": "public-key", "alg": -7 },
                          { "type": "public-key", "alg": -257 },
                          { "type": "public-key", "alg": -8 } ],
    "timeout": 120000,
    "attestation": "none",
    "authenticatorSelection": { "residentKey": "preferred", "requireResidentKey": false,
                                "userVerification": "preferred" },
    "excludeCredentials": [ { "type": "public-key", "id": "…", "transports": ["internal"] } ]
  },
  "message": "请在设备上完成 Windows Hello / 指纹 / 安全密钥验证"
}
```

- 一个账号**最多 10 个** Passkey，满了 → **400**。
- `excludeCredentials` 里是已绑过的凭据，浏览器会据此避免重复绑同一把钥匙。

#### `POST /auth/security/passkey/enable` —— 绑 Passkey 第二步

```json
{
  "ticketId": "…",
  "name": "我的笔记本",            // 可选，不填后端按设备类型自动起名
  "credential": {                  // navigator.credentials.create() 的结果，原样回传
    "id": "…",
    "type": "public-key",
    "rawId": "…",
    "response": {
      "clientDataJSON": "…",       // base64url
      "attestationObject": "…",    // base64url
      "transports": ["internal"]
    }
  }
}
```

回包：

```json
{
  "message": "Passkey 已绑定（共 1 个）",
  "passkeyCount": 1,
  "passkeys": [ { "credentialId": "…", "name": "本机 Windows Hello / 指纹",
                  "createdAt": "…", "lastUsedAt": null, "backedUp": false } ],
  "availableFactors": ["password", "passkey"]
}
```

> **`attestationObject` 里的 `authenticatorData` 不用前端解**：后端自己解 CBOR 拿，
> 前端只要把 `response` 整个原样回传即可（**不要**自己拼 `authenticatorData` 字段）。
>
> 如果这次是**被强制绑定**（用 `enrollToken` 来的），绑定成功后回包里会**顺带带上
> `accessToken`**（登录流程已经算完），前端直接当登录成功处理——与 2FA 绑定时一致。

#### `POST /auth/security/passkey/delete` —— 删除一个 Passkey

```json
{
  "credentialId": "…",             // 从 GET /auth/security 的 passkeys[] 里取
  "factor": "password",            // 身份证明：password / totp / recovery
  "password": "<md5>",             // factor=password 时填
  "code": "123456"                 // factor=totp / recovery 时填
}
```

- **这是「弱化」操作**：已绑 2FA 的账号**不接受只用密码**（必须给 2FA 码或恢复码）；
  还没绑 2FA 的账号可以只给密码（`factor` 默认 `totp` 且带了 `password` 时后端自动按密码处理）。
- **删完不能少于要求的验证方式数量**：不够 → **403**「删掉它之后就不够你要求的验证方式数量了」，
  且**不会落库**（不会把用户锁在门外）。**管理员删最后一个 Passkey 必然 403**。
- 找不到该 `credentialId` → **404**。返回体同 `enable`（`message` / `passkeyCount` / `passkeys` / `availableFactors`）。

### 61.5 数据结构（`users.json` 里的 `security` 字段）

```json
"security": {
  "requiredFactors": 2,
  "policySource": "user",
  "emailFactor": true,
  "passwordEnabled": true,
  "totp": {
    "secret": "…",
    "enabled": true,
    "enabledAt": "2026-09-25T…Z",
    "lastUsedCode": "<counter>:<时间戳>",
    "recoveryCodes": [ { "hash": "$2b$…", "usedAt": null } ]
  },
  "passkeys": [
    {
      "credentialId": "…",           // base64url（凭据 ID，删除/登录时用）
      "publicKey": "…",              // COSE 公钥的 base64url（**永不下发**）
      "alg": -7,                     // 签名算法：-7 ES256 / -257 RS256 / -8 EdDSA
      "signCount": 12,               // 签名计数（同步型 Passkey 常年为 0）
      "transports": ["internal"],    // 设备类型提示
      "name": "本机 Windows Hello / 指纹",
      "aaguid": "…",                 // 认证器型号标识（十六进制）
      "backedUp": false,             // 是否已同步到云端
      "createdAt": "2026-09-25T…Z",
      "lastUsedAt": null
    }
  ]
}
```

- **老用户没有这个字段**，等同于「1 种验证方式、密码开启、邮箱验证通道关闭」，**不需要做任何数据迁移**。

### 61.6 后端的安全设计要点

- **TOTP 密钥与恢复码永不出网**：`public_user_data()` 整体屏蔽 `security`，安全中心只给脱敏视图。
- **2FA 防重放**：同一个时间窗里的码用掉一次就不能再用（`lastUsedCode` 记 `counter:时间戳`）；
  TOTP 容 **±1 个时间窗**（手机时钟漂移）。
- **恢复码一次性**：bcrypt 存 hash（不是明文），用掉立刻标 `usedAt`。
- **邮箱登录码按账号存、一次性**：存在 `EMAIL_CODES[userId]`，**不绑在票据上**
  （所以中途换票据也不会把码弄丢）；有效期内重复发送**沿用同一个码**，
  用掉即废；沿用旧码时**不重置**票据的试错计数（否则反复点「重发」就能绕开 5 次上限）。
- **密码当第二因子也照样锁定**：`/auth/login/factor` 里走 `password` 与 `/auth/login` 共用
  同一套 `failedLoginAttempts` / `lockedUntil`（连续 5 次错 → 锁 15 分钟），
  不会因为换了端点就变成可以无限试密码。
- **时间戳只认服务器时钟**：`/auth/login` 走密码、`/auth/login/factor` 走验证码，验证码有效期以服务端为准。
- **受限票据零信任**：`enrollToken` 不存在 `ACTIVE_TOKENS` 里，**除了安全中心接口，其它接口拿它一律 401**；
  同样绑 IP + User-Agent、15 分钟过期、用完即废。
- **所有敏感操作写审计日志**：`2fa_enabled` / `2fa_disabled` / `2fa_recovery_codes` / `security_policy` /
  `login_mfa_challenge` / `login_factor` / `login_factor_failed` / `mfa_enroll_required` /
  `passkey_added` / `passkey_deleted` / `login_passkey` / `passkey_login_failed` / `passkey_signcount_anomaly` 等。

#### Passkey（WebAuthn）的校验链

后端**没有引 `py_webauthn`**（它会把 `cryptography` 从 47 顶到 50，而 RC 密钥加密正靠它），
改用现成的 `cryptography` + 自写极简 CBOR 解码器，按 W3C WebAuthn L2 逐项校验：

| 校验项 | 说明 | 不过会怎样 |
| --- | --- | --- |
| `type` | 注册必须是 `webauthn.create`，登录必须是 `webauthn.get` | 400 |
| `challenge` | 必须等于后端本次下发的随机数（32 字节） | 400 |
| `origin` | 必须在 `WEBAUTHN_ORIGINS` 白名单里（当前 = 前端 GitHub Pages） | 403 |
| `crossOrigin` | 不允许跨站来源 | 403 |
| `rpIdHash` | `authenticatorData` 前 32 字节 == `sha256(RP ID)` | 400 |
| `UP` 标志 | 必须检测到「用户在场」（真的按了指纹 / 刷了脸） | 400 |
| `AT` 标志 | 注册时必须有凭据公钥 | 400 |
| 凭据 ID 一致性 | 前端回的 `id` 必须与 `authData` 里的一致 | 400 |
| COSE 公钥 | 只支持 EC2/P-256、RSA、OKP/Ed25519；落库前先试解析 | 400 |
| 签名 | 用**存下来的公钥**验 `authData + sha256(clientDataJSON)` | 401 |
| `signCount` | **只记录不拒绝**：同步型 Passkey（iCloud / Google）计数常年为 0，据它拒绝会误伤；计数回退只写审计 `passkey_signcount_anomaly` | 放行 |

- **challenge 票据一次性**：`WEBAUTHN_TICKETS` 里的 `ticketId` / `challengeId` 用完即删，
  同样绑 **IP + User-Agent**、**300 秒**过期、换环境立即作废。
- **公钥永不出网**：`_passkeys_public()` 只给 `credentialId` / `name` / 时间 / `backedUp`，
  `publicKey` 与 `alg` 只在登录验签时后端内部取用。
- **私钥永不离开用户设备**：后端从头到尾没见过私钥，只存公钥——数据库泄露也无法伪造登录。
- **注册要登录态**：`setup` / `enable` / `delete` 都走 `_security_actor`（登录凭据或受限票据），
  登录用的 `start` / `finish` 才是不需要凭据的公开入口。
- **防钓鱼由浏览器兜底**：RP ID 绑定使钓鱼站点上刷不出真站点的 Passkey（浏览器层面强制）。

### 61.7 给前端的要求

1. **登录页要能走「两步」**：`/auth/login` 回包里出现 `mfaRequired` 时**不要**当失败，
   按 `methods` 渲染第二步 UI；拿到 `enrollRequired` 时把用户领到账号安全页（用 `enrollToken`）。
2. **第二步按 `methods` 渲染**（**后端已经算好了**，前端不要自己猜）：
   - `methods` 含 `email` → 调 `/auth/login/email/start`（带 `mfaToken`）发码，再输 6 位码调 `/auth/login/factor`；
     **给「发送验证码」按钮加 60 秒倒计时**（倒计时内后端不会真发新邮件，码也不变）；
   - `methods` 含 `totp` → 直接输入 6 位动态码调 `/auth/login/factor`；
   - `methods` 含 **`password`** → 收密码（md5）调 `/auth/login/factor`，传 `factor: "password"` + `password` 字段。
     **首因子是邮箱验证码的账号，第二个就是密码**（`邮箱+密码` 组合）；
   - `methods` 含 `passkey` → 走 61.7.2 的 `loginWithPasskey({ mfaToken })`；
   - **提供「用恢复码」兜底入口**（`factor: "recovery"`），手机丢了才进得去。
3. **登录页第一因子也要能选**：要求 2 种的账号，第一因子可以是**密码 / Passkey / 邮箱验证码**三种任选
   （分别对应 `/auth/login`、`loginWithPasskey({username})`、`/auth/login/email/start`）；
   第二因子由后端返回的 `methods` 决定，前端照着渲染即可。
4. **保存新票据**：每次回包里的 `mfaToken` 都要覆盖本地旧的（`email/start` 和 `factor` 都可能给新的）。
5. **新增「账号安全」页面**（建议 `user/security.html`，放进用户中心导航）：
   - 显示总览（几种验证方式、邮箱验证开关、2FA 状态、恢复码剩几个）；
   - 绑 2FA：调 `totp/setup` → 用 `qrPng` 直接 `<img src>` 显示二维码（**不要引二维码库**）→ 输码调 `totp/enable`；
   - **恢复码必须让用户确认已保存后再关闭弹窗**（后端不会再有第二次）；
   - 改策略 / 关 2FA / 重发恢复码都要按接口要求带上身份证明（`factor` + `code` 或 `password`）。
6. **提示文案要对**：管理员在页面上看到「强制两种验证方式」时，**不要给关闭按钮**（后端也会 403）。
7. **密码仍然传 md5**（和现在一致），换成明文会全线登录失败。
8. **Passkey 前端必须自己写 WebAuthn 代码**（浏览器 API，没有别的办法绕过）。要点见下面 61.7.1–61.7.3。

#### 61.7.1 三个必须注意的坑

1. **`challenge` / `id` / `rawId` / `user.id` 都是 base64url 字符串，浏览器要的是 `ArrayBuffer`** ——
   必须自己转（下面有现成工具函数）。最容易漏的是 **`publicKey.user.id`**：用 `{...publicKey}` 展开
   会把原始字符串带进去，浏览器读 `user` 属性时直接抛
   `Failed to read the 'user' property from 'PublicKeyCredentialCreationOptions'`，注册根本发不出去。
   **回传时再转回 base64url**（`JSON.stringify` 不能直接序列化 ArrayBuffer）。
2. **注册时不要自己拼 `authenticatorData`**：后端只认 `response.attestationObject`（CBOR），
   把 `navigator.credentials.create()` 的 `response` **原样**回传即可。
3. **必须 HTTPS 且当前页面域名 == RP ID**：在 `https://ziyit-hacker.github.io/` 下才可用；
   本地 `file://` 打开、或后端 ngrok 页面上，浏览器会直接拒绝（前端要捕获异常给友好提示）。

#### 61.7.2 前端可直接照抄的工具函数与两个流程

> 建议新建 `assets/webauthn.js`（或直接放进 `assets/ziyit_api.js`），以下代码**可直接用**，
> 只依赖浏览器原生 API，**不需要引任何第三方库**。

```javascript
/* ============ 1. base64url <-> ArrayBuffer ============ */

function b64urlToBuf(value) {
  let s = String(value).replace(/-/g, '+').replace(/_/g, '/');
  while (s.length % 4) s += '=';                        // 补回 padding
  const bin = atob(s);
  const bytes = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; i++) bytes[i] = bin.charCodeAt(i);
  return bytes.buffer;
}

function bufToB64url(buf) {
  const bytes = new Uint8Array(buf);
  let bin = '';
  for (let i = 0; i < bytes.length; i++) bin += String.fromCharCode(bytes[i]);
  return btoa(bin).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
}

/* ============ 2. 环境自检（页面加载时跑一次） ============ */

function passkeySupported() {
  // 必须 HTTPS + 浏览器支持 WebAuthn，否则干脆不给用户显示「绑定 Passkey」按钮
  return window.isSecureContext === true
      && typeof window.PublicKeyCredential !== 'undefined'
      && !!navigator.credentials;
}

/* ============ 3. 把后端下发的 publicKey 转成浏览器要的格式 ============ */

function toCreationOptions(publicKey) {           // 注册用
  return {
    publicKey: {
      ...publicKey,
      challenge: b64urlToBuf(publicKey.challenge),                        // ← 必转
      user: {                                                             // ← user.id 也必须转！
        ...publicKey.user,
        id: b64urlToBuf(publicKey.user.id),
      },
      excludeCredentials: (publicKey.excludeCredentials || []).map(function (c) {
        return { type: c.type, id: b64urlToBuf(c.id), transports: c.transports };
      }),
    },
  };
}

function toRequestOptions(publicKey) {            // 登录用
  return {
    publicKey: {
      ...publicKey,
      challenge: b64urlToBuf(publicKey.challenge),                        // ← 必转
      allowCredentials: (publicKey.allowCredentials || []).map(function (c) {
        return { type: c.type, id: b64urlToBuf(c.id), transports: c.transports };
      }),
    },
  };
}

/* ============ 4. 把浏览器回包转成后端要的 JSON ============ */

function serializeCredential(cred) {
  const r = cred.response || {};
  const out = { id: cred.id, type: cred.type, rawId: bufToB64url(cred.rawId), response: {} };
  if (typeof r.clientDataJSON !== 'undefined') out.response.clientDataJSON = bufToB64url(r.clientDataJSON);
  if (typeof r.attestationObject !== 'undefined') out.response.attestationObject = bufToB64url(r.attestationObject);
  if (typeof r.authenticatorData !== 'undefined') out.response.authenticatorData = bufToB64url(r.authenticatorData);
  if (typeof r.signature !== 'undefined') out.response.signature = bufToB64url(r.signature);
  if (r.userHandle) out.response.userHandle = bufToB64url(r.userHandle);
  // 设备类型提示（internal=本机 / hybrid=手机扫码 / usb=安全密钥），后端用来自动起名
  if (typeof r.getTransports === 'function') {
    try { out.response.transports = r.getTransports(); } catch (e) { /* 忽略 */ }
  }
  return out;
}

/* 统一的错误提示：区分「用户取消」和「真的不能用」 */
function passkeyErrorMessage(err) {
  if (!err) return 'Passkey 操作失败，请重试';
  if (err.name === 'NotAllowedError') return '已取消，或操作超时（也可以检查设备是否已设置指纹 / Windows Hello）';
  if (err.name === 'InvalidStateError') return '这个 Passkey 已经绑定过了';
  if (err.name === 'SecurityError') return '当前页面域名不允许使用 Passkey，请从官网 https://ziyit-hacker.github.io/ 打开';
  if (err.name === 'NotSupportedError') return '当前浏览器或设备不支持 Passkey';
  return err.message || 'Passkey 操作失败，请重试';
}
```

**注册（在「账号安全」页里绑定）：**

```javascript
async function bindPasskey(name) {
  // ① 后端发 challenge + 注册参数
  const setup = await api('/auth/security/passkey/setup', { method: 'POST', body: {} });
  // ② 弹 Windows Hello / 指纹，让用户完成验证
  const cred = await navigator.credentials.create(toCreationOptions(setup.publicKey));
  // ③ 原样回传，后端自己解 CBOR 取 authenticatorData
  const result = await api('/auth/security/passkey/enable', {
    method: 'POST',
    body: { ticketId: setup.ticketId, credential: serializeCredential(cred), name: name || undefined },
  });
  // ④ 若 result.accessToken 存在 → 说明是被强制绑定，直接当登录成功处理
  return result;
}
```

**登录（Passkey 当第一因子 / 第二因子都走这一套）：**

```javascript
/**
 * @param {object} opts  { username } 或 { mfaToken }，二选一
 */
async function loginWithPasskey(opts) {
  // ① 取 challengeId 与 allowCredentials（账号没绑过时 allowCredentials 为空数组）
  const start = await api('/auth/login/passkey/start', { method: 'POST', body: opts });
  // ② 弹设备验证
  const cred = await navigator.credentials.get(toRequestOptions(start.publicKey));
  // ③ 验签；返回体与 /auth/login 完全同构（accessToken / mfaRequired+mfaToken / enrollRequired）
  return await api('/auth/login/passkey/finish', {
    method: 'POST',
    body: { challengeId: start.challengeId, credential: serializeCredential(cred) },
  });
}

/* 调用示例 */
// ① 登录页：用户输用户名后点「用 Passkey 登录」（必须先有用户名）
try {
  const r1 = await loginWithPasskey({ username: document.querySelector('#username').value });
  if (r1.accessToken) { /* 登录成功 */ }
  else if (r1.mfaRequired) { /* 还差因子，按 r1.methods 渲染第二步，保存 r1.mfaToken */ }
} catch (e) {
  // 后端在「账号不存在 / 没绑过 Passkey」时直接 400，**绝不会**给出空的凭据列表，
  // 所以这里只会拿到一句能看懂的话，不会触发 Windows Hello 那个「出现问题」弹窗。
  showError(e.message || passkeyErrorMessage(e));
}

// ② 第二步：r1.methods 含 passkey 时
try {
  const r2 = await loginWithPasskey({ mfaToken: savedMfaToken });
  if (r2.accessToken) { /* 登录成功 */ }
} catch (e) {
  showError(e.message || passkeyErrorMessage(e));
}
```

所有 `navigator.credentials.*` 都要 `try/catch`，用上面的 `passkeyErrorMessage(e)` 提示。
**后端返回的 400 消息是可以直接展示给用户的**（如「该账号没有可用的 Passkey，请改用密码或邮箱验证码登录」）。

#### 61.7.3 账号安全页（`user/security.html`）要加的东西

1. **「Passkey」区块**（与 2FA 区块并列）：
   - 显示 `passkeys[]` 列表（`name` + `createdAt` + `lastUsedAt`），每条带「删除」按钮，
     删除时按接口要求弹框收身份证明（`factor` + `password` / `code`）。
   - 「绑定 Passkey」按钮：`passkeySupported()` 为 false 时**置灰并说明原因**（非 HTTPS / 域名不对 / 浏览器不支持）。
   - 绑定成功后刷新 `GET /auth/security` 重新渲染列表。
   - `passkeyCount` 为 0 且账号要求 2 种验证方式时，要提示「建议绑定 Passkey 或 2FA」。
2. **「无密码登录」开关**：只有 `canDisablePassword == true`（邮箱已验证 + 已绑 2FA + 已绑 Passkey）
   才允许关 `passwordEnabled`，否则置灰并给出缺哪一项的说明。
3. **登录页加「用 Passkey 登录」按钮**：放在用户名输入框下方；点它时若用户名已填就当第一因子调
   `loginWithPasskey({username})`，若无用户名则提示先输入用户名（Passkey 不像 2FA 能只凭票据盲找账号）。
   - ⚠ **这个按钮不能"点了就无脑调浏览器"**：账号没绑过 Passkey 时后端会回 **400**，
     前端**直接展示那句 `detail` 即可，千万不要**再调 `navigator.credentials.get()`。
     否则浏览器会拿不到任何凭据，Windows Hello 会弹一句莫名其妙的
     「**使用通行证密钥登录出现问题**」（**这正是之前线上那个报错**）。
   - `passkeySupported()` 为 false 时（非 HTTPS / 域名不对 / 浏览器不支持）**直接隐藏该按钮**。
4. **`methods` 数组里出现 `passkey`** 时，第二步 UI 要给「用 Passkey 完成验证」按钮（走同一个
   `loginWithPasskey({ mfaToken })`）。
5. **提示文案**：Passkey 是「设备钥匙」，**换电脑 / 换手机就刷不了**，务必引导用户
   **至少在两个设备上各绑一个**（上限 10 个），或保留 2FA / 恢复码作为退路。
